// 29 PWA (Progressive Web App) topics for Study Hub.
export default [
  {
    id: "pwa-fundamentals",
    category: "pwa",
    difficulty: "Basic",
    topic: "PWA Fundamentals",
    title: "What is a Progressive Web App?",
    summary: "A web app that uses modern browser APIs to feel and behave like a native app.",
    explanation:
      "A PWA is a website built with progressive enhancement so it works for every browser but gains app-like capabilities in supporting ones: installability, offline access, push notifications, and home-screen presence. The three pillars are HTTPS, a service worker, and a web app manifest. Because it is still a website, a PWA is discoverable via search and shareable via URL, unlike native apps that require an app store.",
    code: "// Core PWA requirements checklist\n// 1. Served over HTTPS\n// 2. Has a linked web app manifest\n// 3. Registers a service worker\nif ('serviceWorker' in navigator) {\n  navigator.serviceWorker.register('/sw.js').then(() => {\n    console.log('PWA service worker registered');\n  });\n}",
    interviewQuestion: "What three technical requirements turn a website into an installable PWA?",
  },
  {
    id: "pwa-web-app-manifest",
    category: "pwa",
    difficulty: "Basic",
    topic: "Web App Manifest",
    title: "What is the Web App Manifest?",
    summary: "A JSON file that describes how the app should appear and behave when installed.",
    explanation:
      "The manifest.json controls the app name, icons, theme colors, start URL, and display mode used when the browser installs the PWA. It is linked from HTML via a link tag and is read by the browser's install prompt logic. Fields like display, orientation, and background_color shape the standalone launch experience so the app looks less like a browser tab and more like a native app.",
    code: "{\n  \"name\": \"DevQuiz Study Hub\",\n  \"short_name\": \"DevQuiz\",\n  \"start_url\": \"/?source=pwa\",\n  \"display\": \"standalone\",\n  \"background_color\": \"#0f172a\",\n  \"theme_color\": \"#6366f1\",\n  \"icons\": [\n    { \"src\": \"/icons/icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\" },\n    { \"src\": \"/icons/icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\" }\n  ]\n}",
    interviewQuestion: "Which manifest field controls whether the browser UI (address bar) is hidden after install?",
  },
  {
    id: "pwa-service-worker-lifecycle",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Service Workers",
    title: "Explain the service worker lifecycle",
    summary: "A service worker goes through install, waiting, activate, and idle/terminated states.",
    explanation:
      "When registered, a service worker downloads and enters the install event where it typically pre-caches assets. It then waits until no other tab controls the page, unless skipWaiting is called. Once activated, it fires the activate event, usually used to clean up old caches, then starts controlling fetch events for open clients. Service workers are event-driven and get terminated when idle to save resources, so state must not be kept in memory between events.",
    code: "self.addEventListener('install', (event) => {\n  event.waitUntil(\n    caches.open('v1').then((cache) => cache.addAll(['/', '/index.html']))\n  );\n  self.skipWaiting();\n});\n\nself.addEventListener('activate', (event) => {\n  event.waitUntil(\n    caches.keys().then((keys) =>\n      Promise.all(keys.filter((k) => k !== 'v1').map((k) => caches.delete(k)))\n    )\n  );\n  self.clients.claim();\n});",
    interviewQuestion: "What is the purpose of the waiting phase in the service worker lifecycle, and how do skipWaiting and clients.claim change it?",
  },
  {
    id: "pwa-caching-strategies",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Service Workers",
    title: "Service worker caching strategies",
    summary: "Different fetch-handling patterns trade off freshness against speed and offline reliability.",
    explanation:
      "Cache-first serves from cache and only hits network on a cache miss, ideal for static assets. Network-first tries the network and falls back to cache, good for content that changes often but should still work offline. Stale-while-revalidate returns the cached response immediately while updating the cache in the background, balancing speed and freshness. The right strategy is chosen per route based on how critical freshness versus speed is for that resource.",
    code: "self.addEventListener('fetch', (event) => {\n  event.respondWith(\n    caches.match(event.request).then((cached) => {\n      const network = fetch(event.request).then((response) => {\n        caches.open('runtime').then((cache) => cache.put(event.request, response.clone()));\n        return response;\n      });\n      // stale-while-revalidate\n      return cached || network;\n    })\n  );\n});",
    interviewQuestion: "When would you choose stale-while-revalidate over a strict cache-first strategy?",
  },
  {
    id: "pwa-offline-first-design",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Offline Design",
    title: "What does offline-first design mean?",
    summary: "Designing the app to treat the network as an enhancement rather than a requirement.",
    explanation:
      "Offline-first means the app's core UI and data are read from local storage (cache, IndexedDB) first, with the network used to sync updates in the background. This differs from a typical online-only app where a failed fetch breaks the UI. It requires rethinking data flow: writes are queued locally and synced when connectivity returns, and UI must reflect sync state (pending, synced, conflict) rather than assuming instant server truth.",
    code: "async function loadNotes() {\n  // Always render from local DB first\n  const local = await db.notes.toArray();\n  render(local);\n  try {\n    const fresh = await fetch('/api/notes').then((r) => r.json());\n    await db.notes.bulkPut(fresh);\n    render(fresh);\n  } catch (err) {\n    // Offline: local data already rendered, no crash\n  }\n}",
    interviewQuestion: "How does offline-first architecture change the way you handle write operations compared to a typical CRUD app?",
  },
  {
    id: "pwa-background-sync",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Background Sync",
    title: "How does the Background Sync API work?",
    summary: "Lets the app defer an action until the device has a stable connection, even if the page is closed.",
    explanation:
      "The Background Sync API registers a sync tag from the page, and the browser fires a sync event on the service worker once connectivity is available, retrying with backoff if it fails. This is useful for queuing form submissions or chat messages made while offline. It is a one-off sync — for recurring syncs you need the separate Periodic Background Sync API. Browser support is inconsistent (notably absent in Safari), so a manual retry-on-reconnect fallback is often needed.",
    code: "// In the page\nnavigator.serviceWorker.ready.then((reg) => {\n  reg.sync.register('sync-messages');\n});\n\n// In the service worker\nself.addEventListener('sync', (event) => {\n  if (event.tag === 'sync-messages') {\n    event.waitUntil(sendQueuedMessages());\n  }\n});",
    interviewQuestion: "Why can't you rely solely on the Background Sync API for cross-browser offline queuing?",
  },
  {
    id: "pwa-push-notifications",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Push Notifications",
    title: "How do push notifications work in a PWA?",
    summary: "The Push API delivers server messages to a service worker, which displays them via the Notifications API.",
    explanation:
      "The client subscribes to push using the PushManager, generating an endpoint tied to a push service (e.g. FCM) plus VAPID keys for authentication. The server sends an encrypted payload to that endpoint, which wakes the service worker's push event even if the app is closed. The service worker then calls showNotification to display it. This requires explicit user permission and works only over HTTPS.",
    code: "const subscription = await registration.pushManager.subscribe({\n  userVisibleOnly: true,\n  applicationServerKey: vapidPublicKey,\n});\nawait fetch('/api/subscribe', {\n  method: 'POST',\n  body: JSON.stringify(subscription),\n});\n\n// service worker\nself.addEventListener('push', (event) => {\n  const data = event.data.json();\n  event.waitUntil(self.registration.showNotification(data.title, { body: data.body }));\n});",
    interviewQuestion: "What role do VAPID keys play in web push, and why are they needed?",
  },
  {
    id: "pwa-add-to-home-screen",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Installability",
    title: "How does Add to Home Screen (A2HS) work?",
    summary: "Browsers offer an install prompt that adds a home screen icon launching the PWA in standalone mode.",
    explanation:
      "Chromium browsers fire a beforeinstallprompt event when installability criteria are met, which the app can capture to defer and trigger a custom install button instead of the default mini-infobar. Calling prompt() on the saved event shows the native install dialog, and the appinstalled event confirms success. Safari on iOS has no beforeinstallprompt — users must manually use Share then Add to Home Screen.",
    code: "let deferredPrompt;\nwindow.addEventListener('beforeinstallprompt', (e) => {\n  e.preventDefault();\n  deferredPrompt = e;\n  showCustomInstallButton();\n});\n\ninstallButton.addEventListener('click', async () => {\n  deferredPrompt.prompt();\n  const { outcome } = await deferredPrompt.userChoice;\n  console.log('User choice:', outcome);\n  deferredPrompt = null;\n});",
    interviewQuestion: "Why would you call preventDefault on beforeinstallprompt, and what UX problem does that solve?",
  },
  {
    id: "pwa-app-shell-architecture",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Architecture",
    title: "What is App Shell architecture?",
    summary: "A design pattern that caches the minimal static UI shell separately from dynamic content.",
    explanation:
      "The app shell is the minimal HTML, CSS, and JS needed to render the UI skeleton — navigation, layout, headers — without the data. It's precached during service worker install so it loads instantly, even offline, while dynamic content (like API data) is fetched separately and streamed into the shell. This pattern gives the perceived performance of a native app because the frame renders immediately on repeat visits.",
    code: "// Precache the shell only, not dynamic API data\nconst SHELL_ASSETS = [\n  '/', '/index.html', '/app.css', '/app-shell.js', '/icons/icon-192.png',\n];\n\nself.addEventListener('install', (event) => {\n  event.waitUntil(\n    caches.open('shell-v1').then((cache) => cache.addAll(SHELL_ASSETS))\n  );\n});",
    interviewQuestion: "How does app shell architecture differ from simply caching every page of the app?",
  },
  {
    id: "pwa-workbox",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Tooling",
    title: "What is Workbox and why use it?",
    summary: "A Google library that abstracts common service worker patterns like routing and caching strategies.",
    explanation:
      "Writing raw service worker fetch handling for many routes gets repetitive and error-prone. Workbox provides prebuilt caching strategies (CacheFirst, NetworkFirst, StaleWhileRevalidate), a routing API, precaching with automatic manifest generation, and plugins for expiration and background sync. It integrates with build tools like Webpack (workbox-webpack-plugin) or Vite (vite-plugin-pwa) to generate the service worker at build time based on your actual asset output.",
    code: "import { registerRoute } from 'workbox-routing';\nimport { StaleWhileRevalidate, CacheFirst } from 'workbox-strategies';\nimport { ExpirationPlugin } from 'workbox-expiration';\n\nregisterRoute(\n  ({ request }) => request.destination === 'image',\n  new CacheFirst({\n    cacheName: 'images',\n    plugins: [new ExpirationPlugin({ maxEntries: 60, maxAgeSeconds: 30 * 24 * 60 * 60 })],\n  })\n);",
    interviewQuestion: "What problems does Workbox solve that make it preferable to hand-writing every fetch handler?",
  },
  {
    id: "pwa-vs-native-tradeoffs",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Strategy",
    title: "PWA vs native app: what are the tradeoffs?",
    summary: "PWAs win on reach, cost, and update speed; native apps win on deep OS integration and raw performance.",
    explanation:
      "PWAs are cross-platform from one codebase, indexable by search engines, installable without an app store, and update instantly since there's no review process. Native apps have fuller access to OS APIs (Bluetooth, deep background processing, complex file system access), typically better performance for graphics-heavy work, and stronger discoverability via app stores. The decision often comes down to whether the app needs deep hardware integration or whether reach and iteration speed matter more.",
    code: "// Feature-detect before relying on capabilities that\n// differ sharply between PWA and native environments\nif ('bluetooth' in navigator) {\n  // Limited/partial support in PWAs, broad in native\n} else {\n  showFallbackUI('Bluetooth pairing requires the native app');\n}",
    interviewQuestion: "In what scenario would you recommend a native app over a PWA, and why?",
  },
  {
    id: "pwa-indexeddb",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Offline Storage",
    title: "Why use IndexedDB for offline storage?",
    summary: "A transactional, asynchronous, object-based database in the browser suited for structured offline data.",
    explanation:
      "Unlike localStorage, which is synchronous, string-only, and capped around 5-10MB, IndexedDB stores structured objects (including blobs), supports indexes for efficient querying, and runs asynchronously so it doesn't block the main thread. It's the standard choice for offline-first apps needing to store large or complex datasets like cached API responses, user-generated content, or sync queues. Libraries like Dexie or idb wrap its verbose callback/event API with promises.",
    code: "import { openDB } from 'idb';\n\nconst db = await openDB('quiz-app', 1, {\n  upgrade(db) {\n    const store = db.createObjectStore('answers', { keyPath: 'id' });\n    store.createIndex('by-synced', 'synced');\n  },\n});\n\nawait db.put('answers', { id: 'q1', value: 'B', synced: false });\nconst pending = await db.getAllFromIndex('answers', 'by-synced', false);",
    interviewQuestion: "Why is IndexedDB preferred over localStorage for offline-first data storage?",
  },
  {
    id: "pwa-cache-api-deep-dive",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Service Workers",
    title: "Cache API deep dive",
    summary: "A low-level storage API for Request/Response pairs, the foundation service workers use for asset caching.",
    explanation:
      "The Cache API (via caches.open) stores full Request/Response object pairs, unlike IndexedDB which stores structured data. Cache.addAll fetches and stores multiple URLs atomically — if one fails, none are cached. Matching uses cache.match with options like ignoreSearch or ignoreVary to control how strictly the request must match. Multiple named caches let you version assets (v1, v2) so you can atomically swap and clean up old versions during activation.",
    code: "const cache = await caches.open('assets-v2');\nawait cache.put('/data.json', new Response(JSON.stringify({ ok: true })));\n\nconst match = await cache.match('/data.json', { ignoreSearch: true });\nconst allKeys = await cache.keys();\nawait cache.delete('/old-asset.js');",
    interviewQuestion: "What happens if one URL fails during cache.addAll, and how would you handle partial caching failures?",
  },
  {
    id: "pwa-installability-criteria",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Installability",
    title: "What are Chrome's PWA installability criteria?",
    summary: "A specific checklist the browser evaluates before offering the install prompt.",
    explanation:
      "Chrome requires the app be served over HTTPS, have a valid web app manifest with name/short_name, icons (including a 192x192 and 512x512), start_url, and a display mode of standalone or fullscreen, plus a registered service worker with a fetch event handler. Engagement heuristics also apply — the browser may wait for some user interaction before showing the prompt. Missing any of these silently disables the automatic install prompt, so DevTools' Application panel and Lighthouse are used to debug which criterion failed.",
    code: "// manifest.json must satisfy all of these for installability:\n{\n  \"name\": \"Study Hub\",\n  \"short_name\": \"StudyHub\",\n  \"start_url\": \"/\",\n  \"display\": \"standalone\",\n  \"icons\": [\n    { \"src\": \"icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\" },\n    { \"src\": \"icon-512.png\", \"sizes\": \"512x512\", \"type\": \"image/png\" }\n  ]\n}",
    interviewQuestion: "If the install prompt never fires, what are the first things you'd check?",
  },
  {
    id: "pwa-periodic-background-sync",
    category: "pwa",
    difficulty: "Tricky",
    topic: "Background Sync",
    title: "How does Periodic Background Sync differ from one-off sync?",
    summary: "A separate, more restricted API that lets a PWA refresh content on a recurring schedule in the background.",
    explanation:
      "Periodic Background Sync registers a recurring task with a minInterval, and the browser decides the actual frequency based on factors like site engagement, battery, and network conditions — it is not guaranteed to fire exactly on schedule. Unlike one-off Background Sync, it requires the PWA to already be installed and typically requires explicit permission via the periodic-background-sync permission. It's currently only supported in Chromium-based browsers, and even there the browser can throttle or skip syncs entirely for low-engagement sites.",
    code: "const status = await navigator.permissions.query({ name: 'periodic-background-sync' });\nif (status.state === 'granted') {\n  const registration = await navigator.serviceWorker.ready;\n  await registration.periodicSync.register('refresh-content', {\n    minInterval: 24 * 60 * 60 * 1000, // 1 day, best-effort\n  });\n}",
    interviewQuestion: "Why can't you rely on Periodic Background Sync to fire at exactly the minInterval you specify?",
  },
  {
    id: "pwa-web-share-api",
    category: "pwa",
    difficulty: "Basic",
    topic: "Web APIs",
    title: "What does the Web Share API do?",
    summary: "Lets a web app trigger the OS-native share sheet to share text, links, or files.",
    explanation:
      "navigator.share() opens the platform's native share UI, letting users send content to installed apps like Messages, Mail, or social apps, matching native app UX instead of custom share buttons. It requires a user gesture (like a click) and HTTPS. navigator.canShare() lets you feature-detect support, including whether file sharing is supported, before calling share so you can fall back to custom share buttons on unsupported browsers.",
    code: "shareButton.addEventListener('click', async () => {\n  if (navigator.canShare && navigator.canShare({ url: location.href })) {\n    await navigator.share({\n      title: 'DevQuiz Study Hub',\n      text: 'Check out this PWA topic',\n      url: location.href,\n    });\n  } else {\n    showFallbackShareMenu();\n  }\n});",
    interviewQuestion: "Why must navigator.share be called from within a user gesture handler?",
  },
  {
    id: "pwa-badging-api",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Web APIs",
    title: "What is the Badging API used for?",
    summary: "Lets an installed PWA show a small numeric or dot badge on its app icon, like unread counts.",
    explanation:
      "navigator.setAppBadge(count) sets a badge on the app icon (taskbar, dock, or home screen) for installed PWAs, commonly used for unread message or notification counts. Calling it with no argument shows a generic dot, and clearAppBadge() removes it. It only affects the icon when the app is installed, and support varies by OS/browser — notably it has better support on desktop and Android than iOS Safari.",
    code: "async function updateUnreadBadge(count) {\n  if ('setAppBadge' in navigator) {\n    if (count > 0) {\n      await navigator.setAppBadge(count);\n    } else {\n      await navigator.clearAppBadge();\n    }\n  }\n}",
    interviewQuestion: "Does the Badging API work if the PWA is only open in a browser tab and not installed?",
  },
  {
    id: "pwa-file-system-access-api",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Web APIs",
    title: "What is the File System Access API?",
    summary: "Lets a PWA read from and write directly to the user's local file system with permission.",
    explanation:
      "showOpenFilePicker and showSaveFilePicker let web apps request handles to real files on disk, enabling true 'open/save/edit in place' workflows like a native text editor or IDE running in the browser. Access is permission-gated per file/directory and requires a user gesture to initiate. It is currently Chromium-only (no Firefox or Safari support), so apps needing broad compatibility must fall back to traditional file input plus download-link patterns.",
    code: "async function openTextFile() {\n  const [handle] = await window.showOpenFilePicker({\n    types: [{ description: 'Text', accept: { 'text/plain': ['.txt'] } }],\n  });\n  const file = await handle.getFile();\n  const text = await file.text();\n\n  const writable = await handle.createWritable();\n  await writable.write(text + '\\nedited');\n  await writable.close();\n}",
    interviewQuestion: "What fallback strategy would you use for browsers that don't support the File System Access API?",
  },
  {
    id: "pwa-performance-optimization",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Performance",
    title: "How do you optimize PWA performance?",
    summary: "Combine precaching, code splitting, and efficient runtime caching to hit fast, reliable load times.",
    explanation:
      "Key techniques include precaching the app shell so repeat loads are instant, lazy-loading routes and components to shrink the initial bundle, using stale-while-revalidate for API data so the UI never blocks on network, and setting cache expiration so storage doesn't grow unbounded. Core Web Vitals (LCP, INP, CLS) should still be measured for PWAs just like regular sites, since a service worker doesn't fix a bloated JS bundle or render-blocking resources — it only affects repeat-visit network behavior.",
    code: "// Route-level code splitting keeps the initial\n// service-worker-precached bundle small\nconst QuizPage = React.lazy(() => import('./pages/QuizPage'));\n\nregisterRoute(\n  ({ url }) => url.pathname.startsWith('/api/questions'),\n  new StaleWhileRevalidate({ cacheName: 'questions-api' })\n);",
    interviewQuestion: "Does adding a service worker automatically improve first-visit load performance? Why or why not?",
  },
  {
    id: "pwa-update-strategies",
    category: "pwa",
    difficulty: "Tricky",
    topic: "Service Workers",
    title: "How do you handle PWA update strategies safely?",
    summary: "New service worker versions must be rolled out carefully to avoid serving mismatched app versions.",
    explanation:
      "By default, a new service worker installs but stays 'waiting' until all tabs of the old version close, preventing a half-updated app (new JS calling old cached API shapes, etc). Calling skipWaiting immediately activates the new worker, which can cause a currently-open tab to suddenly run new code against stale in-memory state — so it's common to instead detect the waiting worker and prompt the user to reload. postMessage is used to tell the waiting worker to skip waiting on demand, then controllerchange triggers a full page reload.",
    code: "navigator.serviceWorker.register('/sw.js').then((reg) => {\n  reg.addEventListener('updatefound', () => {\n    const newWorker = reg.installing;\n    newWorker.addEventListener('statechange', () => {\n      if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {\n        showUpdateToast(() => newWorker.postMessage({ type: 'SKIP_WAITING' }));\n      }\n    });\n  });\n});\n\nnavigator.serviceWorker.addEventListener('controllerchange', () => location.reload());",
    interviewQuestion: "Why is calling skipWaiting immediately on install considered risky, and what's the safer pattern?",
  },
  {
    id: "pwa-security-considerations",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Security",
    title: "What are the key security considerations for PWAs?",
    summary: "HTTPS, scope control, and careful cache/message handling prevent PWAs from becoming attack surfaces.",
    explanation:
      "Service workers require HTTPS specifically because they can intercept and rewrite every network request in their scope, so a compromised worker is a powerful MITM vector — this is why browsers refuse to register them over plain HTTP (except localhost). Scope should be as narrow as possible to limit blast radius. Cached responses can leak stale sensitive data if auth-gated content is cached without care, so APIs returning user-specific data typically use NetworkOnly or short-lived cache with proper Vary/credentials handling. Messages received via postMessage from pages should be validated since any page in scope can message the worker.",
    code: "// Never cache authenticated/sensitive API responses broadly\nregisterRoute(\n  ({ url }) => url.pathname.startsWith('/api/account'),\n  new NetworkOnly()\n);\n\n// Validate postMessage origin/data before acting on it\nself.addEventListener('message', (event) => {\n  if (event.data && event.data.type === 'SKIP_WAITING') {\n    self.skipWaiting();\n  }\n});",
    interviewQuestion: "Why does the service worker spec effectively mandate HTTPS, and what attack does that prevent?",
  },
  {
    id: "pwa-testing",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Testing",
    title: "How do you test a PWA?",
    summary: "PWAs need testing across install flow, offline behavior, caching correctness, and push/notification handling.",
    explanation:
      "Chrome DevTools' Application panel lets you inspect manifest validity, service worker state, and cache contents, plus simulate offline mode and force-update the worker. Automated testing can use tools like Puppeteer or Playwright to script install prompts and offline fetch behavior, and Workbox provides testing utilities for cache strategies. It's important to test the update flow specifically (old worker to new worker transition) since that's a common source of production bugs that don't show up in a fresh install test.",
    code: "// Playwright: simulate offline mode and verify app still renders\nawait page.goto('https://app.example.com');\nawait page.context().setOffline(true);\nawait page.reload();\nawait expect(page.locator('h1')).toBeVisible();",
    interviewQuestion: "What PWA-specific behaviors are easy to miss when only testing a fresh install and not an update?",
  },
  {
    id: "pwa-lighthouse-audit",
    category: "pwa",
    difficulty: "Basic",
    topic: "Testing",
    title: "What does the Lighthouse PWA audit check?",
    summary: "An automated tool that scores installability, offline support, and best practices for PWAs.",
    explanation:
      "Lighthouse (built into Chrome DevTools) audits things like whether the manifest is valid and linked, whether a service worker responds with a 200 when offline, whether icons meet size requirements, and whether the page is served over HTTPS with a redirect from HTTP. It's a useful first checkpoint but passing it doesn't guarantee a great offline experience — it verifies the checklist, not the quality of your caching strategy or UX.",
    code: "// Run Lighthouse PWA audit from CLI\n// npx lighthouse https://app.example.com --only-categories=pwa --view\n\n// Common failures it flags:\n// - manifest missing 'start_url'\n// - no 200 response when offline\n// - icons missing required sizes (192x192, 512x512)",
    interviewQuestion: "If a site passes the Lighthouse PWA audit, does that guarantee a good offline user experience? Why not?",
  },
  {
    id: "pwa-cross-platform-considerations",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Cross-Platform",
    title: "What cross-platform inconsistencies should you plan for in PWAs?",
    summary: "PWA feature support varies significantly between Chrome/Android, desktop, and iOS Safari.",
    explanation:
      "Chrome/Android and desktop Chromium browsers have the broadest support: install prompts, push notifications, background sync, badging. iOS Safari historically lagged — push notifications only arrived in iOS 16.4+ and only for installed (A2HS) apps, and APIs like Background Sync and File System Access remain unsupported. Because of this, PWAs need feature detection everywhere and graceful fallbacks, and teams often maintain a capability matrix documenting what works where, updated as browser versions change.",
    code: "function getPwaCapabilities() {\n  return {\n    installPrompt: 'onbeforeinstallprompt' in window,\n    push: 'PushManager' in window,\n    backgroundSync: 'serviceWorker' in navigator && 'SyncManager' in window,\n    badging: 'setAppBadge' in navigator,\n    fileSystemAccess: 'showOpenFilePicker' in window,\n  };\n}",
    interviewQuestion: "How would you design a feature so it degrades gracefully on iOS Safari, which lacks several PWA APIs available on Chrome/Android?",
  },
  {
    id: "pwa-trusted-web-activities",
    category: "pwa",
    difficulty: "Advanced",
    topic: "Android",
    title: "What is a Trusted Web Activity (TWA)?",
    summary: "A way to package a PWA as an installable Android app that renders full-screen Chrome without browser UI.",
    explanation:
      "A TWA wraps a PWA in a thin native Android shell using Chrome Custom Tabs under the hood, letting you publish the PWA to the Google Play Store as a real APK/AAB. It uses Digital Asset Links to verify the native app and the website are owned by the same party, which removes the browser chrome entirely for a fully native-feeling window. Unlike a WebView wrapper, a TWA shares Chrome's cookies, storage, and service worker cache with the regular browser, and Bubblewrap or PWABuilder are common tools to generate the wrapper project.",
    code: "// assetlinks.json served at /.well-known/assetlinks.json\n[{\n  \"relation\": [\"delegate_permission/common.handle_all_urls\"],\n  \"target\": {\n    \"namespace\": \"android_app\",\n    \"package_name\": \"com.example.studyhub\",\n    \"sha256_cert_fingerprints\": [\"AA:BB:CC:...\"]\n  }\n}]",
    interviewQuestion: "How does a Trusted Web Activity differ from wrapping a site in a plain Android WebView?",
  },
  {
    id: "pwa-ios-limitations",
    category: "pwa",
    difficulty: "Tricky",
    topic: "iOS",
    title: "What are the key PWA limitations on iOS?",
    summary: "iOS Safari historically restricts storage, background execution, and several capability APIs for PWAs.",
    explanation:
      "iOS has no beforeinstallprompt event, so installation is manual via Share then Add to Home Screen, and there's no way to programmatically prompt for install. Web Push only became available in iOS 16.4+, and only works for apps added to the home screen, not in Safari tabs. Background Sync and Periodic Background Sync are unsupported. Historically, iOS also aggressively evicted storage (including IndexedDB and Cache Storage) for PWAs not opened in a while, though this has improved for installed home-screen apps. These constraints mean iOS often needs its own degraded but functional experience rather than assuming parity with Chrome/Android.",
    code: "function isIOS() {\n  return /iP(hone|od|ad)/.test(navigator.userAgent);\n}\n\nfunction isIOSInstalled() {\n  return isIOS() && window.navigator.standalone === true;\n}\n\nif (isIOS() && !isIOSInstalled()) {\n  showManualInstallInstructions(); // 'Tap Share, then Add to Home Screen'\n}",
    interviewQuestion: "Since iOS has no beforeinstallprompt event, how would you guide users to install your PWA on an iPhone?",
  },
  {
    id: "pwa-splash-screens",
    category: "pwa",
    difficulty: "Basic",
    topic: "Visual Presentation",
    title: "How do splash screens work for installed PWAs?",
    summary: "A branded loading screen shown while the PWA launches from the home screen, generated from manifest data.",
    explanation:
      "On Android, Chrome auto-generates the splash screen from the manifest's name, background_color, theme_color, and the largest matching icon — there is no separate splash image field. iOS Safari, by contrast, requires explicit apple-touch-startup-image link tags per device resolution since it doesn't read the manifest the same way. Getting the background_color to match your app's actual first-paint background avoids a jarring flash between splash and content.",
    code: "<!-- iOS-specific splash screens (not read from manifest.json) -->\n<link rel='apple-touch-startup-image'\n  href='/splash/iphone-1170x2532.png'\n  media='(device-width: 390px) and (device-height: 844px) and (-webkit-device-pixel-ratio: 3)'>\n\n<!-- Android splash is auto-generated from these manifest fields -->\n<!-- background_color, theme_color, name, and largest icon -->",
    interviewQuestion: "Why does iOS need explicit apple-touch-startup-image tags when Android generates the splash screen automatically?",
  },
  {
    id: "pwa-app-icons-screenshots",
    category: "pwa",
    difficulty: "Basic",
    topic: "Visual Presentation",
    title: "What icon and screenshot sizes does a PWA need?",
    summary: "Manifests need multiple icon sizes plus optional screenshots for richer install UI.",
    explanation:
      "At minimum, a manifest needs 192x192 and 512x512 icons for installability; additional sizes (48, 72, 96, 144, 384) improve rendering across devices and platform launchers. Maskable icons (purpose: 'maskable') provide safe-zone padding so Android can crop them into circles or squircles without clipping content. The screenshots array, with form_factor set to wide or narrow, lets richer install UI (like Chrome's expanded install dialog) show app previews similar to an app store listing.",
    code: "{\n  \"icons\": [\n    { \"src\": \"icon-192.png\", \"sizes\": \"192x192\", \"type\": \"image/png\", \"purpose\": \"any\" },\n    { \"src\": \"icon-512-maskable.png\", \"sizes\": \"512x512\", \"type\": \"image/png\", \"purpose\": \"maskable\" }\n  ],\n  \"screenshots\": [\n    { \"src\": \"screen-wide.png\", \"sizes\": \"1280x720\", \"type\": \"image/png\", \"form_factor\": \"wide\" }\n  ]\n}",
    interviewQuestion: "What problem do maskable icons solve on Android that a regular square icon doesn't?",
  },
  {
    id: "pwa-web-push-protocol",
    category: "pwa",
    difficulty: "Tricky",
    topic: "Push Notifications",
    title: "How does the underlying Web Push protocol work?",
    summary: "A standardized protocol (RFC 8030 family) where the app server pushes encrypted messages via a push service.",
    explanation:
      "After a client subscribes, the browser vendor's push service (e.g. FCM for Chrome, Mozilla's service for Firefox) returns an endpoint URL and keys. The app server encrypts the payload using the client's public key (via ECDH and the aes128gcm content-encoding defined in RFC 8291) and signs the request with VAPID (a JWT signed with the server's private key, per RFC 8292) so the push service can verify the sender without trusting arbitrary origins. The push service then relays the encrypted message to the device, which decrypts it in the browser before the service worker's push event fires — meaning the push service itself never sees the plaintext payload.",
    code: "// Server-side (Node, using web-push library)\nconst webpush = require('web-push');\nwebpush.setVapidDetails('mailto:admin@example.com', publicKey, privateKey);\n\nawait webpush.sendNotification(\n  subscription, // { endpoint, keys: { p256dh, auth } }\n  JSON.stringify({ title: 'New quiz available' })\n);",
    interviewQuestion: "Why can the push service relay your notification payload without being able to read its contents?",
  },
  {
    id: "pwa-analytics",
    category: "pwa",
    difficulty: "Intermediate",
    topic: "Analytics",
    title: "What's different about analytics for PWAs?",
    summary: "PWAs need to track install, offline usage, and standalone-vs-browser context that typical web analytics miss.",
    explanation:
      "Standard pageview analytics can undercount PWA usage because offline sessions may not reach the analytics server immediately, requiring queuing events in IndexedDB and flushing them via background sync when connectivity returns. Useful PWA-specific metrics include install rate (from the appinstalled event), display-mode breakdown (standalone vs browser vs TWA, via matchMedia('(display-mode: standalone)')), service worker cache hit ratio, and push notification opt-in/engagement rates. These help distinguish engaged installed users from casual browser visitors.",
    code: "window.addEventListener('appinstalled', () => {\n  analytics.track('pwa_installed');\n});\n\nconst isStandalone = window.matchMedia('(display-mode: standalone)').matches;\nanalytics.setUserProperty('launch_context', isStandalone ? 'standalone' : 'browser');\n\n// Queue events while offline, flush on reconnect\nwindow.addEventListener('online', () => flushQueuedAnalyticsEvents());",
    interviewQuestion: "Why might a PWA's analytics undercount engaged users if you only rely on standard pageview tracking?",
  },
];
