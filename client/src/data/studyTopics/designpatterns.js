// 30 Design Patterns topics for Study Hub.
export default [
  {
    "id": "designpatterns-singleton-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Creational Patterns",
    "title": "What is the Singleton Pattern?",
    "summary": "Singleton ensures a class has only one instance and provides a single global point of access to it.",
    "explanation": "The Singleton pattern restricts instantiation of a class to a single object, typically by making the constructor private (or private-ish) and exposing a static method that creates the instance on first call and returns the cached instance afterward. It is commonly used for shared resources like configuration objects, logging services, or database connections. Overuse is discouraged because it introduces global state, making unit testing harder and creating hidden dependencies between modules. In JavaScript, ES modules naturally provide singleton-like behavior since a module is only evaluated once and cached on subsequent imports.",
    "code": "class Logger {\n  static #instance;\n\n  constructor() {\n    if (Logger.#instance) {\n      throw new Error('Use Logger.getInstance()');\n    }\n    this.logs = [];\n  }\n\n  static getInstance() {\n    if (!Logger.#instance) {\n      Logger.#instance = new Logger();\n    }\n    return Logger.#instance;\n  }\n\n  log(msg) {\n    this.logs.push(msg);\n  }\n}\n\nconst a = Logger.getInstance();\nconst b = Logger.getInstance();\nconsole.log(a === b); // true",
    "interviewQuestion": "How would you implement a thread-safe Singleton in JavaScript, and why is Singleton considered an anti-pattern by some developers?"
  },
  {
    "id": "designpatterns-factory-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Creational Patterns",
    "title": "What is the Factory Pattern?",
    "summary": "Factory pattern delegates object creation to a dedicated method or function instead of calling a constructor directly.",
    "explanation": "The Factory Method pattern defines an interface or function for creating an object, but lets subclasses or the factory logic decide which concrete class to instantiate. This decouples client code from the concrete classes it needs to instantiate, making it easy to add new product types without changing calling code. It is especially useful when object creation involves conditional logic based on input parameters or configuration. A simple factory function in JavaScript can replace verbose class hierarchies while still achieving the same decoupling benefit.",
    "code": "class Car { drive() { return 'Driving a car'; } }\nclass Truck { drive() { return 'Driving a truck'; } }\n\nfunction vehicleFactory(type) {\n  switch (type) {\n    case 'car':\n      return new Car();\n    case 'truck':\n      return new Truck();\n    default:\n      throw new Error('Unknown vehicle type: ' + type);\n  }\n}\n\nconst v = vehicleFactory('car');\nconsole.log(v.drive()); // Driving a car",
    "interviewQuestion": "What is the difference between the Factory Method pattern and simply calling 'new' directly, and when would you introduce a factory?"
  },
  {
    "id": "designpatterns-abstract-factory-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Creational Patterns",
    "title": "What is the Abstract Factory Pattern?",
    "summary": "Abstract Factory provides an interface for creating families of related objects without specifying their concrete classes.",
    "explanation": "While a simple Factory creates one type of object, an Abstract Factory creates families of related objects that are designed to work together, such as UI components matching a theme (light vs dark buttons, checkboxes, and inputs). Each concrete factory implements the abstract factory interface and produces a consistent family of products. This pattern enforces consistency among products and makes it easy to swap an entire product family by swapping the factory implementation. It adds a layer of indirection, so it should be used only when there truly are multiple related product families.",
    "code": "const lightFactory = {\n  createButton: () => ({ style: 'light-button' }),\n  createCheckbox: () => ({ style: 'light-checkbox' }),\n};\n\nconst darkFactory = {\n  createButton: () => ({ style: 'dark-button' }),\n  createCheckbox: () => ({ style: 'dark-checkbox' }),\n};\n\nfunction renderUI(factory) {\n  const button = factory.createButton();\n  const checkbox = factory.createCheckbox();\n  return [button, checkbox];\n}\n\nconsole.log(renderUI(darkFactory));",
    "interviewQuestion": "How does Abstract Factory differ from Factory Method, and can you give a real-world example where you'd need families of related objects?"
  },
  {
    "id": "designpatterns-builder-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Creational Patterns",
    "title": "What is the Builder Pattern?",
    "summary": "Builder separates the construction of a complex object from its representation, allowing step-by-step construction with a fluent API.",
    "explanation": "The Builder pattern is useful when an object requires many optional parameters or a multi-step construction process that would otherwise lead to unwieldy constructors ('telescoping constructors'). A builder exposes chainable methods to set each property and a final build() method that returns the fully constructed object. This improves readability and allows validation before the object is finalized. It is widely used for constructing SQL queries, HTTP requests, and configuration objects.",
    "code": "class PizzaBuilder {\n  constructor() {\n    this.toppings = [];\n  }\n  addTopping(t) {\n    this.toppings.push(t);\n    return this;\n  }\n  setSize(size) {\n    this.size = size;\n    return this;\n  }\n  build() {\n    return { size: this.size, toppings: this.toppings };\n  }\n}\n\nconst pizza = new PizzaBuilder()\n  .setSize('large')\n  .addTopping('cheese')\n  .addTopping('mushroom')\n  .build();\n\nconsole.log(pizza);",
    "interviewQuestion": "Why would you use the Builder pattern instead of a constructor with many optional parameters?"
  },
  {
    "id": "designpatterns-prototype-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Creational Patterns",
    "title": "What is the Prototype Pattern?",
    "summary": "Prototype creates new objects by cloning an existing object (the prototype) instead of instantiating a class from scratch.",
    "explanation": "The Prototype pattern is useful when object creation is expensive (e.g., involves heavy computation or I/O) or when you want new objects to start with the same state as an existing object. JavaScript supports this natively through its prototype-based inheritance model and Object.create(), as well as via structuredClone or spread syntax for shallow/deep copying. Cloning avoids re-running complex initialization logic and lets you produce variations of a base object efficiently. Care must be taken to distinguish shallow copies (which share nested references) from deep copies.",
    "code": "const carPrototype = {\n  wheels: 4,\n  describe() {\n    return this.brand + ' with ' + this.wheels + ' wheels';\n  },\n};\n\nfunction createCar(brand) {\n  const car = Object.create(carPrototype);\n  car.brand = brand;\n  return car;\n}\n\nconst tesla = createCar('Tesla');\nconsole.log(tesla.describe()); // Tesla with 4 wheels\nconsole.log(Object.getPrototypeOf(tesla) === carPrototype); // true",
    "interviewQuestion": "How does the Prototype pattern relate to JavaScript's native prototype chain, and when is cloning preferable to instantiating a new object?"
  },
  {
    "id": "designpatterns-adapter-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Structural Patterns",
    "title": "What is the Adapter Pattern?",
    "summary": "Adapter converts the interface of a class into another interface that clients expect, letting incompatible interfaces work together.",
    "explanation": "The Adapter pattern acts as a translation layer between two incompatible interfaces, wrapping an existing class or API and exposing the methods clients expect. This is extremely common when integrating third-party libraries, legacy code, or migrating from one API to another without changing consumer code. It follows the single-responsibility principle by isolating interface-conversion logic from business logic. Adapters can wrap objects (object adapter) or use inheritance (class adapter), though JavaScript favors the object composition approach.",
    "code": "class OldPrinter {\n  printOld(text) {\n    return 'Old: ' + text;\n  }\n}\n\n// New interface expected by client code\nclass PrinterAdapter {\n  constructor(oldPrinter) {\n    this.oldPrinter = oldPrinter;\n  }\n  print(text) {\n    return this.oldPrinter.printOld(text);\n  }\n}\n\nconst adapter = new PrinterAdapter(new OldPrinter());\nconsole.log(adapter.print('Hello')); // Old: Hello",
    "interviewQuestion": "Can you describe a real scenario where you used or would use an Adapter to integrate a third-party library?"
  },
  {
    "id": "designpatterns-decorator-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Structural Patterns",
    "title": "What is the Decorator Pattern?",
    "summary": "Decorator attaches additional responsibilities to an object dynamically without altering its structure or affecting other instances.",
    "explanation": "Decorators wrap an object with another object that implements the same interface, adding new behavior before or after delegating to the wrapped object. This provides a flexible alternative to subclassing for extending functionality, since behaviors can be composed at runtime in any combination. Common examples include middleware chains, React higher-order components, and TypeScript's @decorator syntax for classes and methods. Each decorator should remain transparent to the client, i.e., preserve the original interface.",
    "code": "function withLogging(fn) {\n  return function (...args) {\n    console.log('Calling with args:', args);\n    const result = fn(...args);\n    console.log('Result:', result);\n    return result;\n  };\n}\n\nfunction add(a, b) {\n  return a + b;\n}\n\nconst loggedAdd = withLogging(add);\nloggedAdd(2, 3);",
    "interviewQuestion": "How is the Decorator pattern different from simple inheritance, and where have you seen it used in frontend frameworks?"
  },
  {
    "id": "designpatterns-facade-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Structural Patterns",
    "title": "What is the Facade Pattern?",
    "summary": "Facade provides a simplified, unified interface to a complex subsystem of classes, hiding implementation details from clients.",
    "explanation": "The Facade pattern introduces a single entry-point object that internally coordinates calls to multiple subsystem components, reducing the coupling between client code and subsystem internals. It makes a library or subsystem easier to use by exposing only the high-level operations clients actually need. Facades don't prevent direct access to subsystem classes when needed for advanced use cases, they just provide a convenient default path. This is commonly seen in SDK wrappers, jQuery-style APIs over the DOM, and service layers over multiple backend calls.",
    "code": "class CPU { start() { console.log('CPU started'); } }\nclass Memory { load() { console.log('Memory loaded'); } }\nclass HardDrive { read() { console.log('Disk read'); } }\n\nclass ComputerFacade {\n  constructor() {\n    this.cpu = new CPU();\n    this.memory = new Memory();\n    this.disk = new HardDrive();\n  }\n  start() {\n    this.cpu.start();\n    this.memory.load();\n    this.disk.read();\n    console.log('Computer ready');\n  }\n}\n\nnew ComputerFacade().start();",
    "interviewQuestion": "How does the Facade pattern help reduce coupling in large systems, and what is the trade-off of hiding subsystem complexity?"
  },
  {
    "id": "designpatterns-proxy-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Structural Patterns",
    "title": "What is the Proxy Pattern?",
    "summary": "Proxy provides a placeholder or surrogate object that controls access to another object, adding checks, caching, or lazy loading.",
    "explanation": "A Proxy sits between the client and the real subject, implementing the same interface so it can be used transparently in place of the real object. Common proxy variants include virtual proxies (lazy-load expensive objects), protection proxies (access control), logging proxies, and caching proxies. JavaScript has a native Proxy object that intercepts fundamental operations like get, set, and deleteProperty via traps, making it easy to implement validation, reactivity (as Vue 3 does), or logging without modifying the target object. Unlike Decorator, which adds behavior, Proxy primarily controls access to the underlying object.",
    "code": "const bankAccount = { balance: 1000 };\n\nconst accountProxy = new Proxy(bankAccount, {\n  get(target, prop) {\n    console.log('Reading ' + String(prop));\n    return target[prop];\n  },\n  set(target, prop, value) {\n    if (prop === 'balance' && value < 0) {\n      throw new Error('Balance cannot be negative');\n    }\n    target[prop] = value;\n    return true;\n  },\n});\n\naccountProxy.balance = 500;\nconsole.log(accountProxy.balance);",
    "interviewQuestion": "How does JavaScript's native Proxy object relate to the classic Proxy design pattern, and what are some practical uses for it?"
  },
  {
    "id": "designpatterns-composite-pattern",
    "category": "designpatterns",
    "difficulty": "Advanced",
    "topic": "Structural Patterns",
    "title": "What is the Composite Pattern?",
    "summary": "Composite lets you treat individual objects and compositions of objects uniformly through a shared tree-like interface.",
    "explanation": "The Composite pattern organizes objects into tree structures representing part-whole hierarchies, where both leaf nodes and composite (container) nodes implement the same interface. This lets client code call operations like render() or getTotal() on either a single item or an entire subtree without needing to know which one it's dealing with. It's foundational to DOM trees, file system representations, and UI component trees in frameworks like React. The main design challenge is deciding how much of the interface (e.g., add/remove children) leaf nodes should implement versus throwing errors.",
    "code": "class File {\n  constructor(name, size) { this.name = name; this.size = size; }\n  getSize() { return this.size; }\n}\n\nclass Folder {\n  constructor(name) {\n    this.name = name;\n    this.children = [];\n  }\n  add(child) {\n    this.children.push(child);\n    return this;\n  }\n  getSize() {\n    return this.children.reduce((sum, c) => sum + c.getSize(), 0);\n  }\n}\n\nconst root = new Folder('root');\nroot.add(new File('a.txt', 10)).add(new File('b.txt', 20));\nconsole.log(root.getSize()); // 30",
    "interviewQuestion": "How would you design a Composite structure for a nested comments system, and how do leaf nodes differ from composite nodes in your interface?"
  },
  {
    "id": "designpatterns-bridge-pattern",
    "category": "designpatterns",
    "difficulty": "Advanced",
    "topic": "Structural Patterns",
    "title": "What is the Bridge Pattern?",
    "summary": "Bridge decouples an abstraction from its implementation so the two can vary and evolve independently.",
    "explanation": "The Bridge pattern splits a class hierarchy into two separate hierarchies: an abstraction (the high-level control logic) and an implementation (the low-level platform-specific details), connected via composition rather than inheritance. This avoids the combinatorial explosion of subclasses that occurs when you try to support multiple dimensions of variation (e.g., shape type x rendering engine) through inheritance alone. Changes to the implementation side don't ripple into the abstraction side and vice versa. It's frequently used for cross-platform UI toolkits and device drivers where the same abstraction must work across multiple backends.",
    "code": "class Renderer {\n  renderCircle(radius) { throw new Error('not implemented'); }\n}\n\nclass CanvasRenderer extends Renderer {\n  renderCircle(radius) { return 'Canvas circle r=' + radius; }\n}\n\nclass SvgRenderer extends Renderer {\n  renderCircle(radius) { return 'SVG circle r=' + radius; }\n}\n\nclass Circle {\n  constructor(renderer, radius) {\n    this.renderer = renderer;\n    this.radius = radius;\n  }\n  draw() {\n    return this.renderer.renderCircle(this.radius);\n  }\n}\n\nconsole.log(new Circle(new SvgRenderer(), 5).draw());",
    "interviewQuestion": "How does the Bridge pattern prevent a combinatorial explosion of subclasses, and how is it different from the Adapter pattern?"
  },
  {
    "id": "designpatterns-flyweight-pattern",
    "category": "designpatterns",
    "difficulty": "Advanced",
    "topic": "Structural Patterns",
    "title": "What is the Flyweight Pattern?",
    "summary": "Flyweight minimizes memory usage by sharing as much data as possible between similar objects instead of storing it redundantly per instance.",
    "explanation": "The Flyweight pattern separates object state into intrinsic state (shared, immutable data stored once in a shared object) and extrinsic state (context-specific data passed in by the client at call time). A factory typically manages a pool of flyweight objects, returning an existing instance if one with the same intrinsic state already exists rather than creating a new one. This is valuable in scenarios with huge numbers of similar objects, such as rendering thousands of trees in a game, glyphs in a text editor, or particles in a simulation. The trade-off is added complexity in managing shared vs. per-instance state and potential CPU overhead from the factory lookup.",
    "code": "class TreeType {\n  constructor(name, color, texture) {\n    this.name = name;\n    this.color = color;\n    this.texture = texture;\n  }\n}\n\nconst treeTypes = new Map();\nfunction getTreeType(name, color, texture) {\n  const key = name + color + texture;\n  if (!treeTypes.has(key)) {\n    treeTypes.set(key, new TreeType(name, color, texture));\n  }\n  return treeTypes.get(key);\n}\n\nfunction plantTree(x, y, name, color, texture) {\n  const type = getTreeType(name, color, texture);\n  return { x, y, type };\n}",
    "interviewQuestion": "What is the difference between intrinsic and extrinsic state in the Flyweight pattern, and can you give an example where it meaningfully reduces memory?"
  },
  {
    "id": "designpatterns-observer-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Behavioral Patterns",
    "title": "What is the Observer Pattern?",
    "summary": "Observer defines a one-to-many dependency so that when one object (the subject) changes state, all its dependents (observers) are notified automatically.",
    "explanation": "The Observer pattern decouples the subject from its observers, letting subscribers register and unregister interest in events without the subject needing to know their concrete types. It underlies event-driven programming throughout JavaScript, including DOM events, Node's EventEmitter, and reactive libraries like RxJS. The subject maintains a list of observers and calls a common notify method (e.g., update()) on each when its state changes. This promotes loose coupling but can lead to memory leaks if observers aren't properly unsubscribed, and 'notification storms' if overused.",
    "code": "class Subject {\n  constructor() {\n    this.observers = [];\n  }\n  subscribe(fn) {\n    this.observers.push(fn);\n  }\n  unsubscribe(fn) {\n    this.observers = this.observers.filter((o) => o !== fn);\n  }\n  notify(data) {\n    this.observers.forEach((fn) => fn(data));\n  }\n}\n\nconst stock = new Subject();\nconst logPrice = (price) => console.log('New price:', price);\nstock.subscribe(logPrice);\nstock.notify(105.5);",
    "interviewQuestion": "How does the Observer pattern relate to the pub/sub pattern, and what causes memory leaks in observer-based systems?"
  },
  {
    "id": "designpatterns-strategy-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Behavioral Patterns",
    "title": "What is the Strategy Pattern?",
    "summary": "Strategy defines a family of interchangeable algorithms, encapsulates each one, and lets the client select which one to use at runtime.",
    "explanation": "Instead of hardcoding conditional logic to select behavior, the Strategy pattern extracts each algorithm variant into its own object or function implementing a common interface, and the context object delegates to whichever strategy is currently assigned. This makes it easy to add new algorithms without modifying existing code (open/closed principle) and to swap behavior at runtime. In JavaScript, strategies are often just plain functions passed as arguments, since first-class functions make the pattern lightweight. Common examples include sorting comparators, payment method selection, and form validation rules.",
    "code": "const strategies = {\n  paypal: (amount) => 'Paid ' + amount + ' via PayPal',\n  creditCard: (amount) => 'Paid ' + amount + ' via Credit Card',\n};\n\nclass Checkout {\n  constructor(strategy) {\n    this.strategy = strategy;\n  }\n  pay(amount) {\n    return strategies[this.strategy](amount);\n  }\n}\n\nconst checkout = new Checkout('paypal');\nconsole.log(checkout.pay(50));",
    "interviewQuestion": "How does the Strategy pattern help satisfy the open/closed principle, and how would you implement it using plain functions instead of classes?"
  },
  {
    "id": "designpatterns-command-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Behavioral Patterns",
    "title": "What is the Command Pattern?",
    "summary": "Command encapsulates a request as an object, letting you parameterize clients, queue requests, and support undo/redo.",
    "explanation": "The Command pattern turns an action (a method call with its arguments) into a standalone object with a common execute() method, decoupling the object that invokes the action from the object that knows how to perform it. This enables features like undo/redo stacks (by storing an inverse undo() method), request queuing, logging, and macro commands that batch multiple commands together. It's widely used in text editor undo systems, GUI button/menu actions, and job queues. The trade-off is an increase in the number of small classes or objects needed to represent each action.",
    "code": "class AddTextCommand {\n  constructor(doc, text) {\n    this.doc = doc;\n    this.text = text;\n  }\n  execute() {\n    this.doc.content += this.text;\n  }\n  undo() {\n    this.doc.content = this.doc.content.slice(0, -this.text.length);\n  }\n}\n\nconst doc = { content: '' };\nconst history = [];\n\nfunction run(cmd) {\n  cmd.execute();\n  history.push(cmd);\n}\n\nrun(new AddTextCommand(doc, 'Hello'));\nhistory.pop().undo();\nconsole.log(doc.content); // ''",
    "interviewQuestion": "How would you implement undo/redo functionality using the Command pattern, and what does each command object need to store?"
  },
  {
    "id": "designpatterns-chain-of-responsibility-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Behavioral Patterns",
    "title": "What is the Chain of Responsibility Pattern?",
    "summary": "Chain of Responsibility passes a request along a chain of handlers until one of them handles it, decoupling sender from receiver.",
    "explanation": "Each handler in the chain decides either to process the request or pass it to the next handler, forming a pipeline where the sender doesn't need to know which handler will ultimately deal with the request. This is the conceptual basis for middleware systems in Express.js, Koa, and Redux, where each middleware can inspect, modify, short-circuit, or forward a request/action. It promotes loose coupling and makes handler order configurable and extensible, but debugging can be harder since the flow is implicit rather than a single visible call. If no handler processes the request, it may reach the end of the chain unhandled, so a default handler is often added.",
    "code": "class Handler {\n  setNext(handler) {\n    this.next = handler;\n    return handler;\n  }\n  handle(request) {\n    if (this.next) return this.next.handle(request);\n    return null;\n  }\n}\n\nclass AuthHandler extends Handler {\n  handle(request) {\n    if (!request.token) return 'Rejected: no token';\n    return super.handle(request);\n  }\n}\n\nclass LoggingHandler extends Handler {\n  handle(request) {\n    console.log('Logging request', request);\n    return super.handle(request);\n  }\n}\n\nconst chain = new AuthHandler();\nchain.setNext(new LoggingHandler());\nconsole.log(chain.handle({ token: 'abc' }));",
    "interviewQuestion": "How does Express.js middleware relate to the Chain of Responsibility pattern, and what happens if no handler in the chain handles the request?"
  },
  {
    "id": "designpatterns-iterator-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Behavioral Patterns",
    "title": "What is the Iterator Pattern?",
    "summary": "Iterator provides a way to sequentially access elements of a collection without exposing its underlying representation.",
    "explanation": "The Iterator pattern defines a standard interface (commonly next()) for traversing a collection, allowing client code to loop over elements without knowing whether the underlying structure is an array, linked list, tree, or something else. JavaScript formalizes this with the iterable/iterator protocols: any object implementing a Symbol.iterator method that returns an object with a next() method (returning { value, done }) can be used with for...of loops, spread syntax, and destructuring. This decouples traversal logic from the collection's internal storage and allows custom, even infinite, sequences to be consumed lazily.",
    "code": "class Range {\n  constructor(start, end) {\n    this.start = start;\n    this.end = end;\n  }\n  [Symbol.iterator]() {\n    let current = this.start;\n    const end = this.end;\n    return {\n      next() {\n        return current <= end\n          ? { value: current++, done: false }\n          : { value: undefined, done: true };\n      },\n    };\n  }\n}\n\nfor (const n of new Range(1, 5)) {\n  console.log(n);\n}",
    "interviewQuestion": "How does JavaScript's Symbol.iterator protocol implement the Iterator pattern, and how does it enable for...of loops on custom objects?"
  },
  {
    "id": "designpatterns-mediator-pattern",
    "category": "designpatterns",
    "difficulty": "Advanced",
    "topic": "Behavioral Patterns",
    "title": "What is the Mediator Pattern?",
    "summary": "Mediator defines an object that centralizes communication between a set of objects, so they don't reference each other directly.",
    "explanation": "Instead of components communicating in a many-to-many web of direct references (which becomes hard to maintain as the number of components grows), each component talks only to a central mediator, which coordinates the interaction. This reduces coupling between individual components since they only need to know about the mediator's interface, not each other. Chat rooms, air traffic control systems, and UI form components (where enabling one field depends on others) are classic examples. State management libraries like Redux act as a form of mediator between UI components. Overusing Mediator can turn it into a 'god object' that becomes a bottleneck and hard to maintain itself.",
    "code": "class ChatRoom {\n  showMessage(user, message) {\n    console.log(user + ': ' + message);\n  }\n}\n\nclass User {\n  constructor(name, chatRoom) {\n    this.name = name;\n    this.chatRoom = chatRoom;\n  }\n  send(message) {\n    this.chatRoom.showMessage(this.name, message);\n  }\n}\n\nconst room = new ChatRoom();\nconst alice = new User('Alice', room);\nconst bob = new User('Bob', room);\nalice.send('Hi Bob!');\nbob.send('Hey Alice!');",
    "interviewQuestion": "How does the Mediator pattern reduce coupling between components, and what is the risk of the mediator becoming a 'god object'?"
  },
  {
    "id": "designpatterns-memento-pattern",
    "category": "designpatterns",
    "difficulty": "Advanced",
    "topic": "Behavioral Patterns",
    "title": "What is the Memento Pattern?",
    "summary": "Memento captures and externalizes an object's internal state so it can be restored later, without violating encapsulation.",
    "explanation": "The Memento pattern involves three roles: the Originator (the object whose state needs saving), the Memento (a snapshot of that state), and the Caretaker (which stores mementos but never inspects or modifies their contents). This separation allows undo/redo history to be maintained externally while keeping the originator's internal representation encapsulated and hidden from the caretaker. It's frequently paired with the Command pattern to implement undo stacks in editors, and is conceptually similar to how Redux DevTools snapshots application state for time-travel debugging. A key trade-off is memory usage if snapshots are large or frequent, which can be mitigated using diffs instead of full copies.",
    "code": "class Editor {\n  constructor() { this.content = ''; }\n  type(text) { this.content += text; }\n  save() { return { content: this.content }; }\n  restore(memento) { this.content = memento.content; }\n}\n\nconst editor = new Editor();\nconst history = [];\n\neditor.type('Hello');\nhistory.push(editor.save());\neditor.type(' World');\nconsole.log(editor.content); // 'Hello World'\n\neditor.restore(history.pop());\nconsole.log(editor.content); // 'Hello'",
    "interviewQuestion": "What are the three roles in the Memento pattern, and how does it preserve encapsulation compared to just exposing getters/setters for all state?"
  },
  {
    "id": "designpatterns-state-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Behavioral Patterns",
    "title": "What is the State Pattern?",
    "summary": "State lets an object alter its behavior when its internal state changes, appearing as though the object changed its class.",
    "explanation": "Rather than scattering large if/else or switch statements checking a status field throughout a class, the State pattern extracts each state's behavior into its own class implementing a common interface, and the context object delegates calls to its current state object. Transitions between states are handled either by the context or by the state objects themselves calling back to change the context's current state. This keeps state-specific logic localized and makes adding new states straightforward without touching unrelated code. It's commonly used for order/workflow status machines, traffic lights, and media player controls (playing, paused, stopped).",
    "code": "class TrafficLight {\n  constructor() { this.state = 'red'; }\n  next() {\n    const transitions = { red: 'green', green: 'yellow', yellow: 'red' };\n    this.state = transitions[this.state];\n    return this.state;\n  }\n}\n\nconst light = new TrafficLight();\nconsole.log(light.next()); // green\nconsole.log(light.next()); // yellow\nconsole.log(light.next()); // red",
    "interviewQuestion": "How does the State pattern differ from just using a switch statement on a status field, and when does the added structure pay off?"
  },
  {
    "id": "designpatterns-template-method-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Behavioral Patterns",
    "title": "What is the Template Method Pattern?",
    "summary": "Template Method defines the skeleton of an algorithm in a base class, deferring specific steps to subclasses without changing the algorithm's overall structure.",
    "explanation": "A base class implements a method that calls a fixed sequence of steps, some of which are concrete (shared behavior) and some of which are abstract or overridable 'hook' methods that subclasses must or may implement. This enforces a consistent process across subclasses (e.g., a data processing pipeline: read, process, save) while allowing each subclass to customize individual steps. It relies on inheritance, which is a key difference from Strategy, which achieves similar flexibility through composition. Overuse of Template Method can lead to fragile hierarchies if the base algorithm needs to change frequently.",
    "code": "class DataProcessor {\n  process() {\n    this.readData();\n    this.transformData();\n    this.saveData();\n  }\n  readData() { console.log('Reading raw data'); }\n  saveData() { console.log('Saving processed data'); }\n  transformData() { throw new Error('Must implement transformData'); }\n}\n\nclass CsvProcessor extends DataProcessor {\n  transformData() { console.log('Transforming CSV data'); }\n}\n\nnew CsvProcessor().process();",
    "interviewQuestion": "How does the Template Method pattern differ from the Strategy pattern, given both let you customize part of an algorithm?"
  },
  {
    "id": "designpatterns-visitor-pattern",
    "category": "designpatterns",
    "difficulty": "Tricky",
    "topic": "Behavioral Patterns",
    "title": "What is the Visitor Pattern?",
    "summary": "Visitor lets you add new operations to a group of related classes without modifying those classes, by moving the operation into a separate visitor object.",
    "explanation": "The Visitor pattern uses double dispatch: each element in a structure has an accept(visitor) method that calls the appropriate visit method on the visitor for its own concrete type, and the visitor implements a separate method for each element type. This lets you add new operations (visitors) over an existing class hierarchy without touching those classes, which is the opposite trade-off of Template Method or simple polymorphism, where adding a new type is easy but adding a new operation requires modifying every class. It's most valuable when the object structure is stable but the operations performed on it change frequently, such as AST traversal in compilers/linters (e.g., ESLint visitors) or exporting a document tree to multiple formats. The main downside is that adding a new element type requires updating every visitor.",
    "code": "class Circle {\n  accept(visitor) { return visitor.visitCircle(this); }\n}\nclass Square {\n  accept(visitor) { return visitor.visitSquare(this); }\n}\n\nconst areaVisitor = {\n  visitCircle: (c) => 'circle area',\n  visitSquare: (s) => 'square area',\n};\n\nconst shapes = [new Circle(), new Square()];\nshapes.forEach((s) => console.log(s.accept(areaVisitor)));",
    "interviewQuestion": "What is 'double dispatch' in the Visitor pattern, and why does adding a new element type require changing every existing visitor?"
  },
  {
    "id": "designpatterns-dependency-injection-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Architectural Patterns",
    "title": "What is the Dependency Injection Pattern?",
    "summary": "Dependency Injection provides an object's dependencies from the outside rather than having the object construct them itself, decoupling classes from concrete implementations.",
    "explanation": "Instead of a class instantiating the services it needs internally (tight coupling), those dependencies are 'injected' via constructor parameters, setter methods, or a DI container/framework. This makes it trivial to swap implementations, such as substituting a mock service in unit tests, without modifying the consuming class. DI is central to frameworks like Angular (constructor injection with @Injectable), NestJS, and Spring, and it directly supports the Dependency Inversion Principle (the 'D' in SOLID) by making high-level modules depend on abstractions rather than concrete low-level modules. The trade-off is added indirection and, with heavy DI containers, a steeper learning curve and less obvious control flow.",
    "code": "class EmailService {\n  send(msg) { console.log('Email:', msg); }\n}\n\nclass NotificationService {\n  constructor(sender) {\n    this.sender = sender; // injected dependency\n  }\n  notify(msg) {\n    this.sender.send(msg);\n  }\n}\n\n// Easy to swap in tests\nconst mockSender = { send: (msg) => console.log('Mock:', msg) };\nconst service = new NotificationService(mockSender);\nservice.notify('Hello');",
    "interviewQuestion": "How does Dependency Injection support the Dependency Inversion Principle, and how does it make unit testing easier?"
  },
  {
    "id": "designpatterns-repository-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Architectural Patterns",
    "title": "What is the Repository Pattern?",
    "summary": "Repository mediates between the domain/business logic and data mapping layers, providing a collection-like interface for accessing domain objects.",
    "explanation": "A Repository abstracts away the details of data persistence (SQL queries, ORM calls, HTTP requests to an API) behind methods like findById(), save(), and delete(), so business logic doesn't need to know whether data comes from a database, a REST API, or an in-memory cache. This makes it straightforward to swap data sources or mock persistence entirely in unit tests. It also centralizes query logic, avoiding duplication of data-access code scattered across the codebase. The pattern is common in backend services built with layered/clean architecture, sitting between the service layer and the ORM or database driver.",
    "code": "class UserRepository {\n  constructor(db) {\n    this.db = db;\n  }\n  async findById(id) {\n    return this.db.query('SELECT * FROM users WHERE id = ?', [id]);\n  }\n  async save(user) {\n    return this.db.query('INSERT INTO users SET ?', [user]);\n  }\n}\n\n// Business logic doesn't know about SQL at all\nclass UserService {\n  constructor(repo) { this.repo = repo; }\n  async getUser(id) { return this.repo.findById(id); }\n}",
    "interviewQuestion": "How does the Repository pattern help decouple business logic from your data access layer, and how would you unit test a service that depends on one?"
  },
  {
    "id": "designpatterns-null-object-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Behavioral Patterns",
    "title": "What is the Null Object Pattern?",
    "summary": "Null Object provides a default, do-nothing implementation of an interface to avoid null checks scattered throughout the codebase.",
    "explanation": "Instead of returning null or undefined when no meaningful object is available and forcing every caller to add null checks, you return an object that implements the same interface but with neutral, no-op behavior. This eliminates repetitive defensive code (if (obj) { obj.doSomething() }) and reduces the risk of null-pointer-style runtime errors. It's especially useful for things like a 'guest' user object, an empty logger, or a no-op event handler, where the neutral behavior is well-defined and safe. The trade-off is that null object bugs (forgetting a case was ever null) can be silently swallowed instead of failing loudly, so it should be used deliberately, not as a blanket substitute for proper error handling.",
    "code": "class RealUser {\n  getName() { return 'Alice'; }\n  isLoggedIn() { return true; }\n}\n\nclass NullUser {\n  getName() { return 'Guest'; }\n  isLoggedIn() { return false; }\n}\n\nfunction getCurrentUser(session) {\n  return session.user || new NullUser();\n}\n\nconst user = getCurrentUser({});\nconsole.log(user.getName()); // 'Guest' -- no null check needed",
    "interviewQuestion": "What problem does the Null Object pattern solve compared to returning null, and what risk does it introduce if used carelessly?"
  },
  {
    "id": "designpatterns-mvc-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Architectural Patterns",
    "title": "What is the MVC (Model-View-Controller) Pattern?",
    "summary": "MVC separates an application into Model (data/business logic), View (UI rendering), and Controller (handles input and coordinates Model and View).",
    "explanation": "The Model holds application state and business rules and is unaware of the UI. The View renders the Model's data and is typically 'dumb', containing minimal logic. The Controller receives user input, updates the Model accordingly, and selects which View to render, acting as the coordinator between the two. This separation of concerns allows the same Model to be rendered by multiple Views and makes each layer independently testable. Classic examples include Ruby on Rails and Django on the backend; on the frontend, MVC has largely evolved into variants like MVVM or unidirectional-data-flow architectures (Flux/Redux) because two-way bindings between View and Controller became hard to manage at scale.",
    "code": "// Model\nconst model = { count: 0 };\n\n// View\nfunction render(state) {\n  console.log('Count is:', state.count);\n}\n\n// Controller\nfunction increment() {\n  model.count += 1;\n  render(model);\n}\n\nincrement(); // Count is: 1\nincrement(); // Count is: 2",
    "interviewQuestion": "In MVC, why should the Model have no knowledge of the View, and what problems arise when that boundary is violated?"
  },
  {
    "id": "designpatterns-mvvm-pattern",
    "category": "designpatterns",
    "difficulty": "Intermediate",
    "topic": "Architectural Patterns",
    "title": "What is the MVVM (Model-View-ViewModel) Pattern?",
    "summary": "MVVM introduces a ViewModel that exposes Model data and commands to the View via data binding, removing manual DOM/UI update code.",
    "explanation": "Unlike MVC's Controller, the ViewModel in MVVM doesn't reference the View directly; instead, the View binds to observable properties and commands exposed by the ViewModel, and a binding framework automatically synchronizes UI changes with ViewModel state and vice versa. This eliminates most manual DOM manipulation code and makes the ViewModel fully unit-testable in isolation from any rendering framework. Frameworks like Vue (with reactive refs/computed properties) and Angular (with two-way [(ngModel)] binding) are built around this pattern, while React's hooks-based state can be seen as a lightweight, unidirectional variant. The main trade-off is that heavy two-way binding can make data flow harder to trace compared to explicit unidirectional architectures.",
    "code": "// ViewModel using a simple reactive pattern\nfunction createViewModel() {\n  let _name = '';\n  const listeners = [];\n  return {\n    get name() { return _name; },\n    set name(value) {\n      _name = value;\n      listeners.forEach((fn) => fn(_name));\n    },\n    onChange(fn) { listeners.push(fn); },\n  };\n}\n\nconst vm = createViewModel();\nvm.onChange((name) => console.log('View updates to show:', name));\nvm.name = 'Alice';",
    "interviewQuestion": "How does MVVM's data binding differ from MVC's controller-driven updates, and how does that affect testability of the ViewModel?"
  },
  {
    "id": "designpatterns-module-pattern",
    "category": "designpatterns",
    "difficulty": "Basic",
    "topic": "Structural Patterns",
    "title": "What is the Module Pattern?",
    "summary": "Module pattern uses closures (or ES modules) to encapsulate private state and expose only a controlled public API.",
    "explanation": "Before ES modules existed, JavaScript developers used IIFEs (Immediately Invoked Function Expressions) to create a private scope, returning an object literal exposing only the methods/properties meant to be public while keeping other variables inaccessible from the outside. This achieves encapsulation similar to private class fields, preventing external code from directly mutating internal state and reducing global namespace pollution. Native ES modules (import/export) and closures with factory functions have largely replaced the IIFE variant, but the underlying goal—separating public interface from private implementation—is the same. The Revealing Module Pattern is a popular variant that defines everything privately first, then returns an object mapping public names to those private implementations.",
    "code": "const CounterModule = (function () {\n  let count = 0; // private\n\n  function increment() {\n    count += 1;\n    return count;\n  }\n\n  function getCount() {\n    return count;\n  }\n\n  return { increment, getCount }; // public API\n})();\n\nCounterModule.increment();\nconsole.log(CounterModule.getCount()); // 1\nconsole.log(CounterModule.count); // undefined -- private",
    "interviewQuestion": "How does the Module pattern achieve encapsulation in JavaScript using closures, and how have ES modules changed the need for the IIFE version?"
  },
  {
    "id": "designpatterns-solid-principles-overview",
    "category": "designpatterns",
    "difficulty": "Advanced",
    "topic": "Design Principles",
    "title": "What are the SOLID Principles?",
    "summary": "SOLID is an acronym for five object-oriented design principles that make software more maintainable, extensible, and testable.",
    "explanation": "Single Responsibility Principle: a class/module should have only one reason to change. Open/Closed Principle: entities should be open for extension but closed for modification, typically achieved via polymorphism or composition instead of editing existing code. Liskov Substitution Principle: subtypes must be substitutable for their base types without breaking correctness. Interface Segregation Principle: clients shouldn't be forced to depend on interfaces/methods they don't use, favor several small interfaces over one large one. Dependency Inversion Principle: high-level modules should depend on abstractions, not concrete low-level implementations, which is what Dependency Injection typically enables. Together these principles guide decisions like when to introduce a design pattern, and many patterns (Strategy, Factory, DI) exist specifically to help satisfy one or more SOLID principles.",
    "code": "// Open/Closed + Dependency Inversion example\nclass PaymentProcessor {\n  constructor(gateway) {\n    this.gateway = gateway; // depends on abstraction, not concrete gateway\n  }\n  process(amount) {\n    return this.gateway.charge(amount);\n  }\n}\n\nclass StripeGateway {\n  charge(amount) { return 'Charged ' + amount + ' via Stripe'; }\n}\n\nclass PaypalGateway {\n  charge(amount) { return 'Charged ' + amount + ' via PayPal'; }\n}\n\n// Adding a new gateway requires no changes to PaymentProcessor\nnew PaymentProcessor(new StripeGateway()).process(100);",
    "interviewQuestion": "Can you explain each SOLID principle with a short example, and describe a time you refactored code to fix a SOLID violation?"
  },
  {
    "id": "designpatterns-dry-kiss-yagni-principles",
    "category": "designpatterns",
    "difficulty": "Tricky",
    "topic": "Design Principles",
    "title": "What are DRY, KISS, and YAGNI?",
    "summary": "DRY (Don't Repeat Yourself), KISS (Keep It Simple, Stupid), and YAGNI (You Aren't Gonna Need It) are pragmatic principles that guard against over-engineering and duplication.",
    "explanation": "DRY says every piece of knowledge should have a single, unambiguous representation in a system; duplicated logic means duplicated bugs and maintenance cost, though DRY can be taken too far by abstracting prematurely across code that merely looks similar but represents different concerns ('wrong abstraction'). KISS argues that simple, straightforward solutions are easier to understand, debug, and maintain than clever or complex ones, favoring readability over premature optimization. YAGNI warns against building functionality or flexibility (like a generic plugin system or configurable abstraction) before there is a proven, current need for it, since speculative generality adds complexity and is often wrong about future requirements. In interviews, these principles are often invoked as a counterbalance to over-applying design patterns: patterns should solve a real, current problem, not be added 'just in case'.",
    "code": "// Violates DRY, KISS, and YAGNI\nfunction calculateDiscountVerbose(price, type) {\n  if (type === 'vip') { return price * 0.8; }\n  if (type === 'regular') { return price * 0.95; }\n  // ... speculative 'future' discount types never used\n  return price;\n}\n\n// DRY + KISS: single table-driven lookup, no speculative branches\nconst DISCOUNTS = { vip: 0.8, regular: 0.95 };\nfunction calculateDiscount(price, type) {\n  return price * (DISCOUNTS[type] ?? 1);\n}",
    "interviewQuestion": "Can you describe a time DRY led to the wrong abstraction, and how YAGNI would have prevented over-engineering in that case?"
  },
];
