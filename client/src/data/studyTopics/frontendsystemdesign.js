// 30 Frontend System Design topics for Study Hub.
export default [
  {
    id: "frontendsystemdesign-news-feed",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "News Feed Design",
    title: "How would you design a scalable news feed UI like Facebook or Twitter?",
    summary: "A news feed combines paginated data fetching, virtualization, optimistic updates, and cache normalization to render an endless stream efficiently.",
    explanation: "The core challenges are fetching data efficiently (cursor-based pagination instead of offset), rendering only visible items (windowing/virtualization) to keep the DOM light, and normalizing post data in a client-side store so likes/comments update in one place regardless of where a post is rendered. Real-time updates (new posts, live like counts) arrive via websockets or polling and get merged into the same normalized cache. Optimistic UI updates make interactions like liking feel instant while the request resolves in the background, with rollback on failure. Media (images/video) should be lazy-loaded and use placeholders to avoid layout shift.",
    code: "// Normalized feed store shape\nstate = {\n  posts: { byId: { p1: {...}, p2: {...} }, allIds: ['p1','p2'] },\n  feed: { cursor: 'abc123', items: ['p1','p2'], loading: false }\n}\n\n// Optimistic like\nfunction likePost(id) {\n  dispatch(likeStart(id)); // bump count in UI immediately\n  api.like(id).catch(() => dispatch(likeRollback(id)));\n}",
    interviewQuestion: "Walk me through how you would design the data layer and rendering strategy for an infinitely scrolling news feed.",
  },
  {
    id: "frontendsystemdesign-autocomplete",
    category: "frontendsystemdesign",
    difficulty: "Basic",
    topic: "Autocomplete / Typeahead",
    title: "How do you design an autocomplete/typeahead component?",
    summary: "An autocomplete component debounces input, cancels stale requests, caches results, and manages keyboard-accessible suggestion lists.",
    explanation: "Key requirements are minimizing network chatter (debounce keystrokes ~200-300ms), avoiding race conditions where an older slower response overwrites a newer one (use request cancellation via AbortController or a request-id guard), and caching recent queries client-side (LRU map) to make repeated lookups instant. The suggestion dropdown must be a proper ARIA combobox/listbox for accessibility, supporting arrow-key navigation, Enter to select, and Escape to close. For very large datasets, results are typically pre-filtered server-side or via a trie/prefix index, and rendering is virtualized if the list is long.",
    code: "let controller;\nasync function onInput(query) {\n  controller?.abort();\n  controller = new AbortController();\n  if (cache.has(query)) return render(cache.get(query));\n  const res = await fetch(`/search?q=${query}`, { signal: controller.signal });\n  const data = await res.json();\n  cache.set(query, data);\n  render(data);\n}\nconst debouncedOnInput = debounce(onInput, 250);",
    interviewQuestion: "How would you prevent an autocomplete dropdown from flickering when responses return out of order?",
  },
  {
    id: "frontendsystemdesign-chat-ui",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Chat Application UI",
    title: "How would you design the frontend architecture for a chat application like Slack or WhatsApp Web?",
    summary: "A chat UI needs a real-time transport layer, message ordering/dedup logic, optimistic sends, and virtualized reverse-infinite scrolling per conversation.",
    explanation: "Messages arrive over websockets and must be merged with locally-sent optimistic messages using client-generated temporary IDs that get reconciled with server IDs on ack. Each conversation's message list is windowed/virtualized since history can be huge, and scroll position must be preserved when older messages are prepended (reverse infinite scroll). Delivery/read receipts, typing indicators, and presence are separate lightweight event streams multiplexed over the same socket. Offline support requires queuing unsent messages in IndexedDB and replaying them on reconnect, plus handling out-of-order delivery and message dedup by ID.",
    code: "// Optimistic send with temp id reconciliation\nfunction sendMessage(text) {\n  const tempId = uuid();\n  addMessage({ id: tempId, text, status: 'sending' });\n  socket.emit('message', { tempId, text }, (ack) => {\n    reconcileMessage(tempId, ack.serverId, 'sent');\n  });\n}\n\nsocket.on('message', (msg) => {\n  if (!hasMessage(msg.id)) appendMessage(msg);\n});",
    interviewQuestion: "How do you reconcile an optimistically-sent chat message with the version that comes back from the server over a websocket?",
  },
  {
    id: "frontendsystemdesign-infinite-scroll",
    category: "frontendsystemdesign",
    difficulty: "Basic",
    topic: "Infinite Scroll",
    title: "How do you design an infinite scroll feed that stays performant with thousands of items?",
    summary: "Infinite scroll pairs an IntersectionObserver-based fetch trigger with list virtualization so the DOM node count stays bounded regardless of items loaded.",
    explanation: "Naively appending DOM nodes forever degrades scroll performance and memory, so production implementations render only the visible window plus a buffer (virtualization/windowing), recycling off-screen nodes. A sentinel element at the bottom, observed via IntersectionObserver, triggers the next page fetch instead of listening to scroll events directly, which avoids expensive scroll-handler thrashing. Edge cases include preserving scroll position on prepend, handling fast scrolling that outruns fetches (loading skeletons), and providing a way back to top. Accessibility requires announcing newly loaded content to screen readers via a live region.",
    code: "const sentinel = document.querySelector('#sentinel');\nconst observer = new IntersectionObserver((entries) => {\n  if (entries[0].isIntersecting && !loading) {\n    loadNextPage();\n  }\n}, { rootMargin: '400px' });\nobserver.observe(sentinel);\n\n// Combine with react-window / react-virtualized for DOM windowing",
    interviewQuestion: "Why would you use IntersectionObserver instead of a scroll event listener to implement infinite scroll, and what else does the design need to stay performant?",
  },
  {
    id: "frontendsystemdesign-file-upload",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "File Upload System",
    title: "How would you design a robust file upload system supporting large files and flaky networks?",
    summary: "Large, resumable uploads use chunked/multipart transfer with per-chunk retry, progress tracking, and resumability via an upload session id.",
    explanation: "For large files, split into chunks (e.g. 5-10MB) and upload each independently so a failure only requires retrying one chunk, not the whole file; this also enables pausing/resuming (resumable uploads, similar to the tus protocol). The client tracks progress per chunk to compute an aggregate percentage and exposes cancel/retry controls. Concurrency should be capped (e.g. 3-4 parallel chunk uploads) to avoid saturating bandwidth. Client-side validation (file type/size), image/video compression before upload, and drag-and-drop with a queue UI for multiple files round out the UX; server returns a final asset URL after all chunks are committed.",
    code: "async function uploadFile(file) {\n  const chunkSize = 5 * 1024 * 1024;\n  const chunks = Math.ceil(file.size / chunkSize);\n  const uploadId = await initUpload(file.name, file.size);\n  for (let i = 0; i < chunks; i++) {\n    const blob = file.slice(i * chunkSize, (i + 1) * chunkSize);\n    await uploadChunkWithRetry(uploadId, i, blob, 3);\n    updateProgress((i + 1) / chunks);\n  }\n  return completeUpload(uploadId);\n}",
    interviewQuestion: "How would you design an upload flow that lets a user pause a 2GB file upload and resume it later, even after closing the tab?",
  },
  {
    id: "frontendsystemdesign-notification-system",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Notification System",
    title: "How do you design the frontend for an in-app notification system?",
    summary: "A notification system needs a real-time delivery channel, a client-side unread/read state store, and UI surfaces (bell dropdown, toasts, push) that all read from the same source of truth.",
    explanation: "Notifications arrive via websocket/SSE for real-time delivery and are also fetchable via a paginated REST endpoint for history, deduplicated by ID in a normalized store. The UI has multiple simultaneous surfaces: a badge count, a dropdown list, and transient toasts, all derived from the same state to avoid drift. Read/unread state should update optimistically and sync in batches to reduce write load. For scale, grouping/aggregation (\"5 people liked your post\") happens server-side but the client must render collapsed groups and expand on demand. Browser push notifications require a service worker and permission flow layered on top of in-app notifications.",
    code: "socket.on('notification', (n) => {\n  store.notifications.unshift(n);\n  store.unreadCount++;\n  if (isTabVisible()) showToast(n); else incrementFavicon();\n});\n\nfunction markAsRead(ids) {\n  ids.forEach(id => store.byId[id].read = true);\n  store.unreadCount -= ids.length;\n  debouncedSync(ids); // batch PATCH to server\n}",
    interviewQuestion: "How would you keep the unread badge count, the notification dropdown, and toast popups all consistent when notifications arrive in real time?",
  },
  {
    id: "frontendsystemdesign-collab-editor",
    category: "frontendsystemdesign",
    difficulty: "Tricky",
    topic: "Real-time Collaborative Editor",
    title: "How would you design a real-time collaborative text editor like Google Docs?",
    summary: "Collaborative editing relies on conflict-resolution algorithms (OT or CRDTs) to merge concurrent edits deterministically across clients, layered under a rich text rendering engine.",
    explanation: "The hard problem is merging concurrent edits from multiple users without conflicts or lost data; the two dominant approaches are Operational Transformation (transform operations against each other based on order) and CRDTs (data structures that merge commutatively regardless of order, e.g. Yjs, Automerge). Each client applies local edits immediately (local-first) and sends operations to a server/relay that broadcasts them to other clients, who transform/merge and re-render. Cursor and selection presence for other users is a separate lightweight ephemeral state broadcast alongside document ops. Offline editing requires local persistence (IndexedDB) of pending ops that sync and merge on reconnect, and undo/redo must be conflict-aware, not a naive stack.",
    code: "// CRDT-based approach (Yjs-style)\nconst ydoc = new Y.Doc();\nconst ytext = ydoc.getText('content');\nconst provider = new WebsocketProvider(wsUrl, roomId, ydoc);\n\nytext.observe(() => renderEditor(ytext.toString()));\neditor.on('localChange', (delta) => {\n  ytext.applyDelta(delta); // merges automatically via CRDT\n});",
    interviewQuestion: "Would you use Operational Transformation or CRDTs for a collaborative editor, and what tradeoffs would drive that choice?",
  },
  {
    id: "frontendsystemdesign-dashboard-widgets",
    category: "frontendsystemdesign",
    difficulty: "Intermediate",
    topic: "Dashboard with Widgets",
    title: "How do you design a configurable dashboard made of independent widgets?",
    summary: "A widget dashboard treats each widget as an isolated, independently-fetching component within a drag-and-drop grid layout, with layout persisted separately from widget data.",
    explanation: "Each widget owns its own data fetching, loading, and error states so one slow/broken widget does not block the rest of the dashboard from rendering. The grid layout (position, size) is stored separately from widget content, typically using a library like react-grid-layout, and persisted per-user so arrangements are restorable. Widgets should be lazy-loaded (code-split) since a dashboard may support dozens of widget types but a given user only uses a handful. A shared design-system-level widget shell (title bar, loading skeleton, error boundary, refresh/remove controls) keeps behavior consistent across widget types.",
    code: "function WidgetShell({ widget }) {\n  return (\n    <ErrorBoundary fallback={<WidgetError />}>\n      <Suspense fallback={<WidgetSkeleton />}>\n        <LazyWidget type={widget.type} config={widget.config} />\n      </Suspense>\n    </ErrorBoundary>\n  );\n}\n\n// layout persisted independently: [{ i: 'w1', x: 0, y: 0, w: 4, h: 2 }, ...]",
    interviewQuestion: "How would you make sure one failing widget on a dashboard does not break the entire page?",
  },
  {
    id: "frontendsystemdesign-component-library",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Design System / Component Library",
    title: "How would you architect a design system and component library used across multiple product teams?",
    summary: "A design system is a versioned package of tokens, primitives, and composite components built and tested in isolation, distributed via a private registry with strict semver and visual regression gates.",
    explanation: "The foundation is a design token layer (colors, spacing, typography) as framework-agnostic values (JSON/CSS variables) that both design tools and code consume, ensuring parity. Components are built in isolation (Storybook), documented with usage guidelines, and tested with visual regression (Chromatic/Percy) plus accessibility linting in CI. Distribution as a versioned npm package with strict semver lets teams opt into breaking changes deliberately; a codemod strategy helps migrate consumers across major versions. Governance (a core team owning the library, an RFC process for new components, contribution guidelines from product teams) is as important as the technical architecture for keeping it consistent at scale.",
    code: "// design-tokens.json -> generates CSS vars + platform outputs\n{ \"color.primary.500\": \"#3B82F6\", \"space.4\": \"16px\" }\n\n// Button.tsx - composed from primitives, tokens only\nexport function Button({ variant = 'primary', ...props }) {\n  return <button className={cx(styles.base, styles[variant])} {...props} />;\n}\n// Published as @acme/design-system@4.2.0, tested via Storybook + Chromatic",
    interviewQuestion: "How would you version and distribute a shared component library so dozens of product teams can consume it without breaking on every release?",
  },
  {
    id: "frontendsystemdesign-state-management",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "State Management Architecture",
    title: "How do you decide on a state management architecture for a large frontend application?",
    summary: "State should be classified by scope and lifetime (local UI state, shared client state, server/cache state, URL state) and each category gets the tool best suited to it rather than one global store for everything.",
    explanation: "A common mistake is putting server data into a general-purpose global store; server state has different needs (caching, revalidation, staleness) better served by tools like React Query/SWR/Apollo. Local, ephemeral UI state (a dropdown being open) should stay in component state, not global stores, to avoid unnecessary re-renders and complexity. Cross-cutting client state (auth, theme, cart) fits a lightweight global store (Zustand, Redux, Context). URL/search-params should be treated as state for anything that needs to be shareable or bookmarkable (filters, pagination). The architectural decision is really several smaller decisions per state category, plus a convention so the team is consistent.",
    code: "// Categorize before choosing a tool\n// 1. Server state -> React Query\nconst { data } = useQuery(['user', id], () => fetchUser(id));\n// 2. Global client state -> Zustand\nconst useCart = create((set) => ({ items: [], add: (i) => set((s) => ({ items: [...s.items, i] })) }));\n// 3. URL state -> router search params\nconst [filters, setFilters] = useSearchParams();\n// 4. Local UI state -> useState, stays in the component",
    interviewQuestion: "A junior engineer wants to put all API response data into Redux. How would you explain when that is and is not the right call?",
  },
  {
    id: "frontendsystemdesign-microfrontends-overview",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Micro-Frontends",
    title: "What are micro-frontends and when should you use them?",
    summary: "Micro-frontends split a large frontend into independently deployable applications owned by separate teams, composed together at build, server, or runtime.",
    explanation: "The motivation mirrors microservices: large orgs with multiple teams need to ship independently without coordinating releases through a single monolithic frontend codebase. Composition strategies include build-time (npm package composition), server-side (edge-side includes / server composition), and runtime (iframe, Web Components, or JS-level composition like Module Federation). Tradeoffs include duplicated dependencies (larger bundle size) unless shared carefully, harder cross-app state sharing and styling isolation, and increased operational complexity (versioning, routing, shared design system enforcement). Micro-frontends are usually justified by organizational scale (many independent teams) rather than pure technical need — a single team is almost always better off with a modular monolith.",
    code: "// Runtime composition via a shell app\n<script type=\"module\" src=\"https://cdn.acme.com/checkout-mfe/remote.js\"></script>\n<checkout-app config={sharedAuthToken}></checkout-app>\n\n// Shell routes to owning team's deployed app\n/checkout/*  -> checkout-mfe (team payments)\n/catalog/*   -> catalog-mfe (team discovery)",
    interviewQuestion: "When would you recommend micro-frontends over a modular monolith, and what costs come with that choice?",
  },
  {
    id: "frontendsystemdesign-module-federation",
    category: "frontendsystemdesign",
    difficulty: "Tricky",
    topic: "Module Federation",
    title: "How does Webpack Module Federation enable micro-frontends at runtime?",
    summary: "Module Federation lets independently built and deployed JS bundles dynamically import code from one another at runtime, sharing common dependencies to avoid duplication.",
    explanation: "A 'remote' app exposes specific modules (components, hooks) via a federation config, and a 'host' app consumes them by declaring the remote's URL, without either being built together or even known to each other at build time. Shared dependencies (React, React-DOM) are deduplicated using a singleton negotiation at runtime, so both apps use one copy if versions are compatible, falling back to separate copies otherwise. This enables true independent deployment: the remote team ships a new version and the host picks it up on next load without a host rebuild. Failure modes to design for include remote unavailability (fallback UI), version mismatches in shared deps, and increased runtime complexity for debugging across bundle boundaries.",
    code: "// host webpack.config.js\nnew ModuleFederationPlugin({\n  name: 'shell',\n  remotes: { checkout: 'checkout@https://cdn.acme.com/remoteEntry.js' },\n  shared: { react: { singleton: true }, 'react-dom': { singleton: true } },\n});\n\n// consuming the remote\nconst CheckoutApp = React.lazy(() => import('checkout/App'));",
    interviewQuestion: "How does Module Federation avoid shipping duplicate copies of React when a host and several remotes all depend on it?",
  },
  {
    id: "frontendsystemdesign-routing-architecture",
    category: "frontendsystemdesign",
    difficulty: "Basic",
    topic: "Client-Side Routing",
    title: "How do you architect client-side routing for a large single-page application?",
    summary: "Routing architecture combines a route table with code-split lazy-loaded route bundles, nested layouts, guarded routes for auth, and a strategy for data loading tied to navigation.",
    explanation: "Routes should be code-split so users only download the JS for the page they visit, using dynamic import() at the route boundary with a loading fallback. Nested routing (layouts that persist across child route changes, e.g. a dashboard shell with a changing content area) avoids re-mounting shared UI on every navigation. Route guards handle auth/permission checks before rendering protected routes, redirecting unauthenticated users. Modern routers (React Router data APIs, TanStack Router) tie data loading to route transitions (loaders) so data and code load in parallel rather than waterfalling after mount, improving perceived performance.",
    code: "const routes = [\n  {\n    path: '/dashboard',\n    element: <DashboardLayout />,\n    loader: requireAuth,\n    children: [\n      { path: 'overview', lazy: () => import('./Overview') },\n      { path: 'settings', lazy: () => import('./Settings') },\n    ],\n  },\n];\n// code + data both start loading as soon as navigation begins",
    interviewQuestion: "How would you structure routes so navigating within a dashboard does not re-fetch or re-render the sidebar and header on every page change?",
  },
  {
    id: "frontendsystemdesign-offline-first",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Offline-First Architecture",
    title: "How do you design an offline-first frontend architecture?",
    summary: "Offline-first apps treat the local device as the primary data source, syncing to the server in the background via a service worker and a local persistent store, with conflict resolution for divergent writes.",
    explanation: "A service worker caches the app shell and static assets (via the Cache API) so the app loads even with no network, while a local database (IndexedDB) stores application data that the UI reads from directly rather than always hitting the network. Writes go to the local store immediately (optimistic) and are queued in a background sync outbox that flushes to the server when connectivity returns, using the Background Sync API or a manual retry loop. Conflict resolution is required when the same record was edited offline on two devices — strategies range from last-write-wins to CRDTs to manual merge prompts depending on how critical correctness is. The UI must clearly surface sync status (pending, synced, conflict) so users trust the offline experience.",
    code: "// service worker: cache-first for shell, network-first for API with fallback\nself.addEventListener('fetch', (event) => {\n  if (event.request.url.includes('/api/')) {\n    event.respondWith(\n      fetch(event.request).catch(() => caches.match(event.request))\n    );\n  } else {\n    event.respondWith(caches.match(event.request).then((r) => r || fetch(event.request)));\n  }\n});\n\n// writes queue to IndexedDB outbox, flushed on 'online' event",
    interviewQuestion: "How would you design write conflict resolution for a note-taking app that must work fully offline across multiple devices?",
  },
  {
    id: "frontendsystemdesign-caching-strategy",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Frontend Caching Strategy",
    title: "How would you design a caching strategy across the frontend stack?",
    summary: "A layered caching strategy spans the browser HTTP cache, a service worker cache, and an in-memory application cache, each with different invalidation rules and appropriate TTLs.",
    explanation: "Static, immutable assets (hashed JS/CSS bundles) get long-lived HTTP cache headers (Cache-Control: immutable, max-age=1y) since a new deploy produces new filenames, avoiding stale-asset issues entirely. API responses need shorter, purpose-driven caching: some data is cache-then-network (stale-while-revalidate) for perceived speed, other data must always be fresh (financial balances). Application-level caching (React Query/SWR) adds request deduplication, background refetch on window focus, and cache invalidation keyed by query keys so a mutation can precisely invalidate related reads. A service worker can add an offline-capable cache layer on top, but needs a versioning/cleanup strategy so old caches do not accumulate indefinitely.",
    code: "// HTTP layer: immutable hashed assets\n// Cache-Control: public, max-age=31536000, immutable\n\n// App layer: stale-while-revalidate with targeted invalidation\nconst queryClient = new QueryClient({\n  defaultOptions: { queries: { staleTime: 60_000, refetchOnWindowFocus: true } },\n});\nfunction useUpdateProfile() {\n  return useMutation(updateProfile, {\n    onSuccess: () => queryClient.invalidateQueries(['profile']),\n  });\n}",
    interviewQuestion: "How would you design cache invalidation so that updating a user's profile immediately reflects everywhere it is displayed, without over-fetching?",
  },
  {
    id: "frontendsystemdesign-video-player",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Video Streaming Player UI",
    title: "How would you design the frontend architecture for a video streaming player like YouTube or Netflix?",
    summary: "A video player UI layers custom controls over adaptive-bitrate streaming (HLS/DASH), with buffering strategy, quality switching, and accessibility (captions, keyboard control) built in.",
    explanation: "Adaptive bitrate streaming (HLS or DASH via libraries like hls.js or Shaka Player) breaks video into short segments at multiple quality levels, and the player selects segment quality per-request based on measured bandwidth, enabling smooth playback that adapts to network conditions. The custom control layer (play/pause, scrubber, volume, captions, quality selector) sits over the native <video> element and must remain keyboard accessible and expose correct ARIA states. Preloading/prefetching the next likely segment (or next episode) reduces perceived latency, while a buffering health indicator and adaptive retry logic handle network interruptions gracefully. Analytics (rebuffer events, time-to-first-frame, quality switches) are typically instrumented directly against player lifecycle events to monitor QoE (quality of experience) in production.",
    code: "import Hls from 'hls.js';\nconst video = document.querySelector('video');\nconst hls = new Hls({ maxBufferLength: 30 });\nhls.loadSource(manifestUrl);\nhls.attachMedia(video);\nhls.on(Hls.Events.LEVEL_SWITCHED, (_, data) => logQualityChange(data.level));\n\nvideo.addEventListener('waiting', () => trackEvent('rebuffer_start'));",
    interviewQuestion: "How does adaptive bitrate streaming decide which video quality to fetch next, and how would you build custom accessible controls on top of it?",
  },
  {
    id: "frontendsystemdesign-product-listing",
    category: "frontendsystemdesign",
    difficulty: "Intermediate",
    topic: "E-commerce Product Listing Page",
    title: "How would you design an e-commerce product listing page (PLP) for scale and performance?",
    summary: "A PLP balances SEO (server-rendered initial content), fast filtering/sorting (URL-driven state with client-side re-fetch), and image-heavy grid rendering optimized with lazy loading and CLS prevention.",
    explanation: "Since PLPs are a major SEO entry point, initial HTML should be server-rendered or statically generated with product data present, rather than client-fetched after an empty shell. Filters, sort order, and pagination should live in the URL (query params) so results are shareable/bookmarkable and back/forward navigation works correctly, with the filter panel driving a re-fetch rather than local-only filtering once catalogs are large. Product images are the biggest performance cost, so responsive images (srcset), lazy loading below the fold, and reserved aspect-ratio containers (preventing CLS) are essential. Faceted filter counts (e.g. \"Red (12)\") typically come from a search index (Elasticsearch/Algolia) rather than the primary database, since computing live counts across large catalogs on every filter change is expensive.",
    code: "// filters live in URL, drive server/query fetch\nfunction PLP() {\n  const [params, setParams] = useSearchParams();\n  const { data } = useQuery(['products', params.toString()], () =>\n    fetchProducts(params)\n  );\n  return (\n    <>\n      <FilterPanel value={params} onChange={setParams} />\n      <ProductGrid items={data?.items} /> {/* images: loading=\"lazy\", aspect-ratio reserved */}\n    </>\n  );\n}",
    interviewQuestion: "Why should filters and pagination on a product listing page live in the URL instead of purely in component state?",
  },
  {
    id: "frontendsystemdesign-search-results-filters",
    category: "frontendsystemdesign",
    difficulty: "Intermediate",
    topic: "Search Results Page with Filters",
    title: "How do you design a search results page with dynamic filters?",
    summary: "A search-with-filters UI treats query + filters as a single serializable state object synced to the URL, debounces re-fetches, and shows optimistic filter counts before results confirm.",
    explanation: "The query text and every active filter should combine into one state object that maps directly to URL search params, making the exact result set shareable via link and restorable on back navigation. Because a user might toggle multiple filters quickly, requests should be debounced and superseded requests cancelled, similar to autocomplete, to avoid flicker from out-of-order responses. Applying a filter should keep already-rendered results visible with a loading overlay rather than blanking the page, to reduce perceived latency, and empty/no-results states need explicit design (suggested query relaxations). Filter facet counts ideally update to reflect what results would look like if a filter were applied, which requires either a smart search index or a secondary lightweight facet-count query.",
    code: "function useSearch() {\n  const [params, setParams] = useSearchParams();\n  const debounced = useDebouncedValue(params.toString(), 200);\n  const { data, isFetching } = useQuery(\n    ['search', debounced],\n    () => fetchResults(debounced),\n    { keepPreviousData: true } // avoids blank flash while filters change\n  );\n  return { data, isFetching, params, setParams };\n}",
    interviewQuestion: "How would you prevent a search results page from flashing empty or stale content every time a filter is toggled?",
  },
  {
    id: "frontendsystemdesign-a11y-system-design",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Accessibility in System Design",
    title: "How do you build accessibility into a frontend system design from the start rather than bolting it on?",
    summary: "Accessibility at the architecture level means baking semantic components, focus management, and automated a11y checks into the design system and CI pipeline, not treating it as a per-feature afterthought.",
    explanation: "The highest-leverage decision is building accessible primitives once in the shared component library (accessible dropdown, modal, tabs) so every feature team inherits correct ARIA roles, keyboard handling, and focus trapping for free instead of re-solving it per feature. System-level concerns include a global focus-management strategy for route changes and modals, a live-region service for announcing async updates (toasts, loaded content) to screen readers, and color/contrast tokens enforced by the design system rather than ad hoc per-component choices. Automated gates (axe-core in CI, Lighthouse a11y budgets) catch regressions, but must be paired with manual screen-reader testing since automated tools only catch a subset (roughly 30-40%) of real issues. Treating accessibility as a system design concern (a shared service/primitive layer) scales far better than mandating that every feature team re-implement it correctly.",
    code: "// Shared modal primitive - every consumer gets this for free\nfunction Modal({ children, onClose }) {\n  const ref = useFocusTrap();\n  useEffect(() => { const prev = document.activeElement; return () => prev?.focus(); }, []);\n  return (\n    <FocusTrapRegion ref={ref} role=\"dialog\" aria-modal=\"true\">\n      {children}\n      <button onClick={onClose} aria-label=\"Close\">x</button>\n    </FocusTrapRegion>\n  );\n}",
    interviewQuestion: "How would you scale accessibility across dozens of feature teams without relying on every engineer remembering to add ARIA attributes correctly?",
  },
  {
    id: "frontendsystemdesign-performance-budget",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Performance Budget in System Design",
    title: "How do you define and enforce a performance budget in a frontend system design?",
    summary: "A performance budget sets measurable limits (bundle size, Core Web Vitals thresholds) tied to user-facing metrics, enforced automatically in CI so regressions are blocked before merge, not discovered after launch.",
    explanation: "Budgets should target user-perceived metrics (Largest Contentful Paint, Interaction to Next Paint, Cumulative Layout Shift) rather than proxy metrics alone, since a smaller bundle does not guarantee a fast experience if, say, a render-blocking font causes layout shift. Bundle size budgets per route (e.g. initial JS \\< 170KB gzipped) are enforced via CI tooling (bundlesize, webpack performance hints) that fails the build on regression, catching issues before they reach production rather than relying on post-hoc monitoring. Real User Monitoring (RUM) data, segmented by device/network tier, validates that lab-measured budgets actually reflect real users, since a budget that only accounts for fast wifi and high-end devices misses the long tail. Budgets need owners and an escalation process (who gets paged when the budget is breached, what is the rollback path) or they decay into ignored CI warnings over time.",
    code: "// CI budget config (example)\n{\n  \"budgets\": [\n    { \"route\": \"/checkout\", \"resourceType\": \"script\", \"budget\": 170 },\n    { \"route\": \"/checkout\", \"metric\": \"LCP\", \"budget\": 2500 },\n    { \"route\": \"/checkout\", \"metric\": \"CLS\", \"budget\": 0.1 }\n  ]\n}\n// CI step: run Lighthouse CI, fail build if any budget is exceeded",
    interviewQuestion: "How would you set performance budgets that reflect real users rather than just your own fast development machine?",
  },
  {
    id: "frontendsystemdesign-multistep-form",
    category: "frontendsystemdesign",
    difficulty: "Intermediate",
    topic: "Multi-Step Form Wizard",
    title: "How would you design a multi-step form wizard architecture?",
    summary: "A wizard architecture centralizes all step data in one form state object, decouples per-step validation from navigation, and persists progress so users do not lose work on refresh or navigation away.",
    explanation: "All steps should share a single source-of-truth form state (rather than each step owning isolated local state) so data entered in step 1 is available for validation or conditional logic in step 3, and so a final submit sends one coherent payload. Each step defines its own validation schema (e.g. per-step Zod/Yup schema) and only the active step's validation blocks the 'Next' button, while a final cross-step validation runs before actual submission. Progress should persist to sessionStorage/localStorage or the URL on every step so users can leave and return, or the browser can be refreshed, without losing entered data. Step components should be code-split/lazy-loaded since a user only ever sees a fraction of a large multi-step wizard's total code in one session, and the step indicator should reflect both current position and completion/error state per step.",
    code: "const useWizard = create((set) => ({\n  step: 0,\n  data: {},\n  updateStep: (partial) => set((s) => ({ data: { ...s.data, ...partial } })),\n  next: () => set((s) => ({ step: s.step + 1 })),\n  back: () => set((s) => ({ step: Math.max(0, s.step - 1) })),\n}));\n// persist `data` to sessionStorage on every updateStep call",
    interviewQuestion: "How would you structure state in a 6-step signup wizard so a user can jump back to step 2, edit an answer, and have step 5 reflect that change?",
  },
  {
    id: "frontendsystemdesign-api-error-handling",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "API Error Handling Strategy",
    title: "How do you design an API error handling strategy that scales across a large frontend application?",
    summary: "A scalable error strategy classifies errors by type (network, validation, auth, server) at a shared HTTP layer, mapping each to a consistent UI treatment, retry policy, and logging behavior instead of handling errors ad hoc per call site.",
    explanation: "A centralized API client (interceptor/middleware layer) is the right place to classify errors once: 401s trigger a global re-auth flow, 5xx errors get automatic retry with exponential backoff, 4xx validation errors get surfaced as field-level form errors, and network failures show an offline-aware message, instead of every feature team writing its own try/catch logic inconsistently. Errors need a consistent shape (code, message, field-level details) enforced by an API contract so the frontend can pattern-match reliably rather than parsing free-text messages. User-facing error UI should differentiate recoverable errors (show a retry button) from unrecoverable ones (show a fallback state), and every unhandled error should still be caught by a top-level error boundary as a last resort. All errors get reported to a monitoring service (Sentry) with enough context (request id, user id, endpoint) to debug production issues without needing to reproduce them locally.",
    code: "apiClient.interceptors.response.use(\n  (res) => res,\n  async (error) => {\n    const { status } = error.response ?? {};\n    if (status === 401) return authStore.reauthenticate().then(() => apiClient(error.config));\n    if (status >= 500) return retryWithBackoff(error.config);\n    reportToMonitoring(error);\n    return Promise.reject(normalizeError(error));\n  }\n);",
    interviewQuestion: "How would you avoid every team writing its own inconsistent error handling for API calls across a large codebase?",
  },
  {
    id: "frontendsystemdesign-theming-system",
    category: "frontendsystemdesign",
    difficulty: "Intermediate",
    topic: "Theming System (Dark/Light Mode)",
    title: "How would you design a theming system that supports dark/light mode without a flash of incorrect theme?",
    summary: "A theming system exposes semantic design tokens as CSS custom properties, swaps a root attribute to switch themes instantly, and reads the saved preference before first paint to avoid a flash of unstyled/incorrect theme.",
    explanation: "Components should reference semantic tokens (--color-surface, --color-text-primary) rather than raw color values, and each theme is just a different mapping of those tokens, so switching themes is a single attribute change (data-theme=\"dark\") rather than a re-render of every component. To avoid FOUC/flash-of-wrong-theme, the theme preference must be read and applied via an inline blocking script in the document head before the framework hydrates, since waiting for JS/React to mount and read localStorage causes a visible flash. The system should support three states: explicit light, explicit dark, and 'system' (following prefers-color-scheme), with a listener for OS-level theme changes when in system mode. Beyond color, a full theming system may need to account for images/icons that need different variants per theme, and ensure contrast ratios are validated for both themes independently.",
    code: "<!-- inline script in <head>, runs before paint -->\n<script>\n  const saved = localStorage.getItem('theme');\n  const theme = saved || (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light');\n  document.documentElement.setAttribute('data-theme', theme);\n</script>\n\n/* tokens.css */\n[data-theme=\"light\"] { --color-surface: #fff; --color-text: #111; }\n[data-theme=\"dark\"]  { --color-surface: #111; --color-text: #eee; }",
    interviewQuestion: "How would you prevent a flash of the wrong theme on page load in a dark-mode-supporting app?",
  },
  {
    id: "frontendsystemdesign-permission-based-ui",
    category: "frontendsystemdesign",
    difficulty: "Tricky",
    topic: "Permission-Based UI",
    title: "How do you design a UI architecture that adapts to fine-grained user permissions/roles?",
    summary: "A permission-based UI centralizes role/permission checks behind a declarative gate component and hook fed by a single normalized permissions object, rather than scattering conditional checks throughout the component tree.",
    explanation: "Permissions should be fetched once at session start into a normalized client-side structure (e.g. a set of permission strings like billing:write) and exposed via a hook (usePermission('billing:write')) or a declarative <Can I=\"write\" a=\"billing\"> gate component, so feature teams do not hand-roll conditional logic per screen. Critically, frontend permission checks are a UX concern only (hiding/disabling controls a user should not see) and must never be the actual security boundary — every privileged action must be re-validated server-side, since client-side checks can be bypassed. For complex products, permissions can be hierarchical/role-based (RBAC) or attribute-based (ABAC, e.g. \"can edit if owner or admin\"), and the UI layer should be agnostic to which model is used, just consuming a boolean/decision from a shared evaluator. Navigation items, routes, and even entire micro-frontends may need to be conditionally excluded, not just visually hidden, to avoid leaking the existence of features to unauthorized users.",
    code: "function Can({ perform, children, fallback = null }) {\n  const permissions = usePermissions(); // Set<string> loaded once at login\n  return permissions.has(perform) ? children : fallback;\n}\n\n<Can perform=\"billing:write\">\n  <EditBillingButton />\n</Can>\n// Server still re-checks 'billing:write' on the actual mutation endpoint",
    interviewQuestion: "If a user hides a button via browser devtools that should have been permission-gated, what does that tell you about where the real security boundary must live?",
  },
  {
    id: "frontendsystemdesign-component-communication",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Component Communication at Scale",
    title: "What strategies exist for component communication in a large frontend application, and how do you choose between them?",
    summary: "Components communicate via props/callbacks for direct parent-child, context for scoped shared state, a global store for cross-tree app state, and an event bus for decoupled cross-cutting communication — each suited to a different relationship distance.",
    explanation: "Direct parent-child communication should default to props down, callbacks up, since it's the simplest to trace and debug; reaching for global solutions here adds unnecessary indirection. When state needs to be shared across a subtree without prop drilling through many intermediate layers, React Context (or a scoped provider) is appropriate, but should be split into multiple focused contexts rather than one giant context to avoid over-rendering. Truly global, cross-page application state (auth, theme, cart) belongs in a global store (Redux/Zustand) accessible from anywhere without a provider tree dependency. For loosely coupled communication between components that should not know about each other directly (e.g. a toast triggered by an unrelated deep component), a pub/sub event bus or a dedicated service is preferable to threading callbacks through many layers, though it should be used sparingly since it makes data flow harder to trace.",
    code: "// 1. Direct: props/callbacks (default choice)\n<Child value={v} onChange={setV} />\n\n// 2. Scoped shared state: Context\nconst FormContext = createContext();\n\n// 3. Global app state: store\nconst useAuth = create((set) => ({ user: null, login: (u) => set({ user: u }) }));\n\n// 4. Decoupled cross-cutting: event bus (use sparingly)\neventBus.emit('cart:itemAdded', item);\neventBus.on('cart:itemAdded', () => showToast('Added to cart'));",
    interviewQuestion: "When would you reach for an event bus instead of prop drilling or context, and what is the tradeoff of doing so?",
  },
  {
    id: "frontendsystemdesign-monitoring-error-tracking",
    category: "frontendsystemdesign",
    difficulty: "Intermediate",
    topic: "Frontend Monitoring & Error Tracking",
    title: "How do you design a frontend monitoring and error tracking system for production?",
    summary: "A monitoring system layers uncaught error capture (window.onerror, error boundaries), structured logging with request/session context, and Real User Monitoring for performance, all sampled and rate-limited to control cost and noise.",
    explanation: "Error capture needs multiple hooks: window.onerror and unhandledrejection for uncaught synchronous/async errors, and React error boundaries for render-time errors within the component tree, all reporting to a service (Sentry/Datadog) with contextual breadcrumbs (recent user actions, route, user id) attached, since a bare stack trace without context is hard to debug. Source maps must be uploaded (but not publicly served) so minified production stack traces resolve back to real source locations. Real User Monitoring captures Core Web Vitals and custom timing marks from actual user sessions, segmented by device/geography, which is essential because synthetic lab tests miss real-world variance. High-volume events (e.g. every network error) need sampling and deduplication/grouping by fingerprint so the monitoring system itself stays useful and affordable rather than drowning engineers in duplicate alerts.",
    code: "window.addEventListener('unhandledrejection', (e) => report(e.reason, { type: 'unhandledrejection' }));\nwindow.addEventListener('error', (e) => report(e.error, { type: 'window.onerror' }));\n\nclass AppErrorBoundary extends React.Component {\n  componentDidCatch(error, info) {\n    report(error, { componentStack: info.componentStack, route: location.pathname });\n  }\n}\n\n// RUM: report Core Web Vitals from real sessions\nonLCP((metric) => sendToAnalytics('LCP', metric.value, { sampleRate: 0.1 }));",
    interviewQuestion: "What information would you want attached to a captured frontend error to make it actually debuggable without reproducing it locally?",
  },
  {
    id: "frontendsystemdesign-rich-text-editor",
    category: "frontendsystemdesign",
    difficulty: "Tricky",
    topic: "Rich Text Editor",
    title: "How would you design the architecture of a rich text editor (like Notion or Medium's editor)?",
    summary: "A rich text editor represents content as a structured document model (not raw HTML/contenteditable state), with a schema that constrains valid structure, a command layer for edits, and serializers for HTML/Markdown/JSON output.",
    explanation: "Directly manipulating contenteditable's DOM and relying on the browser's native editing behavior is notoriously inconsistent across browsers, so production editors (ProseMirror, Slate, Lexical) maintain their own document model (a tree of typed nodes) as the source of truth, and reconcile it to the DOM, similar to how React reconciles a virtual DOM. A schema defines what structures are valid (e.g. a list item can only contain paragraphs and nested lists), preventing invalid states like a table nested directly inside a heading. Edits are expressed as commands/transactions (insertText, toggleMark, splitBlock) rather than direct mutations, which enables clean undo/redo history and makes collaborative editing (via OT/CRDT on top of the same model) tractable. The model needs serializers to and from HTML (for paste/copy and display) and often Markdown or a custom JSON format for storage, plus careful handling of paste sanitization to avoid injecting arbitrary/unsafe HTML from external sources.",
    code: "// Simplified document model (ProseMirror-style)\ndoc = {\n  type: 'doc',\n  content: [\n    { type: 'heading', attrs: { level: 1 }, content: [{ type: 'text', text: 'Title' }] },\n    { type: 'bulletList', content: [\n      { type: 'listItem', content: [{ type: 'paragraph', content: [{ type: 'text', text: 'Item' }] }] },\n    ]},\n  ],\n};\n// Commands mutate via transactions: tr.insertText('hi').toggleMark('bold')",
    interviewQuestion: "Why do most production rich text editors avoid relying directly on contenteditable's native DOM state as the source of truth?",
  },
  {
    id: "frontendsystemdesign-drag-and-drop",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Drag-and-Drop Interface",
    title: "How would you design a drag-and-drop interface architecture, like a Trello-style board?",
    summary: "A drag-and-drop system separates the drag interaction layer (pointer tracking, drag preview, drop-target detection) from the underlying ordered data model, updating an optimistic local order during drag and persisting on drop.",
    explanation: "Rather than using the native HTML5 Drag and Drop API (which has awkward styling and mobile support limitations), most production implementations use pointer events to track drag position and manually compute the nearest drop target/insertion index by comparing the pointer's coordinates against rendered item bounding boxes. During the drag, the UI shows an optimistic reordering (placeholder gap, live-reordered list) purely as local visual state, and the actual data model (array order, column assignment) is only committed on drop, with the change persisted to the backend and rolled back on failure. Accessibility requires a fully separate keyboard-operable path (e.g. select an item, use arrow keys or dedicated 'move up/move down' actions) since drag gestures are not usable by keyboard-only or screen-reader users. Performance matters at scale: reordering must avoid re-rendering the entire list on every pointer-move tick, typically via transform-based positioning rather than re-flowing layout continuously.",
    code: "function onPointerMove(e) {\n  const overIndex = findDropIndex(e.clientY, itemRects);\n  if (overIndex !== dragState.previewIndex) {\n    setDragState((s) => ({ ...s, previewIndex: overIndex })); // visual only\n  }\n}\nfunction onPointerUp() {\n  commitReorder(dragState.itemId, dragState.previewIndex); // persists + syncs backend\n}\n// Keyboard alt-path: onKeyDown -> moveItem(item, 'up' | 'down')",
    interviewQuestion: "How would you make a drag-and-drop kanban board usable for a keyboard-only user who cannot perform a mouse drag gesture?",
  },
  {
    id: "frontendsystemdesign-i18n-architecture",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Internationalization Architecture",
    title: "How would you architect internationalization (i18n) for a large frontend application?",
    summary: "An i18n architecture externalizes all user-facing strings into locale-keyed message catalogs loaded on demand per locale, uses ICU message syntax for pluralization/formatting, and accounts for layout direction and locale-aware formatting throughout the design system.",
    explanation: "All strings must be extracted out of components into message catalogs (JSON per locale) referenced by key, with a build/lint step that fails if a raw hardcoded string is committed, since retrofitting i18n after the fact is far more expensive than building it in from the start. Locale catalogs should be code-split and lazy-loaded per locale (not bundling all languages into the main bundle) so users only download the language they need. Pluralization, gender, and interpolation are handled via ICU MessageFormat syntax rather than manual string concatenation, since grammar rules (plural forms, word order) vary significantly across languages and cannot be hardcoded from an English mental model. The design system needs to support RTL layout (logical CSS properties like margin-inline-start instead of margin-left, mirrored icons) and locale-aware formatting for dates, numbers, and currency via the Intl API rather than manual formatting.",
    code: "// en.json: { \"cart.itemCount\": \"{count, plural, =0 {No items} one {# item} other {# items}}\" }\nimport { useIntl } from 'react-intl';\nconst intl = useIntl();\nintl.formatMessage({ id: 'cart.itemCount' }, { count: items.length });\n\n// CSS: logical properties support RTL automatically\n.card { padding-inline-start: 16px; }\n\nnew Intl.NumberFormat(locale, { style: 'currency', currency: 'EUR' }).format(49.99);",
    interviewQuestion: "Why is ICU MessageFormat preferable to string concatenation with if/else pluralization logic when internationalizing an app?",
  },
  {
    id: "frontendsystemdesign-frontend-security",
    category: "frontendsystemdesign",
    difficulty: "Advanced",
    topic: "Frontend Security in System Design",
    title: "What frontend security considerations should shape a system design at the architecture level?",
    summary: "Frontend security architecture layers XSS prevention (auto-escaping templates, sanitized rich content, a strict CSP), CSRF protection for cookie-based auth, secure token storage, and dependency supply-chain vetting, applied consistently rather than per-feature.",
    explanation: "XSS is the most common frontend vulnerability class: modern frameworks auto-escape interpolated content by default, but any use of dangerouslySetInnerHTML or v-html for rich text/user content must run through a sanitizer (DOMPurify) with an allowlist, and a strict Content-Security-Policy header should be treated as defense-in-depth against XSS that slips through. Authentication tokens should generally live in httpOnly, secure, sameSite cookies rather than localStorage, since localStorage is readable by any injected script (an XSS becomes a full account takeover), whereas httpOnly cookies are not accessible to JS at all. If cookie-based auth is used, CSRF protection (synchronizer tokens or sameSite=strict/lax) is required since the browser will attach cookies automatically to cross-site requests. Beyond runtime code, the dependency supply chain (npm packages, especially transitive ones) is a real attack surface, warranting lockfiles, automated vulnerability scanning (npm audit/Snyk/Dependabot), and minimizing unnecessary third-party scripts, particularly ones loaded from third-party CDNs without subresource integrity.",
    code: "// Sanitize any user-generated HTML before rendering\nimport DOMPurify from 'dompurify';\n<div dangerouslySetInnerHTML={{ __html: DOMPurify.sanitize(userHtml) }} />\n\n// CSP header (server-set, defense in depth)\n// Content-Security-Policy: default-src 'self'; script-src 'self' https://cdn.acme.com\n\n// Auth: httpOnly cookie, not localStorage\n// Set-Cookie: session=...; HttpOnly; Secure; SameSite=Strict",
    interviewQuestion: "Why is storing an auth token in an httpOnly cookie generally safer than storing it in localStorage, and what does that protect against?",
  },
];
