// 5 python topics for Study Hub.
export default [
  {
    id: "python-basics-datatypes",
    category: "python",
    topic: "Core Python",
    title: "Data Types & Variables",
    difficulty: "Basic",
    summary: "Python has dynamic typing — variables have no declared type",
    explanation:
      "Python's built-in types: int, float, str, bool, list, tuple, set, dict, NoneType. Use type() to inspect. Everything is an object.",
    code: 'x = 42          # int\ny = 3.14        # float\nname = "Alice"  # str\nactive = True   # bool\nnums = [1,2,3]  # list  — mutable\npair = (1,2)    # tuple — immutable\nunique = {1,2}  # set   — unordered unique\ndata = {"a":1}  # dict  — key-value\n\nprint(type(x))  # <class \'int\'>',
    interviewQuestion:
      "What is the difference between a list and a tuple in Python?",
  },
  {
    id: "python-comprehensions",
    category: "python",
    topic: "Core Python",
    title: "List & Dict Comprehensions",
    difficulty: "Intermediate",
    summary: "Concise way to build lists, dicts, and sets in one line",
    explanation:
      "Comprehensions are faster than loops because they're optimized at the C level. Use them for simple transformations; avoid nesting more than 2 levels.",
    code: '# List comprehension\nsquares = [x**2 for x in range(10) if x % 2 == 0]\n# [0, 4, 16, 36, 64]\n\n# Dict comprehension\nword_len = {w: len(w) for w in ["python","is","great"]}\n# {\'python\': 6, \'is\': 2, \'great\': 5}\n\n# Set comprehension\nuniq_chars = {c.lower() for c in "Hello World" if c != " "}',
    interviewQuestion:
      "When would you use a generator expression instead of a list comprehension?",
  },
  {
    id: "python-decorators",
    category: "python",
    topic: "Core Python",
    title: "Decorators",
    difficulty: "Advanced",
    summary: "Functions that wrap other functions to add behaviour",
    explanation:
      "A decorator is syntactic sugar for higher-order functions. @functools.wraps preserves the wrapped function's metadata (__name__, __doc__).",
    code: 'import functools, time\n\ndef timer(func):\n    @functools.wraps(func)\n    def wrapper(*args, **kwargs):\n        start = time.perf_counter()\n        result = func(*args, **kwargs)\n        print(f"{func.__name__} took {time.perf_counter()-start:.4f}s")\n        return result\n    return wrapper\n\n@timer\ndef slow():\n    time.sleep(0.1)\n\nslow()  # slow took 0.1001s',
    interviewQuestion: "What does @functools.wraps do and why is it important?",
  },
  {
    id: "python-generators",
    category: "python",
    topic: "Core Python",
    title: "Generators & yield",
    difficulty: "Intermediate",
    summary:
      "Lazy iterators that produce values one at a time — memory efficient",
    explanation:
      "A generator function uses yield instead of return. Execution suspends at each yield and resumes on next(). Great for large data streams.",
    code: "def fibonacci():\n    a, b = 0, 1\n    while True:\n        yield a\n        a, b = b, a + b\n\ngen = fibonacci()\nprint([next(gen) for _ in range(8)])\n# [0, 1, 1, 2, 3, 5, 8, 13]\n\n# Generator expression (lazy list comprehension)\nevens = (x for x in range(1_000_000) if x % 2 == 0)\nprint(next(evens))  # 0 — only one value computed",
    interviewQuestion:
      "What is the memory advantage of a generator over a list?",
  },
  {
    id: "python-oop",
    category: "python",
    topic: "Core Python",
    title: "Classes & OOP",
    difficulty: "Intermediate",
    summary:
      "Python supports full OOP: inheritance, encapsulation, polymorphism",
    explanation:
      "__init__ is the constructor. Use @property for computed attributes. __repr__ for dev-friendly string, __str__ for user-friendly. Single underscore _x = convention-private; double __x = name-mangled.",
    code: 'class Animal:\n    def __init__(self, name: str):\n        self.name = name\n\n    def speak(self) -> str:\n        raise NotImplementedError\n\n    def __repr__(self):\n        return f"Animal({self.name!r})"\n\nclass Dog(Animal):\n    def speak(self):\n        return f"{self.name} says Woof!"\n\ndog = Dog("Rex")\nprint(dog.speak())   # Rex says Woof!\nprint(repr(dog))     # Animal(\'Rex\')',
    interviewQuestion: "What is the difference between __repr__ and __str__?",
  },
// ---------- BASICS TIER ----------
  {
    id: "python-strings-deep-dive",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Strings: Formatting & Methods",
    summary: "Python strings are immutable sequences with rich built-in formatting and manipulation methods.",
    explanation: "Strings support slicing, concatenation, and dozens of methods like split(), join(), strip(), replace(), and case conversions. f-strings (formatted string literals) are the modern, readable way to embed expressions inside string literals, replacing older % formatting and str.format(). Because strings are immutable, every method that 'modifies' a string actually returns a new string object.",
    code: 'name = "Ada"\nscore = 92.567\n\n# f-strings with format specs\nprint(f"{name} scored {score:.1f}%")   # Ada scored 92.6%\nprint(f"{name!r}")                     # \'Ada\'\n\n# Common methods\ns = "  Hello, World!  "\nprint(s.strip())            # "Hello, World!"\nprint(s.strip().lower())    # "hello, world!"\nprint(s.strip().split(", "))# [\'Hello\', \'World!\']\nprint("-".join(["a","b","c"]))  # "a-b-c"\nprint(s.strip().replace("World", "Python"))\nprint("hello".startswith("he"), "hello".endswith("lo"))',
    interviewQuestion: "How do f-strings differ from str.format() and % formatting, and why are strings immutable in Python?",
  },
  {
    id: "python-lists-deep-dive",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Lists: Methods & Slicing",
    summary: "Lists are mutable, ordered sequences with powerful slicing syntax and in-place methods.",
    explanation: "Slicing uses the [start:stop:step] syntax and always returns a new list, supporting negative indices and reversal via [::-1]. In-place methods like append(), extend(), insert(), pop(), and sort() mutate the original list, while functions like sorted() return a new one. A common pitfall is confusing append() (adds one element) with extend() (adds each element of an iterable).",
    code: 'nums = [5, 3, 8, 1, 9, 2]\n\n# Slicing\nprint(nums[1:4])     # [3, 8, 1]\nprint(nums[::-1])    # [2, 9, 1, 8, 3, 5]\nprint(nums[:3])      # [5, 3, 8]\n\n# Mutating methods\nnums.append(100)     # add single item\nnums.extend([7, 6])  # add each item\nnums.insert(0, -1)   # insert at index\nnums.sort()           # in-place sort\nprint(nums)\n\n# Non-mutating\nnew_sorted = sorted(nums, reverse=True)\nprint(nums.pop())    # removes & returns last item',
    interviewQuestion: "What is the difference between list.append() and list.extend()? What does a negative step in slicing do?",
    comparison: {
      vs: "Tuples",
      rows: [
        { aspect: "Mutability", a: "mutable — append/insert/sort/pop change it in place", b: "immutable — once created, elements can't be reassigned" },
        { aspect: "Hashable", a: "no — can't be used as a dict key or set element", b: "yes (if contents are) — commonly used as dict keys" },
        { aspect: "Signals intent", a: "a collection that will change over time", b: "a fixed record/shape (a point, a row, multiple return values)" },
      ],
      takeaway: "Use a list when the collection will grow/shrink/reorder. Use a tuple for a fixed-size, unchanging group of values — and when you need it as a dict key.",
    },
  },
  {
    id: "python-tuples",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Tuples",
    summary: "Tuples are immutable, ordered sequences often used for fixed collections and multiple return values.",
    explanation: "Once created, a tuple's elements cannot be reassigned, though a tuple can hold mutable objects like lists whose contents can still change. Tuples support unpacking, including extended unpacking with *rest, and are commonly used as dictionary keys or set elements because they are hashable (when their contents are). A single-element tuple requires a trailing comma, e.g. (1,).",
    code: 'point = (3, 4)\nx, y = point  # unpacking\nprint(x, y)\n\n# Extended unpacking\nfirst, *middle, last = (1, 2, 3, 4, 5)\nprint(first, middle, last)  # 1 [2, 3, 4] 5\n\n# Single-element tuple needs a comma\nsingle = (5,)\nnot_a_tuple = (5)\nprint(type(single), type(not_a_tuple))\n\n# Tuples as dict keys (hashable)\nlocations = {(0, 0): "origin", (1, 1): "diagonal"}\nprint(locations[(0, 0)])',
    interviewQuestion: "Why are tuples hashable but lists are not, and how does that affect their use as dictionary keys?",
    comparison: {
      vs: "Lists",
      rows: [
        { aspect: "Mutability", a: "immutable — once created, elements can't be reassigned", b: "mutable — append/insert/sort/pop change it in place" },
        { aspect: "Hashable", a: "yes (if contents are) — commonly used as dict keys", b: "no — can't be used as a dict key or set element" },
        { aspect: "Signals intent", a: "a fixed record/shape (a point, a row, multiple return values)", b: "a collection that will change over time" },
      ],
      takeaway: "Use a tuple for a fixed-size, unchanging group of values — and when you need it as a dict key. Use a list when the collection will grow/shrink/reorder.",
    },
  },
  {
    id: "python-sets",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Sets",
    summary: "Sets are unordered collections of unique, hashable elements optimized for membership tests and set algebra.",
    explanation: "Sets provide O(1) average-case membership testing and support mathematical operations like union (|), intersection (&), difference (-), and symmetric difference (^). Because sets are unordered, they cannot be indexed. Frozensets are the immutable, hashable counterpart, usable as dictionary keys or set elements themselves.",
    code: 'a = {1, 2, 3, 4}\nb = {3, 4, 5, 6}\n\nprint(a | b)   # union: {1,2,3,4,5,6}\nprint(a & b)   # intersection: {3,4}\nprint(a - b)   # difference: {1,2}\nprint(a ^ b)   # symmetric diff: {1,2,5,6}\n\nprint(3 in a)  # O(1) membership test\n\n# frozenset is immutable/hashable\nfs = frozenset([1, 2, 3])\nnested = {fs, frozenset([4, 5])}\nprint(nested)',
    interviewQuestion: "Why is checking membership in a set faster than in a list, and what problem does frozenset solve?",
  },
  {
    id: "python-dictionaries-deep-dive",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Dictionaries: Methods & Patterns",
    summary: "Dictionaries store key-value pairs with O(1) average lookup, and since Python 3.7 preserve insertion order.",
    explanation: "Common methods include get() (with a default to avoid KeyError), setdefault(), update(), pop(), and items()/keys()/values() for iteration. Dictionary comprehensions and the merge operator (|, Python 3.9+) make building and combining dicts concise. Keys must be hashable, so lists cannot be used as keys but tuples can.",
    code: 'user = {"name": "Sam", "age": 30}\n\nprint(user.get("email", "N/A"))  # safe default\nuser.setdefault("roles", []).append("admin")\n\nfor key, value in user.items():\n    print(key, "->", value)\n\n# Merge dicts (3.9+)\ndefaults = {"theme": "dark", "age": 0}\nmerged = defaults | user\nprint(merged)\n\n# Dict comprehension\nsquares = {n: n**2 for n in range(5)}\nprint(squares)',
    interviewQuestion: "What is the difference between dict.get() and direct key access with dict[key], and how does dict.setdefault() work?",
  },
  {
    id: "python-loops-nuances",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Loops: for/while Nuances",
    summary: "Python's for loops iterate over any iterable, and both loop types support an often-overlooked else clause.",
    explanation: "A for loop's else block executes only if the loop completes without hitting a break, which is useful for search patterns. enumerate() and zip() are idiomatic ways to get indices or iterate multiple sequences in parallel instead of manual index management. continue skips to the next iteration, while break exits the loop entirely, and both interact with the else clause differently.",
    code: 'items = ["apple", "banana", "cherry"]\n\nfor i, item in enumerate(items, start=1):\n    print(i, item)\n\nprices = [1.5, 0.5, 2.0]\nfor item, price in zip(items, prices):\n    print(item, price)\n\n# for-else: else runs only if no break occurred\nfor n in range(2, 10):\n    if n % 7 == 0:\n        print("Found multiple of 7:", n)\n        break\nelse:\n    print("No multiple of 7 found")\n\n# while with break/continue\ncount = 0\nwhile count < 5:\n    count += 1\n    if count == 3:\n        continue\n    print(count)',
    interviewQuestion: "What does the else clause on a for loop do, and when would you use it over a flag variable?",
  },
  {
    id: "python-conditions-truthy-walrus",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Conditions: Truthy/Falsy & Walrus Operator",
    summary: "Python evaluates non-boolean values in boolean contexts using truthiness rules, and the walrus operator assigns within expressions.",
    explanation: "Falsy values include 0, 0.0, '', [], {}, (), set(), None, and False; everything else is truthy. The walrus operator (:=), introduced in Python 3.8, lets you assign a value to a variable as part of a larger expression, commonly used to avoid calling a function twice in while loops or comprehensions. Chained comparisons like 0 < x < 10 are also idiomatic Python.",
    code: 'values = [0, "", [], None, "hello", 42]\nfor v in values:\n    print(v, "->", "truthy" if v else "falsy")\n\n# Walrus operator avoids double computation\ndata = [1, 2, 3, 4, 5]\nif (n := len(data)) > 3:\n    print(f"List is long: {n} items")\n\n# Useful in while loops\nimport random\nrandom.seed(1)\nwhile (roll := random.randint(1, 6)) != 6:\n    print("Rolled", roll)\nprint("Got a 6!")\n\n# Chained comparison\nx = 5\nprint(0 < x < 10)',
    interviewQuestion: "What values are considered falsy in Python, and what problem does the walrus operator (:=) solve?",
  },
  {
    id: "python-functions-default-args-first-class",
    category: "python",
    difficulty: "Basic",
    topic: "Basics",
    title: "Functions: Default Args & First-Class Functions",
    summary: "Python functions support default parameter values and are first-class objects that can be passed, returned, and stored.",
    explanation: "A classic pitfall is using a mutable default argument (like a list or dict) — because defaults are evaluated once at function definition time, the same object is reused across calls, causing unexpected shared state. The idiomatic fix is to default to None and create the mutable object inside the function body. Because functions are first-class, they can be assigned to variables, stored in data structures, and passed as arguments to higher-order functions.",
    code: 'def greet(name, greeting="Hello"):\n    return f"{greeting}, {name}!"\n\nprint(greet("Amy"))\nprint(greet("Bo", greeting="Hi"))\n\n# BUGGY: mutable default argument\ndef add_item_bad(item, bucket=[]):\n    bucket.append(item)\n    return bucket\n\nprint(add_item_bad("a"))  # [\'a\']\nprint(add_item_bad("b"))  # [\'a\', \'b\'] -- unexpected!\n\n# FIXED version\ndef add_item_good(item, bucket=None):\n    if bucket is None:\n        bucket = []\n    bucket.append(item)\n    return bucket\n\n# Functions are first-class\ndef apply_twice(fn, value):\n    return fn(fn(value))\n\nprint(apply_twice(lambda x: x * 2, 3))  # 12',
    interviewQuestion: "Why is using a mutable object as a default argument value considered dangerous in Python, and how do you fix it?",
  },

  // ---------- INTERMEDIATE TIER ----------
  {
    id: "python-iterators-protocol",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "Iterators & the Iterator Protocol",
    summary: "An iterator is any object implementing __iter__ and __next__, forming the low-level protocol that powers for loops.",
    explanation: "An iterable is an object that implements __iter__ and returns an iterator; an iterator implements both __iter__ (returning itself) and __next__ (returning the next value or raising StopIteration). This is distinct from generators, which are a convenient shortcut for building iterators using yield. Understanding the protocol explains why you can only exhaust an iterator once, unlike a re-iterable container like a list.",
    code: 'class CountUp:\n    """Custom iterator implementing the iterator protocol."""\n    def __init__(self, limit):\n        self.limit = limit\n        self.current = 0\n\n    def __iter__(self):\n        return self\n\n    def __next__(self):\n        if self.current >= self.limit:\n            raise StopIteration\n        self.current += 1\n        return self.current\n\ncounter = CountUp(3)\nprint(list(counter))   # [1, 2, 3]\nprint(list(counter))   # [] -- exhausted, cannot restart\n\n# Manual protocol usage\nit = iter([10, 20, 30])\nprint(next(it))  # 10\nprint(next(it))  # 20',
    interviewQuestion: "What is the difference between an iterable and an iterator, and how do generators relate to the iterator protocol?",
  },
  {
    id: "python-lambda-functions",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "Lambda Functions",
    summary: "Lambdas are small, single-expression anonymous functions often used inline with higher-order functions like sorted() or map().",
    explanation: "A lambda can take any number of arguments but only contains a single expression, whose result is implicitly returned — it cannot contain statements like assignments or multiple lines. They're most idiomatic as short throwaway callbacks (e.g., a sort key), while anything more complex should be a named def function for readability. Lambdas also close over variables from their enclosing scope, which can lead to late-binding surprises in loops.",
    code: 'people = [("Sam", 25), ("Ana", 32), ("Bo", 19)]\n\n# Sort by age using lambda as key\npeople.sort(key=lambda p: p[1])\nprint(people)\n\nsquared = list(map(lambda x: x**2, [1, 2, 3, 4]))\nprint(squared)\n\nevens = list(filter(lambda x: x % 2 == 0, range(10)))\nprint(evens)\n\n# Late-binding pitfall in loops\nfuncs = [lambda: i for i in range(3)]\nprint([f() for f in funcs])  # [2, 2, 2] not [0, 1, 2]\n\n# Fix with default argument capture\nfuncs_fixed = [lambda i=i: i for i in range(3)]\nprint([f() for f in funcs_fixed])  # [0, 1, 2]',
    interviewQuestion: "Why does creating lambdas in a loop often produce unexpected results, and how do you fix the late-binding closure issue?",
  },
  {
    id: "python-args-kwargs",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "*args & **kwargs",
    summary: "*args and **kwargs let functions accept a variable number of positional and keyword arguments respectively.",
    explanation: "*args collects extra positional arguments into a tuple, while **kwargs collects extra keyword arguments into a dict; the names are convention, not syntax (the * and ** are what matter). They're commonly used to write flexible wrapper functions and decorators that forward arbitrary arguments to another function. You can also use * and ** to unpack a list/tuple and dict respectively when calling a function.",
    code: 'def summarize(*args, **kwargs):\n    print("positional:", args)\n    print("keyword:", kwargs)\n\nsummarize(1, 2, 3, name="Sam", age=30)\n# positional: (1, 2, 3)\n# keyword: {\'name\': \'Sam\', \'age\': 30}\n\ndef combine(a, b, c):\n    return a + b + c\n\nnums = [1, 2, 3]\nprint(combine(*nums))  # unpacking a list as positional args\n\nkw = {"a": 10, "b": 20, "c": 30}\nprint(combine(**kw))   # unpacking a dict as keyword args\n\n# Forwarding args in a wrapper\ndef logged(fn):\n    def wrapper(*args, **kwargs):\n        print(f"Calling {fn.__name__}")\n        return fn(*args, **kwargs)\n    return wrapper',
    interviewQuestion: "How would you write a decorator that works with functions of any signature, and what role do *args/**kwargs play?",
  },
  {
    id: "python-scope-legb",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "Scope & the LEGB Rule",
    summary: "Python resolves variable names using the LEGB rule: Local, Enclosing, Global, then Built-in scope, in that order.",
    explanation: "When a name is referenced, Python searches the innermost (Local) scope first, then any Enclosing function scopes, then the module-level Global scope, and finally Built-in names. Assigning to a variable inside a function creates a new local variable by default, so the global or nonlocal keywords are required to modify an outer-scope variable rather than shadow it. This explains common UnboundLocalError bugs when a variable is read before being locally assigned later in the same function.",
    code: 'x = "global"\n\ndef outer():\n    x = "enclosing"\n    def inner():\n        x = "local"\n        print(x)  # local\n    inner()\n    print(x)  # enclosing\n\nouter()\nprint(x)  # global\n\ncounter = 0\ndef increment():\n    global counter\n    counter += 1\n\nincrement()\nprint(counter)  # 1\n\ndef make_counter():\n    count = 0\n    def inc():\n        nonlocal count\n        count += 1\n        return count\n    return inc\n\nc = make_counter()\nprint(c(), c(), c())  # 1 2 3',
    interviewQuestion: "What is the LEGB rule, and what is the difference between using global and nonlocal inside a nested function?",
  },
  {
    id: "python-exception-handling-advanced",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "Exception Handling: Custom Exceptions, else/finally",
    summary: "Python's try/except/else/finally structure lets you handle errors precisely, and custom exceptions model domain-specific failures.",
    explanation: "The else block runs only if no exception was raised in the try block, keeping error-free logic separate from the risky code, while finally always runs for cleanup regardless of what happened. Custom exceptions should subclass Exception (or a more specific built-in) to create a meaningful hierarchy that callers can catch selectively. Using 'raise NewError(...) from original_error' preserves the original traceback context for debugging.",
    code: 'class InsufficientFundsError(Exception):\n    """Raised when a withdrawal exceeds the balance."""\n    def __init__(self, balance, amount):\n        self.balance = balance\n        self.amount = amount\n        super().__init__(f"Cannot withdraw {amount}, balance is {balance}")\n\ndef withdraw(balance, amount):\n    try:\n        if amount > balance:\n            raise InsufficientFundsError(balance, amount)\n        balance -= amount\n    except InsufficientFundsError as e:\n        print("Error:", e)\n        raise\n    else:\n        print("Withdrawal successful, no errors")\n    finally:\n        print("Transaction attempt finished")\n    return balance\n\ntry:\n    withdraw(100, 150)\nexcept InsufficientFundsError:\n    print("Handled at caller level")',
    interviewQuestion: "What is the difference between the else and finally clauses in a try block, and when would you define a custom exception class?",
  },
  {
    id: "python-file-handling-context-managers",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "File Handling with Context Managers",
    summary: "The with statement ensures files are properly closed even if an error occurs, by using the file object's context manager protocol.",
    explanation: "Opening a file with 'with open(...) as f' guarantees f.close() is called automatically when the block exits, whether normally or via an exception, avoiding resource leaks from forgotten close() calls. Common modes include 'r' (read), 'w' (write, truncates), 'a' (append), and 'rb'/'wb' for binary. Multiple files can be opened in a single with statement, and reading large files line-by-line (rather than .read() all at once) keeps memory usage low.",
    code: '# Writing then reading a file safely\nwith open("notes.txt", "w") as f:\n    f.write("line one\\n")\n    f.write("line two\\n")\n\nwith open("notes.txt", "r") as f:\n    for line in f:\n        print(line.strip())\n\n# Multiple files in one with statement\nwith open("notes.txt") as src, open("copy.txt", "w") as dst:\n    dst.write(src.read())\n\n# File is guaranteed closed even on exception\ntry:\n    with open("notes.txt") as f:\n        raise ValueError("boom")\nexcept ValueError:\n    print("f.closed is now:", f.closed)  # True',
    interviewQuestion: "What guarantee does the with statement provide when working with files, and what happens if an exception occurs inside the block?",
  },
  {
    id: "python-modules-packages",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "Modules & Packages",
    summary: "A module is a single .py file of reusable code, while a package is a directory of modules organized with an __init__.py.",
    explanation: "Python locates modules using sys.path, and the import system caches loaded modules in sys.modules so repeated imports don't re-execute the module body. __init__.py marks a directory as a package (optional but conventional since Python 3.3 for regular packages) and can control what's exposed via __all__. The if __name__ == '__main__': guard lets a file be both an importable module and a runnable script, since __name__ is '__main__' only when run directly.",
    code: '# mypackage/__init__.py\n# from .utils import helper  -- re-export for convenience\n\n# mypackage/utils.py\ndef helper():\n    return "helping!"\n\n# main.py\nimport mypackage.utils as utils\nfrom mypackage.utils import helper\n\nprint(utils.helper())\nprint(helper())\n\n# Runnable-vs-importable guard\ndef main():\n    print("Running as a script")\n\nif __name__ == "__main__":\n    main()\n\n# Modules are cached after first import\nimport sys\nprint("mypackage.utils" in sys.modules)',
    interviewQuestion: "What does the if __name__ == '__main__': guard do, and what is the purpose of __init__.py in a package?",
  },
  {
    id: "python-context-managers-protocol",
    category: "python",
    difficulty: "Intermediate",
    topic: "Intermediate",
    title: "Context Managers: __enter__/__exit__ Protocol",
    summary: "Context managers implement __enter__ and __exit__ to set up and reliably tear down resources around a with block.",
    explanation: "__enter__ runs at the start of the with block and its return value is bound to the 'as' variable; __exit__ runs on exit and receives exception info (type, value, traceback) if one occurred, returning True to suppress the exception or False/None to let it propagate. The contextlib.contextmanager decorator offers a simpler generator-based way to write context managers without a full class, using yield to separate setup from teardown code.",
    code: 'class Timer:\n    def __enter__(self):\n        import time\n        self.start = time.perf_counter()\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        import time\n        self.elapsed = time.perf_counter() - self.start\n        print(f"Elapsed: {self.elapsed:.4f}s")\n        return False  # do not suppress exceptions\n\nwith Timer() as t:\n    sum(range(1_000_000))\n\n# Simpler function-based version\nfrom contextlib import contextmanager\n\n@contextmanager\ndef managed_resource(name):\n    print(f"Acquiring {name}")\n    try:\n        yield name\n    finally:\n        print(f"Releasing {name}")\n\nwith managed_resource("db-connection") as res:\n    print("Using", res)',
    interviewQuestion: "What do __enter__ and __exit__ receive and return, and how does returning True from __exit__ change exception behavior?",
  },

  // ---------- ADVANCED TIER ----------
  {
    id: "python-dataclasses",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Dataclasses",
    summary: "The @dataclass decorator auto-generates boilerplate like __init__, __repr__, and __eq__ for classes that mainly hold data.",
    explanation: "Dataclasses reduce boilerplate for simple data-holding classes by generating __init__, __repr__, and __eq__ based on annotated fields. Fields can have defaults, and frozen=True makes instances immutable (raising an error on attribute assignment) while also making them hashable. Mutable defaults like lists must use field(default_factory=list) instead of a bare default, avoiding the same shared-mutable-default problem as regular functions.",
    code: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Point:\n    x: float\n    y: float\n    tags: list = field(default_factory=list)\n\np1 = Point(1.0, 2.0)\np2 = Point(1.0, 2.0)\nprint(p1)          # Point(x=1.0, y=2.0, tags=[])\nprint(p1 == p2)     # True -- auto-generated __eq__\n\n@dataclass(frozen=True)\nclass ImmutablePoint:\n    x: float\n    y: float\n\nip = ImmutablePoint(1.0, 2.0)\ntry:\n    ip.x = 5.0\nexcept Exception as e:\n    print("Error:", e)  # cannot assign to field',
    interviewQuestion: "Why must mutable default values in a dataclass use field(default_factory=...), and what does frozen=True actually do?",
  },
  {
    id: "python-type-hints",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Type Hints & the typing Module",
    summary: "Type hints annotate expected types for variables, parameters, and return values, enabling static analysis without affecting runtime behavior.",
    explanation: "Python remains dynamically typed at runtime — type hints are not enforced by the interpreter, but tools like mypy or pyright use them to catch bugs before execution. The typing module provides constructs like Optional, Union, List, Dict, Callable, and generics via TypeVar for more expressive annotations, though many built-in collections (list, dict) can now be used directly as generics since Python 3.9. Optional[X] is shorthand for Union[X, None].",
    code: 'from typing import Optional, Union, Callable, TypeVar, Generic\n\ndef greet(name: str, times: int = 1) -> str:\n    return (f"Hello, {name}! " * times).strip()\n\ndef find_user(user_id: int) -> Optional[dict]:\n    users = {1: {"name": "Sam"}}\n    return users.get(user_id)\n\ndef apply(fn: Callable[[int, int], int], a: int, b: int) -> int:\n    return fn(a, b)\n\n# Modern generics (3.9+) without importing List/Dict\ndef total(values: list[int]) -> int:\n    return sum(values)\n\nT = TypeVar("T")\nclass Box(Generic[T]):\n    def __init__(self, item: T) -> None:\n        self.item = item\n\nbox: Box[int] = Box(42)',
    interviewQuestion: "Are Python type hints enforced at runtime? What tools actually use them, and what does Optional[X] mean?",
  },
  {
    id: "python-dunder-methods",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Dunder Methods (__repr__, __eq__, __hash__)",
    summary: "Dunder (double-underscore) methods let custom classes integrate with built-in operators, functions, and protocols.",
    explanation: "__repr__ should return an unambiguous, ideally eval-able string for debugging, while __str__ is for a user-friendly display and falls back to __repr__ if undefined. __eq__ defines equality comparison, and if you override it you should also define __hash__ (or explicitly set it to None) because Python disables the default identity-based hash when __eq__ is customized, which would otherwise break usage in sets/dicts. Other common dunders include __lt__ for ordering, __len__, and __iter__.",
    code: 'class Money:\n    def __init__(self, amount, currency="USD"):\n        self.amount = amount\n        self.currency = currency\n\n    def __repr__(self):\n        return f"Money({self.amount!r}, {self.currency!r})"\n\n    def __eq__(self, other):\n        if not isinstance(other, Money):\n            return NotImplemented\n        return self.amount == other.amount and self.currency == other.currency\n\n    def __hash__(self):\n        return hash((self.amount, self.currency))\n\n    def __lt__(self, other):\n        return self.amount < other.amount\n\nm1 = Money(10)\nm2 = Money(10)\nprint(m1 == m2)      # True\nprint(m1)            # Money(10, \'USD\')\nprint({m1, m2})       # single item -- equal & same hash\nprint(sorted([Money(30), Money(5)]))',
    interviewQuestion: "Why must you define __hash__ when you override __eq__, and what breaks if you forget?",
  },
  {
    id: "python-staticmethod-vs-classmethod",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Static Methods vs Class Methods",
    summary: "@staticmethod defines a method with no implicit first argument, while @classmethod receives the class itself (cls) instead of an instance.",
    explanation: "A regular instance method's first parameter is 'self', bound to the instance. A classmethod's first parameter is 'cls', bound to the class, making it useful for alternative constructors that need to create instances of the (possibly subclassed) class. A staticmethod takes neither self nor cls implicitly — it's just a plain function namespaced inside the class for organizational purposes, with no access to instance or class state.",
    code: 'class Pizza:\n    def __init__(self, toppings):\n        self.toppings = toppings\n\n    @classmethod\n    def margherita(cls):\n        return cls(["mozzarella", "tomato"])  # cls respects subclasses\n\n    @classmethod\n    def pepperoni(cls):\n        return cls(["mozzarella", "pepperoni"])\n\n    @staticmethod\n    def is_valid_topping(topping):\n        return topping in {"mozzarella", "tomato", "pepperoni", "basil"}\n\n    def __repr__(self):\n        return f"Pizza({self.toppings})"\n\nclass StuffedCrustPizza(Pizza):\n    pass\n\np1 = Pizza.margherita()\np2 = StuffedCrustPizza.margherita()  # cls -> StuffedCrustPizza\nprint(type(p2))\nprint(Pizza.is_valid_topping("basil"))',
    interviewQuestion: "Why would you use a classmethod instead of a staticmethod for an alternative constructor, especially with subclassing?",
  },
  {
    id: "python-abstract-base-classes",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Abstract Base Classes",
    summary: "Abstract Base Classes (ABCs) define a required interface that subclasses must implement, enforced at instantiation time.",
    explanation: "Using the abc module, you inherit from ABC and mark methods with @abstractmethod; attempting to instantiate a subclass that hasn't implemented all abstract methods raises a TypeError. ABCs are useful for enforcing consistent interfaces across plugin-style architectures or polymorphic hierarchies. Python's collections.abc module also provides ABCs like Iterable and Sequence that let you check structural compatibility with isinstance().",
    code: 'from abc import ABC, abstractmethod\n\nclass Shape(ABC):\n    @abstractmethod\n    def area(self) -> float:\n        ...\n\n    @abstractmethod\n    def perimeter(self) -> float:\n        ...\n\nclass Rectangle(Shape):\n    def __init__(self, w, h):\n        self.w, self.h = w, h\n\n    def area(self):\n        return self.w * self.h\n\n    def perimeter(self):\n        return 2 * (self.w + self.h)\n\ntry:\n    s = Shape()  # cannot instantiate abstract class\nexcept TypeError as e:\n    print("Error:", e)\n\nr = Rectangle(3, 4)\nprint(r.area(), r.perimeter())',
    interviewQuestion: "What happens if you try to instantiate a class that inherits from an ABC but hasn't implemented all its abstract methods?",
  },
  {
    id: "python-multiple-inheritance",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Multiple Inheritance",
    summary: "Python allows a class to inherit from more than one parent class, combining their attributes and methods.",
    explanation: "Multiple inheritance is powerful for mixin patterns, where small reusable classes add specific behavior (like logging or serialization) to a primary class hierarchy. Conflicts between parent classes that define the same method name are resolved by the Method Resolution Order (MRO), computed via the C3 linearization algorithm. Overuse of multiple inheritance can lead to fragile, hard-to-reason-about hierarchies, so mixins are generally kept small and focused.",
    code: 'class SerializableMixin:\n    def to_dict(self):\n        return self.__dict__\n\nclass LoggingMixin:\n    def log(self, msg):\n        print(f"[{self.__class__.__name__}] {msg}")\n\nclass User(SerializableMixin, LoggingMixin):\n    def __init__(self, name):\n        self.name = name\n\nu = User("Sam")\nu.log("created")\nprint(u.to_dict())\n\n# Diamond inheritance\nclass A:\n    def greet(self):\n        return "A"\n\nclass B(A):\n    def greet(self):\n        return "B->" + super().greet()\n\nclass C(A):\n    def greet(self):\n        return "C->" + super().greet()\n\nclass D(B, C):\n    pass\n\nprint(D().greet())  # B->C->A, following the MRO',
    interviewQuestion: "What is a mixin class, and how does Python resolve method conflicts in a diamond inheritance scenario?",
  },
  {
    id: "python-method-resolution-order",
    category: "python",
    difficulty: "Tricky",
    topic: "Advanced",
    title: "Method Resolution Order (MRO)",
    summary: "MRO defines the specific, deterministic order Python searches parent classes for attributes and methods in multiple inheritance.",
    explanation: "Python uses the C3 linearization algorithm to compute the MRO, which guarantees a consistent order that respects both local precedence (a class before its parents) and the order parents were listed. You can inspect it directly via ClassName.__mro__ or ClassName.mro(). super() doesn't simply call 'the parent class' — it calls the next class in the MRO chain, which is why cooperative multiple inheritance with super() in every class produces predictable diamond-resolution behavior.",
    code: 'class A:\n    def who(self):\n        print("A")\n\nclass B(A):\n    def who(self):\n        print("B")\n        super().who()\n\nclass C(A):\n    def who(self):\n        print("C")\n        super().who()\n\nclass D(B, C):\n    def who(self):\n        print("D")\n        super().who()\n\nprint([cls.__name__ for cls in D.__mro__])\n# [\'D\', \'B\', \'C\', \'A\', \'object\']\n\nD().who()\n# D\n# B\n# C\n# A  -- each called exactly once, not A twice\n\n# Attempting an inconsistent MRO raises TypeError\ntry:\n    class X(A, B):\n        pass\nexcept TypeError as e:\n    print("MRO Error:", e)',
    interviewQuestion: "In a diamond inheritance hierarchy, why does the common base class's __init__ only run once when every class uses super(), and what determines the call order?",
  },
  {
    id: "python-memory-management",
    category: "python",
    difficulty: "Tricky",
    topic: "Advanced",
    title: "Memory Management in Python",
    summary: "CPython manages memory primarily through reference counting, augmented by a cyclic garbage collector for reference cycles.",
    explanation: "Every object tracks how many references point to it; when the count hits zero, CPython immediately deallocates it. This alone can't reclaim reference cycles (e.g., two objects referencing each other), which is why a separate generational garbage collector periodically scans for and cleans up unreachable cycles. Small integers (-5 to 256) and interned strings are cached and reused by CPython as an optimization, which is why 'is' comparisons on them can misleadingly return True — this is an implementation detail, not a language guarantee.",
    code: 'import sys\n\na = [1, 2, 3]\nb = a\nprint(sys.getrefcount(a))  # includes temp ref from getrefcount call itself\n\n# Reference cycle -- not cleaned by refcounting alone\nclass Node:\n    def __init__(self):\n        self.other = None\n\nn1, n2 = Node(), Node()\nn1.other = n2\nn2.other = n1  # cycle: n1 <-> n2\ndel n1, n2      # refcounts don\'t hit zero due to the cycle\n\nimport gc\ncollected = gc.collect()  # cyclic GC finds and frees them\nprint("Objects collected:", collected)\n\n# Small int caching (CPython implementation detail)\nx = 256\ny = 256\nprint(x is y)  # True -- cached\nx2 = 257\ny2 = 257\nprint(x2 is y2)  # often False -- not guaranteed cached',
    interviewQuestion: "Why can't reference counting alone reclaim all garbage in Python, and what role does the generational garbage collector play?",
  },
  {
    id: "python-garbage-collection",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Garbage Collection",
    summary: "Python's gc module exposes the generational cyclic garbage collector that finds and frees objects involved in reference cycles.",
    explanation: "The garbage collector organizes objects into three generations; new objects start in generation 0, and objects that survive a collection are promoted to older generations, which are scanned less frequently since long-lived objects are statistically less likely to become garbage. You can manually trigger collection with gc.collect(), temporarily disable it with gc.disable() for performance-critical sections, and define __del__ on objects for cleanup, though relying on __del__ timing is discouraged since it isn't deterministic for cyclic garbage.",
    code: 'import gc\n\nprint(gc.isenabled())          # True by default\nprint(gc.get_threshold())      # e.g. (700, 10, 10) -- gen0, gen1, gen2\n\nclass Tracked:\n    def __del__(self):\n        print("Tracked object destroyed")\n\nclass Cycle:\n    def __init__(self):\n        self.ref = None\n\na = Cycle()\nb = Cycle()\na.ref, b.ref = b, a\ndel a, b\n\nunreachable = gc.collect()\nprint(f"Collected {unreachable} unreachable objects")\n\n# Disable GC temporarily around a hot loop (advanced perf tuning)\ngc.disable()\ntry:\n    total = sum(range(1_000_000))\nfinally:\n    gc.enable()',
    interviewQuestion: "What are generations in Python's garbage collector, and why does promoting long-lived objects to older generations improve performance?",
  },
  {
    id: "python-copy-vs-deepcopy",
    category: "python",
    difficulty: "Tricky",
    topic: "Advanced",
    title: "Copy vs Deep Copy",
    summary: "copy.copy() creates a shallow copy that shares nested objects with the original, while copy.deepcopy() recursively copies everything.",
    explanation: "A shallow copy duplicates the outer container but its elements still reference the same nested objects as the original, so mutating a nested list inside a shallow copy also affects the original. A deep copy recursively clones every nested object, producing a fully independent structure, at the cost of more time and memory. Simple assignment (b = a) doesn't copy at all — it just creates a second reference to the same object, which is the most common source of confusion here.",
    code: 'import copy\n\noriginal = {"name": "Sam", "scores": [90, 85]}\n\n# Assignment: same object, no copy\nalias = original\nalias["name"] = "Changed"\nprint(original["name"])  # "Changed" too!\n\n# Shallow copy: new outer dict, shared inner list\nshallow = copy.copy(original)\nshallow["scores"].append(100)\nprint(original["scores"])  # [90, 85, 100] -- affected!\n\n# Deep copy: fully independent\ndeep = copy.deepcopy(original)\ndeep["scores"].append(999)\nprint(original["scores"])  # unaffected by deep copy changes\nprint(deep["scores"])',
    interviewQuestion: "If you shallow-copy a dictionary containing a nested list and then mutate that list through the copy, does the original change? Why?",
  },
  {
    id: "python-regex",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Regex with the re Module",
    summary: "The re module provides regular expression matching for pattern-based searching, extraction, and substitution in strings.",
    explanation: "Key functions include re.match() (anchored at the string start), re.search() (finds the first match anywhere), re.findall() (returns all matches), and re.sub() (replaces matches). Compiling a pattern with re.compile() is more efficient when reusing the same pattern repeatedly. Named groups via (?P<name>...) make extracted values more readable than relying on positional group indices.",
    code: 'import re\n\ntext = "Contact: sam@example.com or bo@test.org"\n\n# findall returns all matches\nemails = re.findall(r"[\\w.+-]+@[\\w-]+\\.[\\w.-]+", text)\nprint(emails)\n\n# search finds the first match anywhere\nmatch = re.search(r"(?P<user>[\\w.+-]+)@(?P<domain>[\\w.-]+)", text)\nif match:\n    print(match.group("user"), match.group("domain"))\n\n# sub replaces matches\nmasked = re.sub(r"[\\w.+-]+@[\\w-]+\\.[\\w.-]+", "[hidden]", text)\nprint(masked)\n\n# Compiled pattern for repeated use\nphone_pattern = re.compile(r"\\d{3}-\\d{3}-\\d{4}")\nprint(bool(phone_pattern.match("555-123-4567")))',
    interviewQuestion: "What is the difference between re.match() and re.search(), and why would you use re.compile() for a pattern used many times?",
  },
  {
    id: "python-json-handling",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "JSON Handling",
    summary: "The json module serializes Python objects to JSON text (dumps/dump) and parses JSON text back into Python objects (loads/load).",
    explanation: "json.dumps() converts a Python object to a JSON string while json.dump() writes directly to a file-like object; loads()/load() do the reverse. Not all Python types are JSON-serializable by default — custom objects, datetimes, and sets require a custom default function or a custom JSONEncoder subclass to convert them into serializable types. Common parameters like indent (pretty-printing) and sort_keys improve readability of output.",
    code: 'import json\nfrom datetime import datetime\n\ndata = {"name": "Sam", "active": True, "scores": [90, 85, 77]}\n\njson_str = json.dumps(data, indent=2, sort_keys=True)\nprint(json_str)\n\nparsed = json.loads(json_str)\nprint(parsed["scores"])\n\n# Custom serialization for non-JSON-native types\ndef default_serializer(obj):\n    if isinstance(obj, datetime):\n        return obj.isoformat()\n    raise TypeError(f"Type {type(obj)} not serializable")\n\nevent = {"name": "Launch", "when": datetime(2026, 1, 1)}\nprint(json.dumps(event, default=default_serializer))\n\nwith open("data.json", "w") as f:\n    json.dump(data, f, indent=2)',
    interviewQuestion: "How do you serialize a Python object containing a datetime field to JSON, given that datetime isn't natively JSON-serializable?",
  },
  {
    id: "python-collections-module",
    category: "python",
    difficulty: "Advanced",
    topic: "Advanced",
    title: "Collections Module (Counter, defaultdict, namedtuple, deque)",
    summary: "The collections module provides specialized, high-performance container datatypes beyond the built-in list/dict/set/tuple.",
    explanation: "Counter tallies hashable items and supports most_common() for frequency ranking; defaultdict auto-initializes missing keys with a factory function, eliminating manual existence checks; namedtuple creates lightweight, immutable classes with named fields accessible by attribute; and deque is a double-ended queue with O(1) appends/pops from both ends, ideal for queues and sliding windows unlike a list's O(n) pop(0).",
    code: 'from collections import Counter, defaultdict, namedtuple, deque\n\n# Counter\nwords = "the quick brown fox the lazy dog the fox".split()\ncounts = Counter(words)\nprint(counts.most_common(2))  # [(\'the\', 3), (\'fox\', 2)]\n\n# defaultdict avoids manual key checks\ngroups = defaultdict(list)\nfor word in words:\n    groups[len(word)].append(word)\nprint(dict(groups))\n\n# namedtuple: lightweight immutable record\nPoint = namedtuple("Point", ["x", "y"])\np = Point(1, 2)\nprint(p.x, p.y)\n\n# deque: fast appends/pops from both ends\ndq = deque([1, 2, 3])\ndq.appendleft(0)\ndq.append(4)\ndq.popleft()\nprint(dq)  # deque([1, 2, 3, 4])',
    interviewQuestion: "Why is collections.deque preferred over a list for queue-like operations, and what problem does defaultdict solve?",
  },
  {
    id: "python-datetime-module",
    category: "python",
    difficulty: "Intermediate",
    topic: "Advanced",
    title: "Datetime Module",
    summary: "The datetime module provides classes for manipulating dates and times, including parsing, formatting, and arithmetic via timedelta.",
    explanation: "datetime.now() returns the current naive local datetime, while timezone-aware datetimes (via the timezone class or zoneinfo module) properly represent moments in time across regions and are strongly recommended for anything involving scheduling or comparisons across timezones. strftime() formats a datetime into a string, and strptime() parses a string into a datetime using the same format codes. timedelta objects represent durations and support arithmetic directly on datetime objects.",
    code: 'from datetime import datetime, timedelta, timezone\n\nnow = datetime.now()\nprint(now.strftime("%Y-%m-%d %H:%M:%S"))\n\n# Parsing a string into a datetime\nparsed = datetime.strptime("2026-01-15", "%Y-%m-%d")\nprint(parsed.year, parsed.month, parsed.day)\n\n# Arithmetic with timedelta\nnext_week = now + timedelta(days=7)\nprint(next_week.strftime("%Y-%m-%d"))\n\ndiff = next_week - now\nprint(diff.days)  # 7\n\n# Timezone-aware datetime (recommended over naive)\nutc_now = datetime.now(timezone.utc)\nprint(utc_now.isoformat())',
    interviewQuestion: "What is the difference between a naive and a timezone-aware datetime object, and why does it matter for comparisons across regions?",
  },
];
