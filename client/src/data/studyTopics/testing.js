export default [
  {
    id: 'testing-unit-testing-fundamentals',
    category: 'testing',
    difficulty: 'Basic',
    topic: 'Unit Testing Fundamentals',
    title: 'What is a unit test?',
    summary: 'A unit test verifies a single, isolated piece of logic such as a function or class method.',
    explanation: 'Unit tests focus on the smallest testable parts of an application in isolation from dependencies like databases, network calls, or the file system. They run fast, are deterministic, and pinpoint failures precisely because each test targets one behavior. External dependencies are typically replaced with test doubles so the test only exercises the unit under test. Good unit tests follow the Arrange-Act-Assert pattern for clarity.',
    code: "function add(a, b) {\n  return a + b;\n}\n\ntest('add sums two numbers', () => {\n  // Arrange\n  const a = 2, b = 3;\n  // Act\n  const result = add(a, b);\n  // Assert\n  expect(result).toBe(5);\n});",
    interviewQuestion: 'What makes a test a "unit" test as opposed to an integration test?',
  },
  {
    id: 'testing-integration-testing',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Integration Testing',
    title: 'What does integration testing verify?',
    summary: 'Integration tests check that multiple units or modules work correctly together.',
    explanation: 'Integration tests exercise the interaction between components, such as a service talking to a real or in-memory database, or two modules communicating through an API. They catch issues that unit tests miss, like incorrect wiring, serialization mismatches, or configuration problems. They are slower and more complex to set up than unit tests because they involve more of the real system, often including a test database or containerized dependencies. A healthy suite balances integration tests against faster unit tests.',
    code: "const request = require('supertest');\nconst app = require('../app');\n\ntest('POST /users creates a user in the database', async () => {\n  const res = await request(app)\n    .post('/users')\n    .send({ name: 'Sneha' });\n\n  expect(res.status).toBe(201);\n  const saved = await db.users.findById(res.body.id);\n  expect(saved.name).toBe('Sneha');\n});",
    interviewQuestion: 'How do integration tests differ from unit tests, and when would you choose one over the other?',
  },
  {
    id: 'testing-end-to-end-testing',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'End-to-End Testing',
    title: 'What is end-to-end (E2E) testing?',
    summary: 'E2E tests simulate real user flows through the entire application stack, from UI to backend to database.',
    explanation: 'End-to-end tests validate that a complete user journey works as expected, such as logging in, adding an item to a cart, and checking out. They run against a fully deployed or near-production environment and use browser automation tools like Cypress or Playwright. Because they touch every layer, they are the slowest and most brittle tests, so they should cover critical paths only rather than every edge case. E2E failures can be harder to debug since the root cause could be anywhere in the stack.',
    code: "describe('checkout flow', () => {\n  it('lets a user purchase an item', () => {\n    cy.visit('/products/42');\n    cy.get('[data-testid=add-to-cart]').click();\n    cy.get('[data-testid=cart-icon]').click();\n    cy.get('[data-testid=checkout-btn]').click();\n    cy.get('[data-testid=order-confirmation]')\n      .should('contain', 'Thank you for your order');\n  });\n});",
    interviewQuestion: 'Why should E2E tests be a small fraction of your overall test suite?',
  },
  {
    id: 'testing-test-pyramid',
    category: 'testing',
    difficulty: 'Basic',
    topic: 'Test Pyramid',
    title: 'What is the Test Pyramid?',
    summary: 'The Test Pyramid is a strategy for balancing test types: many fast unit tests, fewer integration tests, and even fewer slow E2E tests.',
    explanation: 'The pyramid shape represents both the quantity and speed tradeoff of each test layer. Unit tests form the wide base because they are cheap, fast, and give precise failure locations. Integration tests form the middle layer, verifying collaboration between components. E2E tests sit at the narrow top since they are expensive, slow, and flaky, so teams keep only enough to cover critical business flows. Violating this shape, for example by writing mostly E2E tests, leads to slow, fragile suites.',
    code: "// Conceptual test counts in a healthy suite\nconst suite = {\n  unit: 500,        // fast, isolated, cheap\n  integration: 80,   // module interactions\n  e2e: 15,            // critical user journeys\n};\n\n// Ratio roughly follows: unit > integration > e2e",
    interviewQuestion: 'Why do teams aim for more unit tests than E2E tests according to the Test Pyramid?',
  },
  {
    id: 'testing-tdd',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'TDD (Test-Driven Development)',
    title: 'What is the Red-Green-Refactor cycle in TDD?',
    summary: 'TDD is a workflow where you write a failing test first, make it pass with minimal code, then refactor.',
    explanation: 'In the Red phase you write a test for behavior that does not exist yet, and it fails. In the Green phase you write the simplest code possible to make the test pass, without worrying about elegance. In the Refactor phase you clean up the implementation while keeping tests green, ensuring no regressions. This cycle encourages small, incremental changes, drives better design through testable code, and produces a regression suite as a natural byproduct of development.',
    code: "// Red: write a failing test\ntest('formats currency', () => {\n  expect(formatCurrency(1000)).toBe('$1,000.00');\n});\n\n// Green: minimal implementation\nfunction formatCurrency(amount) {\n  return '$' + amount.toFixed(2).replace(/\\B(?=(\\d{3})+(?!\\d))/g, ',');\n}\n\n// Refactor: extract logic, keep test passing\n// (implementation improved without changing behavior)",
    interviewQuestion: 'Walk me through the Red-Green-Refactor cycle and what each step is meant to accomplish.',
  },
  {
    id: 'testing-bdd',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'BDD (Behavior-Driven Development)',
    title: 'How does BDD differ from TDD?',
    summary: 'BDD extends TDD by describing behavior in plain, business-readable language using Given-When-Then scenarios.',
    explanation: 'BDD focuses on collaboration between developers, QA, and non-technical stakeholders by expressing requirements as executable specifications. Tools like Cucumber or Jest with a BDD-style syntax let you write scenarios in Gherkin: Given a starting context, When an action occurs, Then an outcome is expected. This shared vocabulary reduces ambiguity in requirements and creates living documentation that stays in sync with the code because it is directly tied to test execution. TDD is more developer-focused on unit-level design, while BDD emphasizes system behavior from a user perspective.',
    code: "Feature: User login\n  Scenario: Successful login with valid credentials\n    Given a registered user with email 'sneha@surfai.in'\n    When the user submits the correct password\n    Then the user should be redirected to the dashboard\n    And a welcome message should be displayed",
    interviewQuestion: 'What problem does BDD solve that plain TDD does not, especially for cross-functional teams?',
  },
  {
    id: 'testing-mocking-stubbing',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Mocking & Stubbing',
    title: 'What is the difference between mocking and stubbing?',
    summary: 'Stubbing returns canned responses for method calls, while mocking additionally verifies that specific interactions occurred.',
    explanation: 'A stub is a test double that provides predetermined answers to calls made during the test, used to control indirect inputs without caring how it was called. A mock goes further by setting expectations on how it should be called, such as verifying a function was invoked exactly once with certain arguments, and the test fails if those expectations are not met. Stubs are about state verification, mocks are about behavior verification. Overusing mocks can make tests brittle and tightly coupled to implementation details.',
    code: "test('sends a welcome email on signup', () => {\n  const emailService = { send: jest.fn() };\n\n  signupUser({ email: 'sneha@surfai.in' }, emailService);\n\n  // Mock: verify the interaction happened\n  expect(emailService.send).toHaveBeenCalledWith(\n    'sneha@surfai.in',\n    'Welcome!'\n  );\n});",
    interviewQuestion: 'When would you choose a stub over a mock, and why does that distinction matter for test maintainability?',
  },
  {
    id: 'testing-test-doubles',
    category: 'testing',
    difficulty: 'Advanced',
    topic: 'Test Doubles (Spy/Fake/Mock)',
    title: 'What are the different kinds of test doubles?',
    summary: 'Test doubles include dummies, stubs, spies, mocks, and fakes, each serving a distinct purpose in isolating code under test.',
    explanation: 'A dummy is passed but never actually used, just to satisfy a parameter list. A stub returns fixed data for calls made during the test. A spy wraps a real function and records how it was called while still allowing the original behavior to run, useful for verifying calls without fully replacing logic. A mock is pre-programmed with expectations and fails the test if those expectations are not met. A fake is a working but simplified implementation, such as an in-memory database replacing a real one. Choosing the right double depends on whether you need to verify state, behavior, or just isolate a slow dependency.',
    code: "// Spy: wraps real implementation, tracks calls\nconst calculateTotal = jest.spyOn(cart, 'calculateTotal');\ncart.checkout();\nexpect(calculateTotal).toHaveBeenCalled();\n\n// Fake: simplified working implementation\nclass FakeUserRepository {\n  constructor() { this.users = new Map(); }\n  save(user) { this.users.set(user.id, user); }\n  findById(id) { return this.users.get(id); }\n}",
    interviewQuestion: 'Explain the difference between a fake and a mock, and give an example of when a fake is preferable.',
  },
  {
    id: 'testing-jest-fundamentals',
    category: 'testing',
    difficulty: 'Basic',
    topic: 'Jest Fundamentals',
    title: 'What are the core building blocks of a Jest test file?',
    summary: 'Jest tests use describe blocks to group related tests, test/it for individual cases, and expect for assertions.',
    explanation: 'Jest is a JavaScript testing framework that bundles a test runner, assertion library, and mocking utilities. The describe function groups related tests for organization and shared setup, while test or its alias it defines an individual test case. The expect function wraps a value and chains matchers like toBe, toEqual, or toHaveBeenCalled to make assertions. Lifecycle hooks such as beforeEach and afterEach let you set up and tear down shared state between tests, keeping each test isolated.',
    code: "describe('Calculator', () => {\n  let calc;\n\n  beforeEach(() => {\n    calc = new Calculator();\n  });\n\n  test('adds two numbers', () => {\n    expect(calc.add(2, 3)).toBe(5);\n  });\n\n  test('throws on division by zero', () => {\n    expect(() => calc.divide(1, 0)).toThrow('Division by zero');\n  });\n});",
    interviewQuestion: 'What is the purpose of beforeEach in Jest, and how does it help test isolation?',
  },
  {
    id: 'testing-react-testing-library-fundamentals',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'React Testing Library Fundamentals',
    title: 'What philosophy does React Testing Library follow?',
    summary: 'RTL encourages testing components the way a user interacts with them, querying by accessible roles and text rather than implementation details.',
    explanation: 'React Testing Library provides utilities to render components and query the resulting DOM using accessible queries like getByRole, getByLabelText, or getByText, discouraging reliance on internal component state or class names. Its guiding principle is "the more your tests resemble the way your software is used, the more confidence they give you." Interactions are simulated with the userEvent library to closely mimic real browser events like typing and clicking. This approach makes tests resilient to refactors since they do not break when internal implementation changes as long as the user-facing behavior stays the same.',
    code: "import { render, screen } from '@testing-library/react';\nimport userEvent from '@testing-library/user-event';\nimport LoginForm from './LoginForm';\n\ntest('shows an error when submitting empty form', async () => {\n  render(<LoginForm />);\n  const user = userEvent.setup();\n\n  await user.click(screen.getByRole('button', { name: /log in/i }));\n\n  expect(await screen.findByText(/email is required/i)).toBeInTheDocument();\n});",
    interviewQuestion: 'Why does React Testing Library discourage querying elements by CSS class or component internal state?',
  },
  {
    id: 'testing-cypress-fundamentals',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Cypress Fundamentals',
    title: 'How does Cypress differ architecturally from Selenium-based tools?',
    summary: 'Cypress runs directly inside the browser alongside the application, giving it native access to the DOM and automatic waiting.',
    explanation: 'Unlike Selenium, which communicates with the browser over a remote WebDriver protocol, Cypress executes in the same run loop as the application under test. This gives it direct access to the DOM, network layer, and the ability to automatically wait for elements and assertions to resolve without manual sleeps. Cypress commands are queued and retried automatically until they pass or time out, which reduces flakiness caused by timing issues. Its architecture, however, historically limited it to testing one origin at a time and one browser tab, though multi-origin support has since been added.',
    code: "describe('search', () => {\n  it('filters results as the user types', () => {\n    cy.visit('/search');\n    cy.get('[data-testid=search-input]').type('laptop');\n    cy.intercept('GET', '/api/search*').as('searchRequest');\n    cy.wait('@searchRequest');\n    cy.get('[data-testid=result-item]').should('have.length.greaterThan', 0);\n  });\n});",
    interviewQuestion: 'How does Cypress automatic retry-and-wait mechanism help reduce flaky tests compared to manual waits?',
  },
  {
    id: 'testing-playwright-fundamentals',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Playwright Fundamentals',
    title: 'What makes Playwright well suited for cross-browser E2E testing?',
    summary: 'Playwright automates Chromium, Firefox, and WebKit through a single API, with built-in auto-waiting and network interception.',
    explanation: 'Playwright was built by Microsoft to test modern web apps across multiple browser engines with one consistent API, unlike Cypress which historically favored Chromium-based browsers. It uses auto-waiting so actions like click or fill wait for elements to be actionable before proceeding, reducing flaky failures. Playwright supports parallel test execution across workers out of the box, browser contexts for isolated sessions without spinning up new browsers, and powerful tools like tracing and codegen for debugging. It also natively supports multiple tabs, iframes, and multi-origin navigation, which were historically harder in Cypress.',
    code: "import { test, expect } from '@playwright/test';\n\ntest('user can log in', async ({ page }) => {\n  await page.goto('/login');\n  await page.fill('#email', 'sneha@surfai.in');\n  await page.fill('#password', 'secret123');\n  await page.click('button[type=submit]');\n\n  await expect(page.getByText('Welcome back')).toBeVisible();\n});",
    interviewQuestion: 'How does the Playwright browser context model help with test isolation and parallelization?',
  },
  {
    id: 'testing-snapshot-testing',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Snapshot Testing',
    title: 'What is snapshot testing and when does it become risky?',
    summary: 'Snapshot testing captures a rendered output and compares future runs against the saved version to detect unintended changes.',
    explanation: 'Jest snapshot testing serializes a component or data structure to a file on first run, then on subsequent runs compares the current output to the stored snapshot and fails if they differ. It is useful for catching unexpected UI or output changes with minimal test-writing effort. The risk is that developers often blindly run "update snapshot" without reviewing the diff, turning the test into a rubber stamp rather than a meaningful check. Snapshots also tend to be large and coupled to implementation details, making them brittle across refactors, so they work best for small, stable, and meaningful outputs.',
    code: "import renderer from 'react-test-renderer';\nimport Badge from './Badge';\n\ntest('renders badge correctly', () => {\n  const tree = renderer.create(<Badge label='New' color='blue' />).toJSON();\n  expect(tree).toMatchSnapshot();\n});\n\n// __snapshots__/Badge.test.js.snap is generated and diffed on future runs",
    interviewQuestion: 'What are the pitfalls of relying heavily on snapshot testing, and how can teams mitigate them?',
  },
  {
    id: 'testing-code-coverage',
    category: 'testing',
    difficulty: 'Basic',
    topic: 'Code Coverage',
    title: 'What does code coverage actually measure?',
    summary: 'Code coverage reports what percentage of lines, branches, or functions were executed during a test run.',
    explanation: 'Coverage tools like Istanbul instrument code to track which statements, branches, functions, and lines execute while tests run, producing metrics such as line coverage and branch coverage. High coverage indicates code was executed, but it says nothing about whether meaningful assertions were made, so 100% coverage can still hide bugs. Coverage is best used as a signal to find untested areas rather than a target to chase for its own sake, since chasing a number can encourage low-value tests that execute code without verifying behavior. Branch coverage is generally a stronger signal than line coverage because it accounts for conditional logic paths.',
    code: "// jest.config.js\nmodule.exports = {\n  collectCoverage: true,\n  coverageThreshold: {\n    global: {\n      branches: 80,\n      functions: 80,\n      lines: 80,\n      statements: 80,\n    },\n  },\n};\n\n// Run: jest --coverage",
    interviewQuestion: 'Why can a codebase have 100% code coverage and still contain significant bugs?',
  },
  {
    id: 'testing-async-code',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Testing Async Code',
    title: 'How do you correctly test asynchronous code in Jest?',
    summary: 'Async tests must return or await the promise so the test runner waits for completion before making assertions.',
    explanation: 'A common bug is writing an async test that does not return or await the promise, causing Jest to mark the test as passed before the assertion even runs. The fix is to mark the test function async and await the async call, or return the promise directly, or use the resolves/rejects matchers. For code using callbacks instead of promises, Jest provides a done callback that must be called explicitly to signal completion. Forgetting these patterns is one of the most common sources of false-positive passing tests in async-heavy codebases.',
    code: "test('fetchUser resolves with user data', async () => {\n  const user = await fetchUser(1);\n  expect(user.name).toBe('Sneha');\n});\n\ntest('fetchUser rejects for invalid id', async () => {\n  await expect(fetchUser(-1)).rejects.toThrow('Invalid user id');\n});",
    interviewQuestion: 'What happens if you forget to await an async call inside a Jest test, and why is that dangerous?',
  },
  {
    id: 'testing-apis',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Testing APIs',
    title: 'What layers should an API test suite cover?',
    summary: 'API testing verifies request/response contracts, status codes, error handling, and edge cases at the HTTP boundary.',
    explanation: 'API tests typically use a tool like Supertest, Postman, or REST-assured to send real HTTP requests to an application and assert on status codes, response bodies, and headers. Good API test suites cover happy paths, validation errors, authentication and authorization failures, and edge cases like pagination boundaries or rate limiting. Tests should also verify idempotency for methods like PUT and DELETE, and confirm that error responses follow a consistent shape. Running these against a test database or in-memory server keeps them fast while still exercising real routing and middleware.',
    code: "const request = require('supertest');\nconst app = require('../app');\n\ndescribe('GET /api/products/:id', () => {\n  it('returns 404 for a nonexistent product', async () => {\n    const res = await request(app).get('/api/products/99999');\n    expect(res.status).toBe(404);\n    expect(res.body.error).toBe('Product not found');\n  });\n\n  it('returns 200 with product details', async () => {\n    const res = await request(app).get('/api/products/1');\n    expect(res.status).toBe(200);\n    expect(res.body).toHaveProperty('name');\n  });\n});",
    interviewQuestion: 'What categories of scenarios should you include when designing an API test suite beyond the happy path?',
  },
  {
    id: 'testing-contract-testing',
    category: 'testing',
    difficulty: 'Advanced',
    topic: 'Contract Testing',
    title: 'What problem does contract testing solve in microservices?',
    summary: 'Contract testing verifies that a consumer and provider service agree on the shape of their API without requiring full end-to-end integration tests.',
    explanation: "In a microservices architecture, spinning up every dependent service for integration tests is slow and fragile. Contract testing, using tools like Pact, has the consumer define expectations of a provider's API as a contract, which the provider then verifies independently against its real implementation. This catches breaking changes early, in CI, without needing all services running simultaneously. It shifts the trust boundary from 'did we integrate correctly in a full environment' to 'does each side honor an agreed contract,' which scales much better across many services and teams.",
    code: "// consumer-side pact test\nconst { pactWith } = require('jest-pact');\n\npactWith({ consumer: 'WebApp', provider: 'UserService' }, (provider) => {\n  it('gets a user by id', async () => {\n    await provider.addInteraction({\n      state: 'user 1 exists',\n      uponReceiving: 'a request for user 1',\n      withRequest: { method: 'GET', path: '/users/1' },\n      willRespondWith: { status: 200, body: { id: 1, name: 'Sneha' } },\n    });\n\n    const user = await getUser(provider.mockService.baseUrl, 1);\n    expect(user.name).toBe('Sneha');\n  });\n});",
    interviewQuestion: 'How does consumer-driven contract testing reduce the need for full end-to-end integration environments?',
  },
  {
    id: 'testing-visual-regression-testing',
    category: 'testing',
    difficulty: 'Advanced',
    topic: 'Visual Regression Testing',
    title: 'How does visual regression testing catch UI bugs that functional tests miss?',
    summary: 'Visual regression testing takes screenshots of UI states and compares pixel differences against a baseline to catch unintended styling changes.',
    explanation: "Tools like Percy, Chromatic, or Playwright's screenshot comparisons render components or pages, capture an image, and diff it against an approved baseline image. This catches issues that DOM-based assertions cannot, such as broken CSS, overlapping elements, incorrect colors, or font rendering issues, none of which necessarily change the DOM structure. Because rendering can vary slightly across operating systems, browsers, and font rendering engines, these tools usually run in a controlled, consistent environment and apply a pixel-difference threshold to avoid false positives from anti-aliasing noise. Reviewers approve intentional visual changes to update the baseline.",
    code: "import { test, expect } from '@playwright/test';\n\ntest('homepage visual snapshot', async ({ page }) => {\n  await page.goto('/');\n  await expect(page).toHaveScreenshot('homepage.png', {\n    maxDiffPixelRatio: 0.02,\n  });\n});",
    interviewQuestion: 'What kinds of bugs does visual regression testing catch that functional or snapshot-of-DOM tests would miss?',
  },
  {
    id: 'testing-mutation-testing',
    category: 'testing',
    difficulty: 'Advanced',
    topic: 'Mutation Testing',
    title: 'What is mutation testing and what does a "killed mutant" mean?',
    summary: 'Mutation testing intentionally introduces small bugs into code and checks whether the test suite catches them.',
    explanation: 'A mutation testing tool like Stryker automatically generates "mutants" by making small changes to the source code, such as flipping a comparison operator or changing a boolean literal. It then reruns the test suite against each mutant. If a test fails, the mutant is "killed," meaning the suite successfully detected the injected bug. If all tests still pass, the mutant "survives," revealing a gap where the suite lacks meaningful assertions even if code coverage looks high. Mutation testing is more rigorous than code coverage because it measures assertion quality, not just execution, though it is computationally expensive to run on large codebases.',
    code: "// Original code\nfunction isAdult(age) {\n  return age >= 18;\n}\n\n// Stryker generates mutants like:\nfunction isAdult(age) {\n  return age > 18; // mutated operator\n}\n\n// If no test distinguishes age === 18, this mutant survives,\n// revealing a missing boundary test case.",
    interviewQuestion: 'Why is mutation testing considered a stronger quality signal than code coverage percentage?',
  },
  {
    id: 'testing-ice-cream-cone-anti-pattern',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Ice Cream Cone Anti-Pattern',
    title: 'What is the Ice Cream Cone anti-pattern in testing?',
    summary: 'The Ice Cream Cone is an inverted Test Pyramid where teams rely heavily on slow manual and E2E tests with very few unit tests.',
    explanation: 'This anti-pattern happens when organizations under-invest in unit testing and instead push most verification into manual QA and brittle end-to-end suites, forming a shape that is wide at the top and narrow at the bottom, resembling an ice cream cone. The result is slow feedback loops, flaky CI pipelines, expensive test maintenance, and difficulty pinpointing failure causes since a failing E2E test could stem from any layer of the stack. Fixing it usually requires investing in fast unit and integration tests, and reserving E2E and manual testing for a small set of critical, high-value user journeys.',
    code: "// Anti-pattern: too many slow, high-level tests\nconst suite = {\n  unit: 20,          // too few\n  integration: 30,\n  e2e: 150,           // way too many, slow and flaky\n  manualQA: 'extensive',\n};\n\n// Symptom: CI takes hours, failures are hard to trace to root cause",
    interviewQuestion: 'What symptoms in a CI pipeline suggest a team has fallen into the Ice Cream Cone anti-pattern?',
  },
  {
    id: 'testing-ci-test-automation',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'CI Test Automation',
    title: 'What role does CI play in a test automation strategy?',
    summary: 'CI pipelines automatically run the test suite on every push or pull request, giving fast feedback before code merges.',
    explanation: 'Continuous Integration systems like GitHub Actions, CircleCI, or Jenkins trigger test runs automatically whenever code changes, blocking merges if tests fail. A well-designed pipeline runs fast unit tests first to fail fast, then integration tests, then a smaller set of E2E tests, often in parallel across multiple workers to reduce total wall-clock time. Test results, coverage reports, and artifacts like screenshots or videos from failed E2E runs should be surfaced directly in the CI interface for quick debugging. Caching dependencies and splitting test suites across parallel jobs are common techniques to keep pipeline duration manageable as the suite grows.',
    code: "# .github/workflows/test.yml\nname: Test\non: [pull_request]\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: 20, cache: 'npm' }\n      - run: npm ci\n      - run: npm run test:unit -- --coverage\n      - run: npm run test:integration\n      - run: npm run test:e2e",
    interviewQuestion: 'How would you structure a CI pipeline to keep feedback fast as a test suite grows into the thousands of tests?',
  },
  {
    id: 'testing-flaky-tests',
    category: 'testing',
    difficulty: 'Advanced',
    topic: 'Flaky Tests & How to Fix Them',
    title: 'What causes flaky tests and how do you diagnose them?',
    summary: 'Flaky tests pass and fail intermittently without code changes, usually due to timing issues, shared state, or environment dependencies.',
    explanation: 'Common causes of flakiness include race conditions from unawaited async operations, tests that depend on execution order or shared mutable state, reliance on real timers or network calls, hardcoded waits that are sometimes too short, and non-deterministic data like random values or current timestamps. Diagnosing flakiness involves running the suspect test in isolation and in a loop, checking for shared fixtures between tests, and reviewing for missing awaits or improperly mocked time. Fixes include using fake timers, ensuring proper test isolation with fresh state per test, replacing arbitrary sleeps with condition-based waits, and controlling randomness with seeded values.',
    code: "// Flaky: relies on real time and a fixed sleep\ntest('token expires after 1 second', async () => {\n  const token = createToken();\n  await new Promise((r) => setTimeout(r, 1000)); // slow and unreliable\n  expect(isExpired(token)).toBe(true);\n});\n\n// Fixed: use fake timers for determinism\ntest('token expires after 1 second', () => {\n  jest.useFakeTimers();\n  const token = createToken();\n  jest.advanceTimersByTime(1000);\n  expect(isExpired(token)).toBe(true);\n  jest.useRealTimers();\n});",
    interviewQuestion: 'A test fails intermittently in CI but always passes locally. What steps would you take to diagnose and fix it?',
  },
  {
    id: 'testing-test-data-management',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Test Data Management',
    title: 'What strategies exist for managing test data across a suite?',
    summary: 'Test data management covers how you create, isolate, and clean up data so tests are repeatable and independent of each other.',
    explanation: 'Common approaches include using factories or builders to generate realistic test data with sensible defaults that can be overridden per test, seeding a test database before a run, and wrapping each test in a database transaction that rolls back afterward for isolation. Shared fixtures should be avoided or used carefully since mutating shared data across tests causes order-dependent flakiness. For integration and E2E tests, using unique identifiers per test run, such as timestamps or UUIDs, prevents collisions when tests run in parallel. Test data should also avoid containing real production PII.',
    code: "// Factory pattern for consistent, overridable test data\nfunction buildUser(overrides = {}) {\n  return {\n    id: crypto.randomUUID(),\n    name: 'Test User',\n    email: `user-${Date.now()}@example.com`,\n    role: 'member',\n    ...overrides,\n  };\n}\n\ntest('admin can delete a post', () => {\n  const admin = buildUser({ role: 'admin' });\n  expect(canDeletePost(admin)).toBe(true);\n});",
    interviewQuestion: 'How would you prevent test data collisions when integration tests run in parallel against a shared database?',
  },
  {
    id: 'testing-property-based-testing',
    category: 'testing',
    difficulty: 'Advanced',
    topic: 'Property-Based Testing',
    title: 'How does property-based testing differ from example-based testing?',
    summary: 'Property-based testing generates many random inputs to verify a general property holds, rather than checking specific hand-picked examples.',
    explanation: 'Instead of writing individual assertions like "reverse of [1,2,3] is [3,2,1]," property-based testing with a library like fast-check or jsverify defines a property such as "reversing a list twice returns the original list" and lets the framework generate hundreds of random inputs to try to falsify it. When a failing case is found, most frameworks automatically shrink the input to the smallest example that still reproduces the failure, making debugging easier. This approach is especially good at uncovering edge cases developers would not think to test manually, such as empty arrays, negative numbers, or unicode strings.',
    code: "const fc = require('fast-check');\n\ntest('reversing twice returns the original array', () => {\n  fc.assert(\n    fc.property(fc.array(fc.integer()), (arr) => {\n      const result = reverse(reverse(arr));\n      expect(result).toEqual(arr);\n    })\n  );\n});",
    interviewQuestion: 'What kind of bugs is property-based testing especially good at catching that example-based tests tend to miss?',
  },
  {
    id: 'testing-load-testing-basics',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Load Testing Basics',
    title: 'What does load testing measure and what tools are commonly used?',
    summary: 'Load testing simulates many concurrent users or requests to measure how a system performs under expected and peak traffic.',
    explanation: 'Tools like k6, JMeter, or Gatling ramp up virtual users against an API or application and measure metrics such as response time percentiles, throughput, error rate, and resource utilization under load. The goal is to find performance bottlenecks, verify the system meets its service level objectives, and determine the breaking point before a real traffic spike does. Related test types include stress testing, which pushes past normal capacity to find the failure point, and soak testing, which runs sustained load over a long period to catch memory leaks or resource exhaustion. Load tests should run against production-like environments since results on underpowered infrastructure are misleading.',
    code: "import http from 'k6/http';\nimport { check, sleep } from 'k6';\n\nexport const options = {\n  vus: 100,\n  duration: '30s',\n};\n\nexport default function () {\n  const res = http.get('https://api.example.com/products');\n  check(res, { 'status is 200': (r) => r.status === 200 });\n  sleep(1);\n}",
    interviewQuestion: 'What is the difference between load testing, stress testing, and soak testing?',
  },
  {
    id: 'testing-accessibility-testing',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Accessibility Testing',
    title: 'How can accessibility testing be automated, and what are its limits?',
    summary: 'Automated accessibility testing tools like axe-core catch a subset of WCAG violations such as missing labels and poor color contrast, but manual review is still required.',
    explanation: 'Tools like jest-axe or the axe-core browser extension scan rendered DOM output for common accessibility violations, including missing alt text, insufficient color contrast, improper heading hierarchy, and missing form labels or ARIA attributes. These automated checks typically catch only around 30-50 percent of real-world accessibility issues since many problems, like whether an interaction is usable via keyboard-only navigation or whether a screen reader announces content in a logical order, require manual or assistive-technology testing. A complete accessibility strategy combines automated scans in CI to prevent regressions with periodic manual audits and testing with real screen readers.',
    code: "import { render } from '@testing-library/react';\nimport { axe, toHaveNoViolations } from 'jest-axe';\nimport SignupForm from './SignupForm';\n\nexpect.extend(toHaveNoViolations);\n\ntest('signup form has no accessibility violations', async () => {\n  const { container } = render(<SignupForm />);\n  const results = await axe(container);\n  expect(results).toHaveNoViolations();\n});",
    interviewQuestion: 'Why can a component pass every automated accessibility check and still be unusable for a screen reader user?',
  },
  {
    id: 'testing-cross-browser-testing',
    category: 'testing',
    difficulty: 'Intermediate',
    topic: 'Cross-Browser Testing',
    title: 'Why is cross-browser testing still necessary, and how is it typically automated?',
    summary: 'Cross-browser testing verifies that an application behaves and renders consistently across different browser engines and versions.',
    explanation: 'Despite standardization efforts, browsers like Chrome, Firefox, and Safari use different rendering engines and implement newer CSS or JavaScript APIs at different paces, which can cause layout shifts, unsupported features, or subtle behavioral differences. Tools like Playwright and Selenium Grid can run the same test suite against multiple browser engines, while cloud services like BrowserStack or Sauce Labs provide access to real devices and older browser versions that are hard to maintain locally. Teams typically prioritize their most-used browser and OS combinations based on real analytics data rather than testing every possible combination, since the matrix grows exponentially.',
    code: "// playwright.config.js\nmodule.exports = {\n  projects: [\n    { name: 'chromium', use: { browserName: 'chromium' } },\n    { name: 'firefox', use: { browserName: 'firefox' } },\n    { name: 'webkit', use: { browserName: 'webkit' } },\n  ],\n};\n\n// Same test suite now runs against all three engines",
    interviewQuestion: 'How would you prioritize which browser and device combinations to include in a cross-browser test matrix?',
  },
  {
    id: 'testing-test-naming-conventions',
    category: 'testing',
    difficulty: 'Basic',
    topic: 'Test Naming Conventions',
    title: 'What makes a good test name?',
    summary: 'A good test name describes the scenario, the action, and the expected outcome so failures are understandable without reading the test body.',
    explanation: 'Vague names like "test1" or "works correctly" provide no information when a test fails in a CI log. A common convention is "should [expected behavior] when [condition]" or the given-when-then style, which reads like a specification and doubles as documentation. Descriptive names make it faster to identify what broke without opening the file, and they make the test suite easier to navigate when searching for coverage of a specific behavior. Consistency across the codebase in naming style also makes it easier for new team members to write tests that fit the existing conventions.',
    code: "// Poor naming\ntest('test1', () => { /* ... */ });\n\n// Descriptive naming\ntest('should return 401 when auth token is missing', () => {\n  const res = makeRequest({ headers: {} });\n  expect(res.status).toBe(401);\n});\n\ntest('should apply a 10% discount when cart total exceeds $100', () => {\n  const total = calculateTotal({ subtotal: 150 });\n  expect(total).toBe(135);\n});",
    interviewQuestion: 'Why does a well-named test matter beyond just style preference, especially when a CI run fails?',
  },
  {
    id: 'testing-parameterized-tests',
    category: 'testing',
    difficulty: 'Basic',
    topic: 'Parameterized Tests',
    title: 'What are parameterized tests and why use them?',
    summary: 'Parameterized tests run the same test logic against multiple input/output pairs, reducing duplication for testing many edge cases.',
    explanation: "Instead of copy-pasting near-identical test blocks for each input variation, Jest's test.each and describe.each let you define a table of inputs and expected outputs that get run through the same assertion logic. This keeps test files concise, makes it easy to add a new edge case as a single row, and ensures consistent assertion logic across all cases. It is especially useful for testing pure functions with many boundary conditions, such as validation logic or mathematical calculations, where the shape of the test never changes, only the data.",
    code: "test.each([\n  [0, false],\n  [17, false],\n  [18, true],\n  [65, true],\n])('isAdult(%i) returns %s', (age, expected) => {\n  expect(isAdult(age)).toBe(expected);\n});",
    interviewQuestion: 'When would you reach for a parameterized test instead of writing separate test cases?',
  },
  {
    id: 'testing-testing-legacy-code',
    category: 'testing',
    difficulty: 'Tricky',
    topic: 'Testing Legacy Code',
    title: 'How do you start adding tests to an untested legacy codebase?',
    summary: 'Testing legacy code starts with characterization tests that capture existing behavior, then uses seams to isolate code before refactoring.',
    explanation: 'Legacy code, often defined as code without tests, is risky to change because there is no safety net to catch regressions. The recommended approach, popularized by Michael Feathers, is to first write characterization tests that document current behavior exactly as it is, even if that behavior seems wrong, to create a baseline before touching anything. Next, identify "seams," points where you can alter behavior without editing the code directly, such as extracting a dependency behind an interface, to break tight coupling and make the code testable in isolation. Refactoring should proceed in small, verifiable steps, running the characterization tests after each change, and prioritizing coverage on the highest-risk, most-changed areas of the code rather than trying to test everything at once.',
    code: "// Step 1: characterization test captures current behavior as-is\ntest('legacy calculateShipping matches current output for known input', () => {\n  // Not necessarily \"correct\", just documents existing behavior\n  expect(calculateShipping({ weight: 5, zone: 'B' })).toBe(12.5);\n});\n\n// Step 2: introduce a seam to isolate a hard dependency\nfunction calculateShipping(order, rateProvider = defaultRateProvider) {\n  const rate = rateProvider.getRate(order.zone);\n  return order.weight * rate;\n}\n// Now rateProvider can be faked in tests without touching global state",
    interviewQuestion: 'You inherit a 10,000-line module with zero tests that the business depends on. Walk me through your approach to safely adding test coverage.',
  },
];
