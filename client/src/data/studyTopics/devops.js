// 30 DevOps topics for Study Hub.
export default [
  {
    id: "devops-cicd-fundamentals",
    category: "devops",
    difficulty: "Basic",
    topic: "CI/CD Fundamentals",
    title: "What is CI/CD and why does it matter?",
    summary: "CI/CD automates building, testing, and releasing code so changes reach production quickly and safely.",
    explanation:
      "Continuous Integration means developers merge code into a shared branch frequently, with an automated pipeline building and testing every change to catch problems early. Continuous Delivery extends this by keeping the codebase always in a releasable state, while Continuous Deployment goes further and automatically ships every passing change to production. Together they replace slow, manual, error-prone releases with a repeatable pipeline. The core benefit is fast feedback: a broken build is caught in minutes, not weeks later during a manual QA pass.",
    code: "# Typical CI/CD pipeline stages\n1. checkout\n2. install dependencies\n3. lint\n4. unit tests\n5. build artifact\n6. integration tests\n7. deploy to staging\n8. smoke tests\n9. deploy to production (manual or automatic)",
    interviewQuestion: "What is the difference between continuous delivery and continuous deployment?",
  },
  {
    id: "devops-github-actions-fundamentals",
    category: "devops",
    difficulty: "Basic",
    topic: "GitHub Actions Fundamentals",
    title: "How is a GitHub Actions workflow structured?",
    summary: "GitHub Actions runs YAML-defined workflows triggered by repository events like pushes or pull requests.",
    explanation:
      "A workflow lives in .github/workflows/*.yml and defines triggers (on:), one or more jobs, and the steps each job runs. Jobs run in isolated runners (hosted or self-hosted) and can run in parallel or depend on each other via needs. Steps can run shell commands or reusable actions from the marketplace, and the runner checks out code fresh for every job. Secrets, caching, and matrix builds let you parameterize a single workflow across multiple environments.",
    code: "name: CI\non:\n  push:\n    branches: [main]\n  pull_request:\n\njobs:\n  test:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with:\n          node-version: 20\n      - run: npm ci\n      - run: npm test",
    interviewQuestion: "What triggers can start a GitHub Actions workflow, and how do you make one job wait for another?",
  },
  {
    id: "devops-docker-fundamentals",
    category: "devops",
    difficulty: "Basic",
    topic: "Docker Fundamentals",
    title: "What is a Docker image versus a container?",
    summary: "An image is a read-only template built from a Dockerfile; a container is a running instance of that image.",
    explanation:
      "A Dockerfile describes layers: a base OS/runtime, dependencies, and application code, each instruction adding a cached layer. Building the Dockerfile produces an immutable image, and docker run creates a container -- an isolated process with its own filesystem, network namespace, and process tree, sharing the host kernel. Because containers share the kernel rather than virtualizing hardware, they start in milliseconds and use far less overhead than a virtual machine. Layer caching means only changed layers get rebuilt, which is why dependency installation is usually placed before copying application source.",
    code: "FROM node:20-alpine\nWORKDIR /app\nCOPY package*.json ./\nRUN npm ci --production\nCOPY . .\nEXPOSE 3000\nCMD [\"node\", \"server.js\"]",
    interviewQuestion: "Why should COPY package.json and npm install happen before COPY . . in a Dockerfile?",
  },
  {
    id: "devops-docker-compose",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Docker Compose",
    title: "What problem does Docker Compose solve?",
    summary: "Docker Compose defines and runs multi-container applications from a single declarative YAML file.",
    explanation:
      "Instead of manually running several docker run commands with matching networks and volumes, Compose lets you describe services, their images or build contexts, environment variables, port mappings, volumes, and dependencies in one docker-compose.yml. Running docker compose up creates a shared network so services can reach each other by service name as a hostname. depends_on controls startup order but not readiness, so applications still need their own retry or wait logic for dependent services like databases. Compose is primarily aimed at local development and simple single-host deployments, not production-grade orchestration.",
    code: "version: \"3.9\"\nservices:\n  web:\n    build: .\n    ports:\n      - \"3000:3000\"\n    environment:\n      - DATABASE_URL=postgres://db:5432/app\n    depends_on:\n      - db\n  db:\n    image: postgres:16\n    volumes:\n      - pgdata:/var/lib/postgresql/data\nvolumes:\n  pgdata:",
    interviewQuestion: "Does depends_on in Docker Compose guarantee that a dependent service is ready to accept connections? Why or why not?",
  },
  {
    id: "devops-kubernetes-fundamentals",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Kubernetes Fundamentals",
    title: "What is the role of the Kubernetes control plane?",
    summary: "The control plane observes cluster state and continuously reconciles it toward the desired state you declare.",
    explanation:
      "Kubernetes clusters have a control plane (API server, etcd, scheduler, controller manager) and worker nodes running kubelet and a container runtime. You declare desired state via YAML manifests -- for example, run 3 replicas of this image -- and controllers in the control plane constantly compare actual state to desired state, taking corrective action like rescheduling a crashed pod. etcd is the cluster's source of truth, a distributed key-value store holding all object definitions. This declarative, reconciliation-loop model is what makes Kubernetes self-healing rather than requiring manual intervention.",
    code: "# core kubectl commands\nkubectl get nodes\nkubectl get pods -n default\nkubectl describe pod my-app-abc123\nkubectl apply -f deployment.yaml\nkubectl logs -f my-app-abc123\nkubectl exec -it my-app-abc123 -- sh",
    interviewQuestion: "What is etcd's role in a Kubernetes cluster, and what happens if it becomes unavailable?",
  },
  {
    id: "devops-k8s-pods-deployments",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Kubernetes Pods & Deployments",
    title: "How do Deployments manage Pods?",
    summary: "A Deployment manages ReplicaSets, which in turn ensure a specified number of identical Pod replicas are running.",
    explanation:
      "A Pod is the smallest deployable unit in Kubernetes, usually one main container plus optional sidecars sharing network and storage. You rarely create Pods directly; instead a Deployment manages a ReplicaSet that keeps the desired replica count running, and the Deployment itself manages rollout history, enabling rolling updates and rollbacks. When you update a Deployment's pod template (e.g. a new image tag), it creates a new ReplicaSet and gradually shifts traffic and replicas from the old one to the new one according to the configured strategy. Labels and selectors are how Deployments, ReplicaSets, and Services find the Pods they are responsible for.",
    code: "apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: web\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: web\n  template:\n    metadata:\n      labels:\n        app: web\n    spec:\n      containers:\n        - name: web\n          image: myorg/web:1.4.0\n          ports:\n            - containerPort: 8080",
    interviewQuestion: "What is the relationship between a Deployment, a ReplicaSet, and a Pod?",
  },
  {
    id: "devops-iac-terraform",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Infrastructure as Code (Terraform)",
    title: "How does Terraform manage infrastructure declaratively?",
    summary: "Terraform lets you describe cloud resources in HCL and reconciles real infrastructure to match that description.",
    explanation:
      "You declare resources such as VMs, networks, or databases in .tf files, and Terraform builds a dependency graph to determine creation order. It keeps a state file recording the mapping between your configuration and real-world resource IDs, and terraform plan diffs desired configuration against current state before terraform apply makes changes. Because state is the source of truth, teams store it remotely (e.g. S3 with locking) so multiple engineers don't clobber each other's changes. Modules let you package reusable, parameterized infrastructure definitions instead of duplicating resource blocks across environments.",
    code: "resource \"aws_instance\" \"web\" {\n  ami           = \"ami-0abcdef1234567890\"\n  instance_type = \"t3.micro\"\n  tags = {\n    Name = \"web-server\"\n  }\n}\n\noutput \"public_ip\" {\n  value = aws_instance.web.public_ip\n}",
    interviewQuestion: "Why is remote state with locking important when multiple engineers run Terraform against the same infrastructure?",
  },
  {
    id: "devops-container-orchestration-concepts",
    category: "devops",
    difficulty: "Advanced",
    topic: "Container Orchestration Concepts",
    title: "What core problems does container orchestration solve?",
    summary: "Orchestrators automate scheduling, scaling, networking, and self-healing for fleets of containers.",
    explanation:
      "Running a handful of containers by hand is manageable, but running hundreds across many hosts requires automated scheduling (deciding which node runs which container based on resources), service discovery (so containers can find each other as they move), health monitoring and automatic restarts, rolling updates, and scaling policies. Orchestrators like Kubernetes, Nomad, and ECS abstract the underlying hosts into a resource pool and let you describe workloads declaratively rather than imperatively placing containers. This also enables bin-packing -- efficiently fitting many workloads onto fewer machines based on requested CPU and memory -- which is difficult to do manually at scale.",
    code: "# Declarative resource requests drive scheduling decisions\nresources:\n  requests:\n    cpu: \"250m\"\n    memory: \"256Mi\"\n  limits:\n    cpu: \"500m\"\n    memory: \"512Mi\"",
    interviewQuestion: "What is the difference between a resource request and a resource limit, and how does the scheduler use requests?",
  },
  {
    id: "devops-blue-green-deployment",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Blue-Green Deployment",
    title: "How does blue-green deployment minimize release risk?",
    summary: "Blue-green deployment runs two identical production environments and switches traffic between them instantly.",
    explanation:
      "You maintain two full environments, blue (currently live) and green (idle or newly deployed). The new version is deployed and tested against green while blue keeps serving all live traffic, so there is zero customer impact during deployment. Once green is verified, a router or load balancer switches traffic over, typically instantly, and blue is kept warm as an immediate rollback target. The tradeoff is cost and complexity: you need double the infrastructure and must carefully handle stateful components like databases that both environments share.",
    code: "# Switch load balancer target group from blue to green\naws elbv2 modify-listener \\\n  --listener-arn $LISTENER_ARN \\\n  --default-actions Type=forward,TargetGroupArn=$GREEN_TG_ARN\n\n# Rollback is just switching back\naws elbv2 modify-listener \\\n  --listener-arn $LISTENER_ARN \\\n  --default-actions Type=forward,TargetGroupArn=$BLUE_TG_ARN",
    interviewQuestion: "What makes rollback so fast in a blue-green deployment, and what's the main cost tradeoff?",
  },
  {
    id: "devops-canary-deployment",
    category: "devops",
    difficulty: "Advanced",
    topic: "Canary Deployment",
    title: "How does a canary release limit the blast radius of a bad deploy?",
    summary: "Canary deployment routes a small percentage of real traffic to the new version before a full rollout.",
    explanation:
      "Instead of switching all traffic at once, a canary release sends, say, 5% of production traffic to the new version while 95% continues hitting the stable version. Metrics like error rate, latency, and business KPIs are compared between canary and baseline, and if the canary looks healthy, traffic is gradually increased until it reaches 100%; if it looks unhealthy, traffic is shifted back and the release is aborted automatically. This requires fine-grained traffic splitting, typically via a service mesh or ingress controller, and good observability to make the promote/rollback decision quickly. It limits the blast radius of a bad release to a small slice of users instead of everyone.",
    code: "apiVersion: networking.istio.io/v1beta1\nkind: VirtualService\nmetadata:\n  name: web\nspec:\n  hosts: [\"web\"]\n  http:\n    - route:\n        - destination:\n            host: web\n            subset: stable\n          weight: 95\n        - destination:\n            host: web\n            subset: canary\n          weight: 5",
    interviewQuestion: "What signals would you monitor to decide whether to promote or abort a canary release?",
  },
  {
    id: "devops-rolling-deployment",
    category: "devops",
    difficulty: "Basic",
    topic: "Rolling Deployment",
    title: "How does a rolling deployment update instances gradually?",
    summary: "Rolling deployment replaces old instances with new ones incrementally, keeping the service available throughout.",
    explanation:
      "Rather than taking everything down and bringing up the new version (which causes downtime) or running two full parallel environments (blue-green), a rolling deployment updates a few instances at a time. As each new instance passes its health check, traffic shifts to it and an old instance is terminated, repeating until all instances run the new version. Kubernetes Deployments do this by default, controlled by maxSurge (how many extra pods can be created above the desired count) and maxUnavailable (how many can be down at once). The main downside is that both old and new versions serve traffic simultaneously during the rollout, so the two versions must be compatible.",
    code: "spec:\n  strategy:\n    type: RollingUpdate\n    rollingUpdate:\n      maxSurge: 1\n      maxUnavailable: 0\n  replicas: 5",
    interviewQuestion: "During a rolling deployment, both old and new versions serve traffic at once -- what does that imply about API and schema compatibility?",
  },
  {
    id: "devops-monitoring-alerting-prometheus-grafana",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Monitoring & Alerting (Prometheus/Grafana)",
    title: "How do Prometheus and Grafana fit together in a monitoring stack?",
    summary: "Prometheus scrapes and stores time-series metrics, while Grafana visualizes them and Alertmanager routes alerts.",
    explanation:
      "Prometheus periodically pulls (scrapes) metrics from HTTP endpoints exposed by instrumented applications or exporters, storing them as time series labeled with key-value pairs. PromQL is the query language used to aggregate and analyze this data, both for dashboards and for alerting rules. Alerting rules evaluate PromQL expressions continuously and fire alerts to Alertmanager, which handles deduplication, grouping, and routing to Slack, PagerDuty, email, etc. Grafana connects to Prometheus (and other data sources) as a visualization layer, turning PromQL queries into dashboards, but it does not itself store metrics.",
    code: "# Prometheus alerting rule\ngroups:\n  - name: web-alerts\n    rules:\n      - alert: HighErrorRate\n        expr: rate(http_requests_total{status=~\"5..\"}[5m]) > 0.05\n        for: 10m\n        labels:\n          severity: critical\n        annotations:\n          summary: \"Error rate above 5% for 10 minutes\"",
    interviewQuestion: "What is the difference between Prometheus pulling metrics versus a push-based metrics system, and when might push be preferable?",
  },
  {
    id: "devops-log-aggregation-elk",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Log Aggregation (ELK Stack)",
    title: "How does the ELK stack centralize logs from many services?",
    summary: "ELK (Elasticsearch, Logstash, Kibana) collects, parses, indexes, and visualizes logs from distributed systems.",
    explanation:
      "In a distributed system, logs scattered across many hosts and containers are unsearchable without centralization. Logstash (or lightweight shippers like Filebeat) collects and parses raw log lines into structured JSON documents, which are indexed into Elasticsearch for fast full-text and field-based search. Kibana provides the UI to search, filter, and build dashboards over that indexed data. A common pattern is Filebeat on each host shipping logs to Logstash for parsing/enrichment, then into Elasticsearch, keeping the resource-heavy parsing off the application hosts.",
    code: "# Filebeat config shipping container logs to Logstash\nfilebeat.inputs:\n  - type: container\n    paths:\n      - \"/var/lib/docker/containers/*/*.log\"\noutput.logstash:\n  hosts: [\"logstash:5044\"]",
    interviewQuestion: "Why would you use Filebeat plus Logstash instead of writing directly to Elasticsearch from every application?",
  },
  {
    id: "devops-load-balancers",
    category: "devops",
    difficulty: "Basic",
    topic: "Load Balancers in DevOps",
    title: "What does a load balancer do and what algorithms does it use?",
    summary: "A load balancer distributes incoming traffic across multiple backend instances to improve availability and scale.",
    explanation:
      "Load balancers sit in front of a pool of servers and distribute requests using strategies like round robin, least connections, or weighted routing based on server capacity. They also perform health checks, automatically removing unhealthy instances from rotation so traffic never reaches a dead backend. Layer 4 load balancers operate on IP/TCP information and are fast but protocol-agnostic, while Layer 7 load balancers understand HTTP and can route based on path, headers, or hostname, enabling more advanced patterns like canary routing. Load balancers are also where TLS termination commonly happens, offloading encryption from application servers.",
    code: "upstream backend {\n    least_conn;\n    server 10.0.1.10:8080;\n    server 10.0.1.11:8080;\n    server 10.0.1.12:8080 backup;\n}\n\nserver {\n    listen 80;\n    location / {\n        proxy_pass http://backend;\n    }\n}",
    interviewQuestion: "What is the difference between Layer 4 and Layer 7 load balancing, and when would you need Layer 7?",
  },
  {
    id: "devops-reverse-proxies-nginx",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Reverse Proxies (Nginx)",
    title: "What role does Nginx play as a reverse proxy?",
    summary: "A reverse proxy sits between clients and backend servers, forwarding requests while hiding backend topology.",
    explanation:
      "Unlike a forward proxy that represents clients, a reverse proxy like Nginx represents servers: clients only ever talk to the proxy, which forwards requests to one or more backend applications. This enables TLS termination, request buffering, caching, gzip compression, and centralized access logging without touching application code. Nginx can also route based on path or hostname to different backend services, effectively acting as a lightweight API gateway. Because it's a single entry point, it's also where you'd add rate limiting or IP allow-listing before requests ever reach application servers.",
    code: "server {\n    listen 443 ssl;\n    server_name api.example.com;\n\n    ssl_certificate     /etc/ssl/certs/example.crt;\n    ssl_certificate_key /etc/ssl/private/example.key;\n\n    location /api/ {\n        proxy_pass http://app_backend:4000/;\n        proxy_set_header Host $host;\n        proxy_set_header X-Real-IP $remote_addr;\n    }\n}",
    interviewQuestion: "Why would you terminate TLS at the reverse proxy instead of in each backend application?",
  },
  {
    id: "devops-secrets-management-cicd",
    category: "devops",
    difficulty: "Advanced",
    topic: "Secrets Management in CI/CD",
    title: "How should secrets be handled safely in a CI/CD pipeline?",
    summary: "Secrets should be injected at runtime from a secure store, never hardcoded or committed into pipeline configs.",
    explanation:
      "Secrets (API keys, DB passwords, signing keys) must never appear in source control or plaintext pipeline YAML. CI platforms provide encrypted secret stores (GitHub Actions secrets, GitLab CI variables) that inject values as environment variables at runtime, masked from logs. For more sophisticated setups, pipelines authenticate to a dedicated secrets manager (Vault, AWS Secrets Manager) using short-lived tokens (e.g. OIDC federation) rather than long-lived static credentials, fetching secrets just-in-time and never persisting them to disk unencrypted. Least privilege matters too: a pipeline deploying to staging shouldn't hold production credentials.",
    code: "jobs:\n  deploy:\n    runs-on: ubuntu-latest\n    permissions:\n      id-token: write\n    steps:\n      - uses: aws-actions/configure-aws-credentials@v4\n        with:\n          role-to-assume: arn:aws:iam::123456789012:role/deploy-role\n          aws-region: us-east-1\n      - run: aws s3 sync ./dist s3://my-bucket",
    interviewQuestion: "Why is OIDC-based cloud authentication in CI generally safer than storing long-lived AWS access keys as secrets?",
  },
  {
    id: "devops-environment-config-management",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Environment Configuration Management",
    title: "How should configuration differ across environments without duplicating code?",
    summary: "Environment-specific config should be externalized from code so the same build artifact runs anywhere.",
    explanation:
      "The twelve-factor app principle is to store config in the environment, not in code, so the exact same build artifact can be promoted from staging to production without rebuilding. This is typically implemented with environment variables, per-environment config files layered over defaults, or a config service like Consul or AWS Parameter Store. It's important to distinguish config (varies per environment, e.g. DB host) from secrets (sensitive, e.g. DB password) even though both often arrive as environment variables, because secrets need stricter access control and rotation. Baking environment-specific values into a Docker image defeats the purpose of building once and deploying everywhere.",
    code: "# .env.production (not committed)\nDATABASE_URL=postgres://prod-db:5432/app\nLOG_LEVEL=warn\n\n# .env.staging\nDATABASE_URL=postgres://staging-db:5432/app\nLOG_LEVEL=debug",
    interviewQuestion: "What does 'build once, deploy everywhere' mean, and how does baking config into an image violate it?",
  },
  {
    id: "devops-health-checks-readiness-probes",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Health Checks & Readiness Probes",
    title: "What is the difference between a liveness and a readiness probe?",
    summary: "Liveness probes detect whether a container should be restarted; readiness probes detect whether it should receive traffic.",
    explanation:
      "A liveness probe checks whether an application is alive but potentially stuck (e.g. deadlocked); if it fails, the orchestrator kills and restarts the container. A readiness probe checks whether the application is ready to serve requests -- for example, whether it has finished loading a cache or connecting to a database; if it fails, the container is temporarily removed from load balancer rotation without being restarted. Confusing the two is a common bug: using a liveness-style check for readiness can cause a container that's merely warming up to be killed and restarted in a crash loop instead of just being held out of rotation.",
    code: "livenessProbe:\n  httpGet:\n    path: /healthz\n    port: 8080\n  initialDelaySeconds: 15\n  periodSeconds: 10\nreadinessProbe:\n  httpGet:\n    path: /ready\n    port: 8080\n  periodSeconds: 5\n  failureThreshold: 3",
    interviewQuestion: "If a pod is slow to warm up its cache on startup, which probe should reflect that, and what happens if you get it wrong?",
  },
  {
    id: "devops-auto-scaling",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Auto-Scaling",
    title: "How does horizontal auto-scaling decide when to add or remove instances?",
    summary: "Auto-scaling adjusts the number of running instances based on observed metrics like CPU, memory, or request rate.",
    explanation:
      "Horizontal Pod Autoscaling (or an ASG in AWS) watches a target metric against a threshold and adjusts replica count to keep it near that target -- for example, scaling out when average CPU exceeds 70%. Scaling up typically needs to happen faster than scaling down to absorb sudden traffic spikes, so cooldown periods and stabilization windows prevent flapping (rapid scale up/down cycles). Metrics-based scaling reacts after load has already increased, so systems with very spiky traffic sometimes combine it with scheduled scaling for known patterns or predictive scaling. Vertical scaling (bigger instances) is a separate strategy, often combined with horizontal scaling but limited by the max size of a single machine.",
    code: "apiVersion: autoscaling/v2\nkind: HorizontalPodAutoscaler\nmetadata:\n  name: web-hpa\nspec:\n  scaleTargetRef:\n    apiVersion: apps/v1\n    kind: Deployment\n    name: web\n  minReplicas: 2\n  maxReplicas: 10\n  metrics:\n    - type: Resource\n      resource:\n        name: cpu\n        target:\n          type: Utilization\n          averageUtilization: 70",
    interviewQuestion: "What causes autoscaler flapping, and what mechanisms prevent it?",
  },
  {
    id: "devops-cloud-providers-overview",
    category: "devops",
    difficulty: "Basic",
    topic: "Cloud Providers Overview (AWS/GCP/Azure)",
    title: "What are the core service categories shared across AWS, GCP, and Azure?",
    summary: "Major cloud providers offer roughly equivalent compute, storage, networking, and managed database services under different names.",
    explanation:
      "All three major clouds provide the same underlying building blocks: virtual machines (EC2, Compute Engine, Azure VMs), object storage (S3, Cloud Storage, Blob Storage), managed relational databases (RDS, Cloud SQL, Azure SQL), managed Kubernetes (EKS, GKE, AKS), and serverless functions (Lambda, Cloud Functions, Azure Functions). The differences that matter in practice are pricing models, regional availability, IAM structure, and how deeply integrated their tooling is (e.g. GCP's networking is famously simpler than AWS's VPC model). Multi-cloud strategies trade vendor lock-in avoidance for real operational complexity, since each provider's APIs, quotas, and failure modes differ.",
    code: "# Rough service equivalence table\n# AWS         GCP              Azure\n# EC2         Compute Engine   Virtual Machines\n# S3          Cloud Storage    Blob Storage\n# RDS         Cloud SQL        Azure SQL Database\n# EKS         GKE              AKS\n# Lambda      Cloud Functions  Azure Functions",
    interviewQuestion: "What are the practical tradeoffs of designing a system to be multi-cloud from day one?",
  },
  {
    id: "devops-serverless-deployment",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Serverless Deployment",
    title: "What operational tradeoffs come with serverless functions?",
    summary: "Serverless deployment runs code on-demand without managing servers, trading control for automatic scaling and pay-per-use pricing.",
    explanation:
      "With serverless (e.g. AWS Lambda), you deploy a function and the platform handles provisioning, scaling to zero, and scaling out automatically per request, and you're billed per invocation and duration rather than for idle capacity. The tradeoff is cold starts -- latency added when a new execution environment must be initialized -- and constraints like execution time limits, limited local disk, and no persistent in-memory state between invocations. Serverless shines for spiky, event-driven workloads (image processing, webhooks) but is a poor fit for long-running processes or workloads needing consistent low latency without cold-start mitigation like provisioned concurrency.",
    code: "# serverless.yml (Serverless Framework)\nfunctions:\n  processUpload:\n    handler: handler.processUpload\n    events:\n      - s3:\n          bucket: uploads-bucket\n          event: s3:ObjectCreated:*\n    timeout: 30\n    memorySize: 512",
    interviewQuestion: "What causes a Lambda cold start, and what techniques reduce its impact?",
  },
  {
    id: "devops-cdn-configuration",
    category: "devops",
    difficulty: "Intermediate",
    topic: "CDN Configuration",
    title: "How does a CDN reduce latency and origin load?",
    summary: "A CDN caches content at edge locations close to users, serving it without a round trip to the origin server.",
    explanation:
      "A Content Delivery Network runs a network of geographically distributed edge servers (points of presence) that cache static and sometimes dynamic content close to end users, reducing latency and offloading traffic from the origin server. Cache behavior is controlled by HTTP headers like Cache-Control and ETag, and cache keys can incorporate query strings or headers to serve different variants correctly. A critical operational concern is cache invalidation: when you deploy a new version of an asset, you must either invalidate/purge the CDN cache or use versioned/hashed filenames so new deploys naturally get new cache keys, avoiding stale content being served indefinitely.",
    code: "# Cache-Control header strategy\n# Hashed, immutable assets: cache forever\nCache-Control: public, max-age=31536000, immutable\n\n# HTML entry point: always revalidate\nCache-Control: no-cache",
    interviewQuestion: "Why is cache invalidation described as one of the two hard problems in computer science, and how do hashed filenames sidestep it?",
  },
  {
    id: "devops-dns-management",
    category: "devops",
    difficulty: "Basic",
    topic: "DNS Management",
    title: "What do the common DNS record types do?",
    summary: "DNS translates domain names to IP addresses and other resource data using a hierarchy of record types.",
    explanation:
      "An A record maps a hostname to an IPv4 address, and AAAA does the same for IPv6. A CNAME record aliases one hostname to another and cannot coexist with other records at the same name. MX records route email for a domain, and TXT records hold arbitrary text often used for domain verification or SPF/DKIM email authentication. TTL (time-to-live) controls how long resolvers cache a record; lowering TTL before a planned change (like a migration) lets you propagate updates faster, since resolvers won't serve stale cached data as long.",
    code: "; Example zone file excerpt\napp.example.com.   300  IN  A      203.0.113.10\nwww.example.com.    300  IN  CNAME  app.example.com.\nexample.com.        3600 IN  MX  10 mail.example.com.\nexample.com.         3600 IN  TXT \"v=spf1 include:_spf.google.com ~all\"",
    interviewQuestion: "Why would you lower a DNS record's TTL before performing a migration, and when should you do it?",
  },
  {
    id: "devops-ssl-certificate-management",
    category: "devops",
    difficulty: "Intermediate",
    topic: "SSL Certificate Management",
    title: "How does automated certificate renewal with Let's Encrypt work?",
    summary: "TLS certificates prove domain ownership and enable encryption, and modern tooling automates their issuance and renewal.",
    explanation:
      "A certificate authority signs a certificate binding a public key to a domain, and clients trust it because the CA's root is in their trust store. Let's Encrypt issues free, short-lived (90-day) certificates via the ACME protocol, proving domain control through an HTTP-01 challenge (serving a token at a well-known URL) or DNS-01 challenge (creating a TXT record), which matters for wildcard certificates or hosts that can't expose HTTP. Because certificates expire, tools like Certbot or cert-manager in Kubernetes automate renewal well before expiry, and an expired certificate is one of the most common causes of a sudden, embarrassing full outage.",
    code: "# cert-manager Certificate resource (Kubernetes)\napiVersion: cert-manager.io/v1\nkind: Certificate\nmetadata:\n  name: example-tls\nspec:\n  secretName: example-tls\n  dnsNames:\n    - example.com\n  issuerRef:\n    name: letsencrypt-prod\n    kind: ClusterIssuer",
    interviewQuestion: "What is the difference between HTTP-01 and DNS-01 ACME challenges, and when do you need DNS-01?",
  },
  {
    id: "devops-zero-downtime-deployment",
    category: "devops",
    difficulty: "Advanced",
    topic: "Zero-Downtime Deployment",
    title: "What must be true across the stack to achieve true zero-downtime deploys?",
    summary: "Zero-downtime deployment requires coordinated care across load balancing, connection draining, and backward-compatible changes.",
    explanation:
      "Zero downtime is not just a deployment strategy (rolling, blue-green, canary) -- it requires every layer to cooperate. New instances must pass readiness checks before receiving traffic, and old instances need graceful shutdown / connection draining so in-flight requests complete before the process exits (SIGTERM handling matters here). Database migrations must be backward-compatible during the rollout window, since old and new code may run simultaneously against the same schema -- this is why additive, multi-step migrations (add column, backfill, deploy code using it, later drop old column) are preferred over one-shot destructive changes. Load balancer health checks must also be tuned so they detect and route around instances mid-shutdown.",
    code: "process.on('SIGTERM', async () => {\n  server.close(() => {\n    console.log('HTTP server closed');\n  });\n  await drainInFlightRequests();\n  await db.disconnect();\n  process.exit(0);\n});",
    interviewQuestion: "Why can't you simply DROP a column in the same deploy that stops using it, if you want zero downtime?",
  },
  {
    id: "devops-rollback-strategies",
    category: "devops",
    difficulty: "Intermediate",
    topic: "Rollback Strategies",
    title: "What are the tradeoffs between rolling back code versus rolling forward?",
    summary: "Rollback reverts to a previously known-good version quickly, while rolling forward ships a fix on top of the current state.",
    explanation:
      "The fastest recovery from a bad deploy is usually rollback: redeploying the last known-good artifact, which Kubernetes supports natively via rollout history. But rollback isn't always safe -- if the bad deploy included a database migration, rolling back code while the schema has already changed can break things worse, so some teams prefer rolling forward with a hotfix instead. Good practice is keeping deploy artifacts immutable and versioned (not overwriting the previous build) so rollback is always available as an option, and designing migrations to be backward-compatible specifically so rollback remains safe even after a schema change.",
    code: "# Kubernetes rollback\nkubectl rollout history deployment/web\nkubectl rollout undo deployment/web\nkubectl rollout undo deployment/web --to-revision=3\nkubectl rollout status deployment/web",
    interviewQuestion: "Why might rolling back be unsafe after a deploy that included a destructive database migration?",
  },
  {
    id: "devops-gitops",
    category: "devops",
    difficulty: "Advanced",
    topic: "GitOps",
    title: "What does it mean for Git to be the source of truth in GitOps?",
    summary: "GitOps uses a Git repository as the single source of truth for declarative infrastructure and app state, reconciled by an agent.",
    explanation:
      "In GitOps, the desired state of a cluster (which manifests, which image tags, which config) lives entirely in a Git repository, and an in-cluster controller like Argo CD or Flux continuously compares live cluster state to that repo and reconciles any drift automatically, pulling rather than being pushed to. Deploys happen by opening a pull request that changes a manifest (e.g. bumping an image tag), which gives you code review, audit history, and easy rollback (git revert) for infrastructure changes the same way you'd get for application code. This pull-based model is also more secure than traditional push-based CI/CD, since the cluster doesn't need to expose credentials to an external CI system -- the in-cluster agent pulls from Git instead.",
    code: "# Argo CD Application pointing at a Git repo of manifests\napiVersion: argoproj.io/v1alpha1\nkind: Application\nmetadata:\n  name: web\nspec:\n  source:\n    repoURL: https://github.com/org/k8s-manifests\n    path: apps/web\n    targetRevision: main\n  destination:\n    server: https://kubernetes.default.svc\n    namespace: production\n  syncPolicy:\n    automated:\n      selfHeal: true",
    interviewQuestion: "Why is a pull-based GitOps model considered more secure than a traditional CI system pushing credentials into the cluster?",
  },
  {
    id: "devops-artifact-registries",
    category: "devops",
    difficulty: "Basic",
    topic: "Artifact Registries",
    title: "What is an artifact registry used for in a CI/CD pipeline?",
    summary: "An artifact registry stores versioned build outputs like container images or packages so they can be reliably deployed later.",
    explanation:
      "After CI builds and tests an artifact (a Docker image, npm package, or compiled binary), it's pushed to a registry -- Docker Hub, Amazon ECR, GitHub Container Registry, Artifactory -- tagged with an identifier, ideally an immutable one like a Git SHA rather than a mutable tag like 'latest'. Deployment steps then pull that exact, already-tested artifact rather than rebuilding it, which guarantees what you tested is exactly what you deploy. Registries also enforce access control and, in mature setups, scan images for known vulnerabilities before they're allowed to be pulled into production.",
    code: "# Tag and push with an immutable, traceable tag\ndocker build -t ghcr.io/org/web:$(git rev-parse --short HEAD) .\ndocker push ghcr.io/org/web:$(git rev-parse --short HEAD)\n\n# Deploy pulls that exact digest, not \"latest\"\nkubectl set image deployment/web web=ghcr.io/org/web:a1b2c3d",
    interviewQuestion: "Why is tagging images with 'latest' risky for production deployments compared to tagging with a Git SHA?",
  },
  {
    id: "devops-build-pipeline-optimization",
    category: "devops",
    difficulty: "Advanced",
    topic: "Build Pipelines Optimization",
    title: "What techniques speed up slow CI pipelines?",
    summary: "Pipeline speed comes from caching dependencies, parallelizing independent steps, and avoiding redundant work.",
    explanation:
      "The biggest wins usually come from dependency caching (node_modules, pip cache, Docker layer cache) keyed on a lockfile hash, so unchanged dependencies aren't reinstalled every run. Running independent jobs -- lint, unit tests, type-check -- in parallel rather than sequentially reduces wall-clock time even though total compute stays similar. Test suites can be split across parallel shards, and monorepos benefit hugely from only building/testing packages actually affected by a given change (affected-graph tooling like Nx or Turborepo) instead of rebuilding everything on every commit. Docker builds specifically benefit from multi-stage builds and BuildKit cache mounts to avoid re-downloading dependencies on every image build.",
    code: "- uses: actions/cache@v4\n  with:\n    path: ~/.npm\n    key: npm-${{ hashFiles('package-lock.json') }}\n    restore-keys: npm-\n\n# Docker BuildKit cache mount\nRUN --mount=type=cache,target=/root/.npm \\\n    npm ci",
    interviewQuestion: "In a monorepo, how would you avoid rebuilding and re-testing every package on every commit?",
  },
  {
    id: "devops-incident-response-basics",
    category: "devops",
    difficulty: "Tricky",
    topic: "Incident Response Basics",
    title: "Why should mitigation come before root-cause analysis during an active incident?",
    summary: "During an incident, the priority is restoring service quickly; deep root-cause investigation happens afterward in a postmortem.",
    explanation:
      "The instinct to fully understand why something broke before fixing it can prolong customer impact unnecessarily -- if a rollback would likely resolve the issue, do it first and investigate the root cause once service is stable, rather than debugging live in production while users are affected. Good incident response defines clear roles (an incident commander coordinating response, separate from whoever is heads-down debugging) so communication doesn't get lost. A blameless postmortem afterward documents the timeline, root cause, and concrete follow-up actions -- the goal is fixing systemic gaps, not assigning fault, because blame-driven postmortems make engineers hide information in future incidents instead of surfacing it.",
    code: "# Incident response checklist (abbreviated)\n1. Acknowledge alert, declare incident\n2. Assign incident commander\n3. Mitigate first (rollback / feature flag off / scale up)\n4. Communicate status to stakeholders every 15-30 min\n5. Confirm resolution, monitor for recurrence\n6. Write blameless postmortem with action items",
    interviewQuestion: "You're debugging a production incident and you're not sure of the root cause, but a rollback would probably fix it. What do you do first, and why?",
  },
];
